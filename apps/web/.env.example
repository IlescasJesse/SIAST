VITE_API_URL=http://localhost:5101

# Visor 3D: en dev, vacío = usa fallback http://<hostname>:5174 (apps/modelado-3d corriendo aparte).
# En producción DEBE ser "/visor3d/" (base path del build, mismo dominio) — ver apps/modelado-3d/vite.config.js
VITE_VIEWER_URL=/visor3d/
