---
phase: 04-metricas-operacionales
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - apps/api/src/services/metricas.service.ts
  - apps/api/src/controllers/metricas.controller.ts
  - apps/api/src/routes/metricas.routes.ts
autonomous: true
requirements:
  - MET-01
  - MET-02
  - MET-03
  - MET-04

must_haves:
  truths:
    - "GET /api/metricas?tipo=area retorna MetricasGlobalResponse con datos reales de DB"
    - "GET /api/metricas?tipo=tecnico con RESPONSABLE_* siempre usa areaSoporteId del JWT (no puede ser forzado por query param)"
    - "GET /api/metricas?tipo=proceso con TECNICO_* fuerza tecnicoId = user.id (no puede ver datos de otro técnico)"
    - "Fechas malformadas retornan 400 antes de llegar a Prisma"
    - "bigint de $queryRaw nunca llega a JSON.stringify (siempre convertido a Number)"
  artifacts:
    - path: "apps/api/src/services/metricas.service.ts"
      provides: "Toda la aggregation logic con Prisma — SLA, tiempos, tendencia diaria, carga técnicos"
      exports: ["obtenerMetricasGlobal", "obtenerMetricasPorArea", "obtenerMetricasPorTecnico"]
    - path: "apps/api/src/controllers/metricas.controller.ts"
      provides: "Endpoint único GET /api/metricas con Zod validation y role scoping"
      contains: "export const obtener"
    - path: "apps/api/src/routes/metricas.routes.ts"
      provides: "Ruta única con todos los roles Phase 3+ incluidos"
      contains: "RESPONSABLE_TI"
  key_links:
    - from: "apps/api/src/routes/metricas.routes.ts"
      to: "apps/api/src/controllers/metricas.controller.ts"
      via: "router.get('/', rolesMetricas, ctrl.obtener)"
      pattern: "ctrl\\.obtener"
    - from: "apps/api/src/controllers/metricas.controller.ts"
      to: "apps/api/src/services/metricas.service.ts"
      via: "metricasService.obtener(params)"
      pattern: "metricasService\\.obtener"
    - from: "apps/api/src/services/metricas.service.ts"
      to: "prisma.ticket / prisma.$queryRaw"
      via: "Prisma aggregation queries"
      pattern: "prisma\\.ticket\\.groupBy|prisma\\.\\$queryRaw"
---

<objective>
Implementar el backend completo del endpoint unificado `GET /api/metricas` con:
- Servicio `metricas.service.ts` con aggregation real vía Prisma (SLA, tiempos, tendencia diaria, carga técnicos, primera respuesta)
- Controlador refactorizado con Zod validation de query params, role scoping automático, y endpoint único
- Ruta actualizada con todos los roles de Phase 3+ incluidos

Propósito: Reemplaza los 3 endpoints separados legacy con mock-style aggregation por un único endpoint
paramétrico con datos reales y scoping de seguridad correcto.

Salida: `GET /api/metricas?tipo=area|tecnico|proceso` funcional con datos MySQL reales.
</objective>

<execution_context>
@C:\Users\ilesm\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\ilesm\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\ilesm\Documents\SIAST\.planning\PROJECT.md
@C:\Users\ilesm\Documents\SIAST\.planning\ROADMAP.md
@C:\Users\ilesm\Documents\SIAST\.planning\STATE.md
@C:\Users\ilesm\Documents\SIAST\.planning\phases\04-metricas-operacionales\04-CONTEXT.md
@C:\Users\ilesm\Documents\SIAST\.planning\phases\04-metricas-operacionales\04-RESEARCH.md
@C:\Users\ilesm\Documents\SIAST\.planning\phases\04-metricas-operacionales\04-PATTERNS.md
@C:\Users\ilesm\Documents\SIAST\.planning\phases\04-metricas-operacionales\04-01-SUMMARY.md

<interfaces>
<!-- Tipos del endpoint unificado (creados en Plan 01) -->
```typescript
// De packages/shared/src/index.ts
export interface MetricasGlobalResponse {
  tipo: "area";
  totalTickets: number;
  ticketsActivos: number;
  ticketsResueltos: number;
  slaGlobal: number;
  tiempoPromedioHoras: number | null;
  tendenciaDiaria: TendenciaDia[];
  distribucionCategoria: DistribucionCategoria[];
  comparativoPorArea: Array<{ areaNombre: string; areaSoporteId: number; total: number; resueltos: number }>;
  eficienciaResponsables: EficienciaResponsable[];
}

export interface MetricasPorAreaResponse {
  tipo: "tecnico";
  areaId: number;
  areaNombre: string;
  ticketsActivos: number;
  tiempoPromedioHoras: number | null;
  slaGlobal: number;
  ticketsReabiertos: number;
  tendenciaDiaria: TendenciaDia[];
  distribucionSubcategoria: DistribucionCategoria[];
  cargaTecnicos: Array<{ tecnicoNombre: string; tecnicoId: number; activos: number; completados: number }>;
  rendimientoTecnicos: RendimientoTecnico[];
}

export interface MetricasPorTecnicoResponse {
  tipo: "proceso";
  tecnicoId: number;
  tecnicoNombre: string;
  ticketsCompletados: number;
  tiempoPromedioHoras: number | null;
  tiemprimeraRespuestaHoras: number | null;
  ratioResueltosCancelados: number | null;
  tendenciaProductividad: TendenciaDia[];
  comparativaVsArea: Array<{ label: string; tecnico: number; promedioArea: number }>;
  distribucionResultado: Array<{ name: string; value: number; color: string }>;
}
```

<!-- AuthRequest tipo del proyecto -->
```typescript
// apps/api/src/types/index.ts (observado en otros controladores)
export interface AuthRequest extends Request {
  user?: {
    id: number;
    rol: string;
    jti?: string;
    areaSoporteId?: number; // solo RESPONSABLE_*
  };
}
```

<!-- Patrón de controlador existente -->
```typescript
// apps/api/src/controllers/tickets.controller.ts
export const listar = async (req: AuthRequest, res: Response, next: NextFunction) => {
  try {
    const query: Record<string, string> = {};
    for (const [k, v] of Object.entries(req.query)) {
      if (typeof v === "string") query[k] = v;
      else if (Array.isArray(v) && typeof v[0] === "string") query[k] = v[0];
    }
    const result = await ticketsService.listarTickets(req.user!, query);
    res.json(result);
  } catch (err) {
    next(err);
  }
};
```

<!-- ROLES_RESPONSABLE exportado desde roles.middleware.ts -->
```typescript
export const ROLES_RESPONSABLE = [
  "RESPONSABLE_TI",
  "RESPONSABLE_REDES",
  "RESPONSABLE_MANTENIMIENTO",
  "RESPONSABLE_RECURSOS_MATERIALES",
] as const;
```

<!-- SLA metas hardcodeadas (D-02) -->
```typescript
const SLA_HORAS: Record<string, number> = {
  TECNOLOGIAS: 24,
  SERVICIOS: 48,
  RECURSOS_MATERIALES: 72,
};
```

<!-- Patrón $queryRaw con bigint conversion obligatoria -->
```typescript
type DayRow = { dia: string; creados: bigint; resueltos: bigint };
const rows = await prisma.$queryRaw<DayRow[]>`
  SELECT DATE(created_at) AS dia, COUNT(*) AS creados,
    SUM(CASE WHEN estado = 'RESUELTO' THEN 1 ELSE 0 END) AS resueltos
  FROM tickets WHERE activo = true
    AND created_at >= ${fechaInicio}
    AND created_at <= ${fechaFin}
  GROUP BY DATE(created_at) ORDER BY dia ASC
`;
// OBLIGATORIO: convertir bigint antes de retornar
return rows.map(r => ({ dia: String(r.dia), creados: Number(r.creados), resueltos: Number(r.resueltos) }));
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: Crear metricas.service.ts con aggregation real vía Prisma</name>
  <files>
    apps/api/src/services/metricas.service.ts
  </files>
  <read_first>
    - apps/api/src/services/tickets.service.ts — leer las primeras 100 líneas para import pattern, patrón de filtro `where`, manejo de roles, y soft delete `activo: true`
    - apps/api/src/controllers/metricas.controller.ts — leer completo para entender los 3 endpoints legacy que se reemplazan y reutilizar las queries que ya funcionan (porCategoria, porSubcategoria raw, SLA de procesos)
    - apps/api/src/middleware/roles.middleware.ts — leer para importar ROLES_RESPONSABLE
    - packages/database/prisma/schema.prisma — verificar campos de Ticket (fechaAsignacion, fechaResolucion), HistorialTicket (estadoNuevo, usuarioId, createdAt), y AreaSoporte (nombre, subcategorias)
  </read_first>
  <action>
Crear `apps/api/src/services/metricas.service.ts` NUEVO (no existe actualmente). El archivo implementa
tres funciones exportadas consumidas por el controlador:

```typescript
import { prisma } from "../config/database.js";
import { Prisma, Rol } from "@prisma/client";
import type {
  MetricasGlobalResponse, MetricasPorAreaResponse, MetricasPorTecnicoResponse,
  TendenciaDia, EficienciaResponsable, RendimientoTecnico, DistribucionCategoria,
} from "@stf/shared";

// ── Constantes SLA (D-02) ─────────────────────────────────────────────────────
const SLA_HORAS: Record<string, number> = {
  TECNOLOGIAS: 24,
  SERVICIOS: 48,
  RECURSOS_MATERIALES: 72,
};

// ── Helper: construir where con rango de fechas ───────────────────────────────
function buildDateWhere(fechaInicio?: Date, fechaFin?: Date) {
  if (!fechaInicio && !fechaFin) return {};
  const createdAt: Record<string, Date> = {};
  if (fechaInicio) createdAt.gte = fechaInicio;
  if (fechaFin) {
    const fin = new Date(fechaFin);
    fin.setHours(23, 59, 59, 999);
    createdAt.lte = fin;
  }
  return { createdAt };
}

// ── Helper: calcular SLA % ────────────────────────────────────────────────────
async function calcularSLA(where: Record<string, unknown>): Promise<number> {
  const resueltos = await prisma.ticket.findMany({
    where: { ...where, estado: "RESUELTO", fechaResolucion: { not: null }, activo: true },
    select: { categoria: true, createdAt: true, fechaResolucion: true },
  });
  if (resueltos.length === 0) return 0;
  let cumplieron = 0;
  for (const t of resueltos) {
    const metaMs = (SLA_HORAS[t.categoria] ?? 24) * 3_600_000;
    const diff = t.fechaResolucion!.getTime() - t.createdAt.getTime();
    if (diff <= metaMs) cumplieron++;
  }
  return Math.round((cumplieron / resueltos.length) * 100);
}

// ── Helper: tendencia diaria via $queryRaw ────────────────────────────────────
// CRÍTICO: bigint → Number antes de retornar (pitfall 1 del RESEARCH)
async function calcularTendencia(
  fechaInicio: Date,
  fechaFin: Date,
  areaId?: number,
): Promise<TendenciaDia[]> {
  type DayRow = { dia: string; creados: bigint; resueltos: bigint };
  const rows = await prisma.$queryRaw<DayRow[]>`
    SELECT
      DATE(t.created_at) AS dia,
      COUNT(*) AS creados,
      SUM(CASE WHEN t.estado = 'RESUELTO' THEN 1 ELSE 0 END) AS resueltos
    FROM tickets t
    ${areaId
      ? Prisma.sql`JOIN usuarios u ON t.tecnico_id = u.id AND u.area_soporte_id = ${areaId}`
      : Prisma.empty}
    WHERE t.activo = true
      AND t.created_at >= ${fechaInicio}
      AND t.created_at <= ${fechaFin}
    GROUP BY DATE(t.created_at)
    ORDER BY dia ASC
  `;
  return rows.map((r) => ({
    dia: String(r.dia),
    creados: Number(r.creados),
    resueltos: Number(r.resueltos),
  }));
}

// ── Helper: tiempo promedio resolución (horas) ────────────────────────────────
async function calcularTiempoPromedio(where: Record<string, unknown>): Promise<number | null> {
  const tickets = await prisma.ticket.findMany({
    where: { ...where, estado: "RESUELTO", fechaResolucion: { not: null }, activo: true },
    select: { createdAt: true, fechaResolucion: true },
  });
  if (tickets.length === 0) return null;
  const totalMs = tickets.reduce((acc, t) => acc + (t.fechaResolucion!.getTime() - t.createdAt.getTime()), 0);
  return Math.round((totalMs / tickets.length / 3_600_000) * 100) / 100;
}

// ── Helper: tiempo primera respuesta del técnico (D-04) ───────────────────────
// Proxy: primera transición a EN_PROGRESO en HistorialTicket del técnico
async function calcularTiemprimeraRespuesta(
  tecnicoId: number,
  where: Record<string, unknown>,
): Promise<number | null> {
  const tickets = await prisma.ticket.findMany({
    where: { ...where, tecnicoId, fechaAsignacion: { not: null }, activo: true },
    select: {
      id: true,
      fechaAsignacion: true,
      historial: {
        where: { estadoNuevo: "EN_PROGRESO", usuarioId: tecnicoId },
        orderBy: { createdAt: "asc" },
        take: 1,
        select: { createdAt: true },
      },
    },
  });
  const tiempos = tickets
    .filter((t) => t.historial.length > 0)
    .map((t) => t.historial[0].createdAt.getTime() - t.fechaAsignacion!.getTime());
  if (tiempos.length === 0) return null;
  const avgMs = tiempos.reduce((a, b) => a + b, 0) / tiempos.length;
  return Math.round((avgMs / 3_600_000) * 100) / 100;
}

// ── Función pública: Tab Global (ADMIN) ───────────────────────────────────────
export async function obtenerMetricasGlobal(
  fechaInicio?: Date,
  fechaFin?: Date,
): Promise<MetricasGlobalResponse> {
  const dateWhere = buildDateWhere(fechaInicio, fechaFin);
  const baseWhere = { activo: true, ...dateWhere };

  const fInicio = fechaInicio ?? new Date(Date.now() - 30 * 86_400_000);
  const fFin = fechaFin ?? new Date();

  const [totalTickets, ticketsActivos, ticketsResueltos, slaGlobal, tiempoPromedioHoras] =
    await Promise.all([
      prisma.ticket.count({ where: baseWhere }),
      prisma.ticket.count({ where: { activo: true, estado: { notIn: ["RESUELTO", "CANCELADO"] } } }),
      prisma.ticket.count({ where: { ...baseWhere, estado: "RESUELTO" } }),
      calcularSLA(baseWhere),
      calcularTiempoPromedio(baseWhere),
    ]);

  // Tendencia diaria
  const tendenciaDiaria = await calcularTendencia(fInicio, fFin);

  // Distribución por categoría (bigint → Number)
  const catRaw = await prisma.ticket.groupBy({
    by: ["categoria"],
    where: baseWhere,
    _count: { _all: true },
  });
  const distribucionCategoria: DistribucionCategoria[] = catRaw.map((r) => ({
    categoria: r.categoria as string,
    total: r._count._all,
  }));

  // Comparativo por área: join tickets → tecnico → areaSoporte
  const areas = await prisma.areaSoporte.findMany({
    where: { activo: true },
    select: { id: true, nombre: true },
  });
  const comparativoPorArea = await Promise.all(
    areas.map(async (a) => {
      type AreaRow = { total: bigint; resueltos: bigint };
      const [row] = await prisma.$queryRaw<AreaRow[]>`
        SELECT COUNT(*) AS total,
               SUM(CASE WHEN t.estado = 'RESUELTO' THEN 1 ELSE 0 END) AS resueltos
        FROM tickets t
        JOIN usuarios u ON t.tecnico_id = u.id
        WHERE t.activo = true
          AND u.area_soporte_id = ${a.id}
          ${dateWhere.createdAt?.gte ? Prisma.sql`AND t.created_at >= ${dateWhere.createdAt.gte}` : Prisma.empty}
          ${dateWhere.createdAt?.lte ? Prisma.sql`AND t.created_at <= ${dateWhere.createdAt.lte}` : Prisma.empty}
      `;
      return {
        areaNombre: a.nombre,
        areaSoporteId: a.id,
        total: Number(row?.total ?? 0),
        resueltos: Number(row?.resueltos ?? 0),
      };
    }),
  );

  // Tabla eficiencia responsables
  const responsables = await prisma.usuario.findMany({
    where: {
      activo: true,
      rol: { in: [Rol.RESPONSABLE_TI, Rol.RESPONSABLE_REDES, Rol.RESPONSABLE_MANTENIMIENTO, Rol.RESPONSABLE_RECURSOS_MATERIALES] },
      areaSoporteId: { not: null },
    },
    select: { id: true, nombre: true, apellidos: true, areaSoporteId: true, areaSoporte: { select: { nombre: true } } },
  });
  const eficienciaResponsables: EficienciaResponsable[] = await Promise.all(
    responsables.map(async (r) => {
      const areaWhere = { activo: true, ...dateWhere, tecnico: { areaSoporteId: r.areaSoporteId! } };
      const [resueltos, sla, tiempo] = await Promise.all([
        prisma.ticket.count({ where: { ...areaWhere, estado: "RESUELTO" } }),
        calcularSLA(areaWhere),
        calcularTiempoPromedio(areaWhere),
      ]);
      return {
        id: r.id,
        nombre: r.nombre,
        apellidos: r.apellidos,
        areaNombre: r.areaSoporte?.nombre ?? "",
        areaSoporteId: r.areaSoporteId!,
        ticketsResueltos: resueltos,
        tiempoPromedioHoras: tiempo,
        slaGlobal: sla,
      };
    }),
  );

  return {
    tipo: "area",
    totalTickets,
    ticketsActivos,
    ticketsResueltos,
    slaGlobal,
    tiempoPromedioHoras,
    tendenciaDiaria,
    distribucionCategoria,
    comparativoPorArea,
    eficienciaResponsables,
  };
}

// ── Función pública: Tab Por Responsable ──────────────────────────────────────
export async function obtenerMetricasPorArea(
  areaId: number,
  fechaInicio?: Date,
  fechaFin?: Date,
): Promise<MetricasPorAreaResponse> {
  const dateWhere = buildDateWhere(fechaInicio, fechaFin);
  const areaWhere = { activo: true, ...dateWhere, tecnico: { areaSoporteId: areaId } };

  const fInicio = fechaInicio ?? new Date(Date.now() - 30 * 86_400_000);
  const fFin = fechaFin ?? new Date();

  const area = await prisma.areaSoporte.findUnique({ where: { id: areaId }, select: { nombre: true } });

  const [ticketsActivos, slaGlobal, tiempoPromedioHoras] = await Promise.all([
    prisma.ticket.count({ where: { activo: true, estado: { notIn: ["RESUELTO", "CANCELADO"] }, tecnico: { areaSoporteId: areaId } } }),
    calcularSLA(areaWhere),
    calcularTiempoPromedio(areaWhere),
  ]);

  // Tickets reabiertos: transiciones a ABIERTO desde RESUELTO/CANCELADO
  type ReabiertosRow = { total: bigint };
  const [reabiertosRow] = await prisma.$queryRaw<ReabiertosRow[]>`
    SELECT COUNT(DISTINCT ht.ticket_id) AS total
    FROM historial_tickets ht
    JOIN tickets t ON ht.ticket_id = t.id
    JOIN usuarios u ON t.tecnico_id = u.id
    WHERE t.activo = true
      AND u.area_soporte_id = ${areaId}
      AND ht.estado_nuevo = 'ABIERTO'
      AND ht.estado_anterior IN ('RESUELTO', 'CANCELADO')
  `;
  const ticketsReabiertos = Number(reabiertosRow?.total ?? 0);

  // Tendencia, distribución subcategoría, carga técnicos
  const [tendenciaDiaria, subCatRaw] = await Promise.all([
    calcularTendencia(fInicio, fFin, areaId),
    prisma.ticket.groupBy({
      by: ["subcategoria"],
      where: areaWhere,
      _count: { _all: true },
    }),
  ]);

  const distribucionSubcategoria: DistribucionCategoria[] = subCatRaw.map((r) => ({
    categoria: r.subcategoria as string,
    total: r._count._all,
  }));

  // Técnicos del área
  const tecnicos = await prisma.usuario.findMany({
    where: { activo: true, areaSoporteId: areaId, rol: { notIn: [Rol.RESPONSABLE_TI, Rol.RESPONSABLE_REDES, Rol.RESPONSABLE_MANTENIMIENTO, Rol.RESPONSABLE_RECURSOS_MATERIALES, Rol.ADMIN, Rol.MESA_AYUDA, Rol.EMPLEADO] } },
    select: { id: true, nombre: true, apellidos: true, rol: true },
  });

  const [cargaTecnicos, rendimientoTecnicos] = await Promise.all([
    Promise.all(
      tecnicos.map(async (t) => ({
        tecnicoNombre: `${t.nombre} ${t.apellidos}`,
        tecnicoId: t.id,
        activos: await prisma.ticket.count({ where: { tecnicoId: t.id, activo: true, estado: { notIn: ["RESUELTO", "CANCELADO"] } } }),
        completados: await prisma.ticket.count({ where: { tecnicoId: t.id, activo: true, estado: "RESUELTO", ...dateWhere } }),
      })),
    ),
    Promise.all(
      tecnicos.map(async (t): Promise<RendimientoTecnico> => {
        const tecWhere = { tecnicoId: t.id, activo: true, ...dateWhere };
        const [activos, completados, cancelados, tiempo, primeraResp] = await Promise.all([
          prisma.ticket.count({ where: { tecnicoId: t.id, activo: true, estado: { notIn: ["RESUELTO", "CANCELADO"] } } }),
          prisma.ticket.count({ where: { ...tecWhere, estado: "RESUELTO" } }),
          prisma.ticket.count({ where: { ...tecWhere, estado: "CANCELADO" } }),
          calcularTiempoPromedio(tecWhere),
          calcularTiemprimeraRespuesta(t.id, tecWhere),
        ]);
        return {
          id: t.id,
          nombre: t.nombre,
          apellidos: t.apellidos,
          rol: t.rol as string,
          ticketsActivos: activos,
          ticketsCompletados: completados,
          tiempoPromedioHoras: tiempo,
          tiemprimeraRespuestaHoras: primeraResp,
          ratioResueltosCancelados:
            completados + cancelados > 0
              ? Math.round((completados / (completados + cancelados)) * 100) / 100
              : null,
        };
      }),
    ),
  ]);

  return {
    tipo: "tecnico",
    areaId,
    areaNombre: area?.nombre ?? "",
    ticketsActivos,
    tiempoPromedioHoras,
    slaGlobal,
    ticketsReabiertos,
    tendenciaDiaria,
    distribucionSubcategoria,
    cargaTecnicos,
    rendimientoTecnicos,
  };
}

// ── Función pública: Tab Por Técnico ─────────────────────────────────────────
export async function obtenerMetricasPorTecnico(
  tecnicoId: number,
  fechaInicio?: Date,
  fechaFin?: Date,
): Promise<MetricasPorTecnicoResponse> {
  const dateWhere = buildDateWhere(fechaInicio, fechaFin);
  const tecWhere = { tecnicoId, activo: true, ...dateWhere };

  const fInicio = fechaInicio ?? new Date(Date.now() - 30 * 86_400_000);
  const fFin = fechaFin ?? new Date();

  const tecnico = await prisma.usuario.findUnique({
    where: { id: tecnicoId },
    select: { nombre: true, apellidos: true, areaSoporteId: true },
  });

  const [ticketsCompletados, cancelados, tiempoPromedioHoras, tiemprimeraRespuestaHoras] =
    await Promise.all([
      prisma.ticket.count({ where: { ...tecWhere, estado: "RESUELTO" } }),
      prisma.ticket.count({ where: { ...tecWhere, estado: "CANCELADO" } }),
      calcularTiempoPromedio(tecWhere),
      calcularTiemprimeraRespuesta(tecnicoId, tecWhere),
    ]);

  const ratioResueltosCancelados =
    ticketsCompletados + cancelados > 0
      ? Math.round((ticketsCompletados / (ticketsCompletados + cancelados)) * 100) / 100
      : null;

  // Tendencia productividad personal
  type ProdRow = { dia: string; completados: bigint };
  const prodRows = await prisma.$queryRaw<ProdRow[]>`
    SELECT DATE(created_at) AS dia, COUNT(*) AS completados
    FROM tickets
    WHERE activo = true AND tecnico_id = ${tecnicoId} AND estado = 'RESUELTO'
      AND created_at >= ${fInicio} AND created_at <= ${fFin}
    GROUP BY DATE(created_at) ORDER BY dia ASC
  `;
  const tendenciaProductividad: TendenciaDia[] = prodRows.map((r) => ({
    dia: String(r.dia),
    creados: 0,
    resueltos: Number(r.completados),
  }));

  // Comparativa vs promedio del área
  const promedioAreaHoras = tecnico?.areaSoporteId
    ? await calcularTiempoPromedio({ activo: true, ...dateWhere, tecnico: { areaSoporteId: tecnico.areaSoporteId } })
    : null;
  const comparativaVsArea = [
    {
      label: "Tiempo promedio resolución (h)",
      tecnico: tiempoPromedioHoras ?? 0,
      promedioArea: promedioAreaHoras ?? 0,
    },
  ];

  // Distribución resueltos / cancelados para pastel
  const distribucionResultado = [
    { name: "Resueltos", value: ticketsCompletados, color: "#9e9e9e" },
    { name: "Cancelados", value: cancelados, color: "#bdbdbd" },
  ];

  return {
    tipo: "proceso",
    tecnicoId,
    tecnicoNombre: tecnico ? `${tecnico.nombre} ${tecnico.apellidos}` : String(tecnicoId),
    ticketsCompletados,
    tiempoPromedioHoras,
    tiemprimeraRespuestaHoras,
    ratioResueltosCancelados,
    tendenciaProductividad,
    comparativaVsArea,
    distribucionResultado,
  };
}
```
  </action>
  <verify>
    <automated>
      cd C:\Users\ilesm\Documents\SIAST && npx tsc --noEmit -p apps/api/tsconfig.json 2>&1 | head -30
    </automated>
  </verify>
  <acceptance_criteria>
    - El archivo apps/api/src/services/metricas.service.ts existe
    - grep -c "export async function obtenerMetricasGlobal" apps/api/src/services/metricas.service.ts devuelve 1
    - grep -c "export async function obtenerMetricasPorArea" apps/api/src/services/metricas.service.ts devuelve 1
    - grep -c "export async function obtenerMetricasPorTecnico" apps/api/src/services/metricas.service.ts devuelve 1
    - grep -c "SLA_HORAS" apps/api/src/services/metricas.service.ts devuelve al menos 1 (SLA hardcodeado D-02)
    - grep -c "Number(r\." apps/api/src/services/metricas.service.ts devuelve al menos 3 (conversiones bigint en raw queries)
    - grep -c "Prisma.sql" apps/api/src/services/metricas.service.ts devuelve al menos 1 (no concatenación de strings)
    - npx tsc --noEmit en apps/api sin errores relacionados a metricas.service.ts
  </acceptance_criteria>
  <done>metricas.service.ts creado con 3 funciones exportadas, SLA hardcodeado, bigint convertido, Prisma.sql usado para parámetros dinámicos.</done>
</task>

<task type="auto">
  <name>Tarea 2: Refactorizar metricas.controller.ts y metricas.routes.ts para endpoint único con Zod validation</name>
  <files>
    apps/api/src/controllers/metricas.controller.ts
    apps/api/src/routes/metricas.routes.ts
  </files>
  <read_first>
    - apps/api/src/controllers/metricas.controller.ts — leer completo para entender estructura a reemplazar (los 3 exports legacy: metricasSolicitudes, metricasTecnicos, metricasProcesos)
    - apps/api/src/routes/metricas.routes.ts — leer completo para ver la ruta actual a reemplazar
    - apps/api/src/middleware/roles.middleware.ts — leer para confirmar nombres exactos de ROLES_RESPONSABLE y el import de requireRol
    - apps/api/src/types/index.ts — leer para confirmar el tipo AuthRequest y si areaSoporteId está disponible en req.user
  </read_first>
  <action>
**1. Reemplazar apps/api/src/controllers/metricas.controller.ts completo:**

```typescript
import { z } from "zod";
import type { Response, NextFunction } from "express";
import type { AuthRequest } from "../types/index.js";
import { ROLES_RESPONSABLE } from "../middleware/roles.middleware.js";
import * as metricasService from "../services/metricas.service.js";

// ── Validación de query params (Zod — seguridad D-threat Fechas malformadas) ──
const QuerySchema = z.object({
  tipo: z.enum(["area", "tecnico", "proceso"]).default("area"),
  fechaInicio: z.string().optional().transform((s) => {
    if (!s) return undefined;
    const d = new Date(s);
    if (isNaN(d.getTime())) throw new Error("fechaInicio inválida");
    return d;
  }),
  fechaFin: z.string().optional().transform((s) => {
    if (!s) return undefined;
    const d = new Date(s);
    if (isNaN(d.getTime())) throw new Error("fechaFin inválida");
    return d;
  }),
  areaId: z.string().optional().transform((s) => (s ? Number(s) : undefined)),
  tecnicoId: z.string().optional().transform((s) => (s ? Number(s) : undefined)),
});

// ============================================================
// GET /api/metricas?tipo=area|tecnico|proceso&fechaInicio=&fechaFin=&areaId=&tecnicoId=
// ============================================================
export const obtener = async (req: AuthRequest, res: Response, next: NextFunction): Promise<void> => {
  try {
    // 1. Validar y parsear query params — 400 en fechas inválidas
    const parsed = QuerySchema.safeParse(req.query);
    if (!parsed.success) {
      res.status(400).json({
        error: "Parámetros inválidos",
        details: parsed.error.flatten().fieldErrors,
      });
      return;
    }

    const { tipo, fechaInicio, fechaFin, tecnicoId } = parsed.data;
    let { areaId } = parsed.data;
    const user = req.user!;

    // 2. Role scoping — SEGURIDAD CRÍTICA (D-12, amenaza T-04-02-01)
    // RESPONSABLE_*: ignorar areaId del query param, forzar desde JWT
    if ((ROLES_RESPONSABLE as readonly string[]).includes(user.rol)) {
      if (!user.areaSoporteId) {
        res.status(403).json({ error: "Responsable sin área asignada en token" });
        return;
      }
      areaId = user.areaSoporteId;
    }

    // 3. TECNICO_*: para tipo=proceso, forzar su propio tecnicoId (amenaza T-04-02-04)
    const ROLES_TECNICO = [
      "TECNICO_TI", "TECNICO_REDES", "TECNICO_SERVICIOS",
      "TECNICO_ELECTRICISTA", "TECNICO_PLOMERO", "TECNICO_MOVILIDAD",
    ];
    const tecnicoIdEfectivo =
      ROLES_TECNICO.includes(user.rol) ? user.id : (tecnicoId ?? undefined);

    // 4. Delegar al servicio según tipo
    let data;
    if (tipo === "area") {
      data = await metricasService.obtenerMetricasGlobal(fechaInicio, fechaFin);
    } else if (tipo === "tecnico") {
      if (!areaId) {
        res.status(400).json({ error: "areaId requerido para tipo=tecnico" });
        return;
      }
      data = await metricasService.obtenerMetricasPorArea(areaId, fechaInicio, fechaFin);
    } else {
      // tipo === "proceso"
      if (!tecnicoIdEfectivo) {
        res.status(400).json({ error: "tecnicoId requerido para tipo=proceso" });
        return;
      }
      data = await metricasService.obtenerMetricasPorTecnico(tecnicoIdEfectivo, fechaInicio, fechaFin);
    }

    res.json(data);
  } catch (err) {
    next(err);
  }
};
```

NOTA: Los exports legacy `metricasSolicitudes`, `metricasTecnicos`, `metricasProcesos` se ELIMINAN en
este reemplazo. El Plan 02 asume que Plan 03 y 04 (frontend) no los consumen — usarán el nuevo endpoint.

**2. Reemplazar apps/api/src/routes/metricas.routes.ts completo:**

```typescript
import { Router } from "express";
import { authMiddleware } from "../middleware/auth.middleware.js";
import { requireRol } from "../middleware/roles.middleware.js";
import * as ctrl from "../controllers/metricas.controller.js";

const router = Router();

router.use(authMiddleware);

// Todos los roles con acceso a métricas (Phase 3+ incluidos — pitfall 6 del RESEARCH)
const rolesMetricas = requireRol(
  "ADMIN",
  "MESA_AYUDA",
  "RESPONSABLE_TI",
  "RESPONSABLE_REDES",
  "RESPONSABLE_MANTENIMIENTO",
  "RESPONSABLE_RECURSOS_MATERIALES",
  "TECNICO_TI",
  "TECNICO_REDES",
  "TECNICO_SERVICIOS",
  "TECNICO_ELECTRICISTA",
  "TECNICO_PLOMERO",
  "TECNICO_MOVILIDAD",
);

// Endpoint único paramétrico (D-10)
router.get("/", rolesMetricas, ctrl.obtener);

export default router;
```

IMPORTANTE: Verificar en `apps/api/src/index.ts` o en el archivo de rutas principal que `/api/metricas`
está montado como `router.use("/metricas", metricasRouter)`. Si las rutas legacy usaban `/metricas/solicitudes`,
`/metricas/tecnicos`, `/metricas/procesos` y el frontend aún las llama — esas llamadas se romperán al eliminarlas.
Eso es intencional: el nuevo frontend (Plan 03/04) usará `/api/metricas?tipo=...`.
  </action>
  <verify>
    <automated>
      cd C:\Users\ilesm\Documents\SIAST && npx tsc --noEmit -p apps/api/tsconfig.json 2>&1 | head -30
    </automated>
  </verify>
  <acceptance_criteria>
    - grep -c "export const obtener" apps/api/src/controllers/metricas.controller.ts devuelve 1
    - grep -v "^//" apps/api/src/controllers/metricas.controller.ts | grep -c "metricasSolicitudes\|metricasTecnicos\|metricasProcesos" devuelve 0 (funciones legacy eliminadas)
    - grep -c "z.enum\(\[.area., .tecnico., .proceso.\]\)" apps/api/src/controllers/metricas.controller.ts devuelve 1 (Zod validation presente)
    - grep -c "ROLES_RESPONSABLE" apps/api/src/controllers/metricas.controller.ts devuelve al menos 1 (role scoping D-12)
    - grep -c "RESPONSABLE_TI" apps/api/src/routes/metricas.routes.ts devuelve 1 (roles Phase 3 incluidos)
    - grep -c "TECNICO_ELECTRICISTA" apps/api/src/routes/metricas.routes.ts devuelve 1
    - grep -c "router.get\(., rolesMetricas, ctrl.obtener\)" apps/api/src/routes/metricas.routes.ts devuelve 1
    - npx tsc --noEmit en apps/api sin errores
  </acceptance_criteria>
  <done>Endpoint único GET /api/metricas con Zod validation activo, RESPONSABLE_* forzado por JWT, TECNICO_* forzado por id propio, roles Phase 3 en la ruta.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP query params → controlador | Parámetros tipo, fechaInicio, fechaFin, areaId, tecnicoId llegan sin confianza |
| JWT payload → role scoping | areaSoporteId en token es fuente de verdad; query param areaId se ignora para RESPONSABLE_* |
| Controlador → servicio | Solo parámetros validados y ya convertidos (Date, number) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Elevation of Privilege | obtener controller — areaId param | mitigate | Si user.rol in ROLES_RESPONSABLE → forzar areaId = user.areaSoporteId del JWT, ignorar query param. Implementado explícitamente en controlador. |
| T-04-02-02 | Information Disclosure | metricas.routes.ts — EMPLEADO access | mitigate | requireRol excluye EMPLEADO del requireRol list. EMPLEADO recibe 403 al intentar acceder. |
| T-04-02-03 | Tampering | $queryRaw con Prisma.sql | mitigate | Todos los valores dinámicos en raw SQL usan Prisma.sql tagged templates (${value}). Nunca string concatenation. Verificado en metricas.service.ts. |
| T-04-02-04 | Information Disclosure | tipo=proceso — TECNICO_* ve datos de otro técnico | mitigate | Si user.rol in ROLES_TECNICO → tecnicoIdEfectivo = user.id, ignora query param tecnicoId. |
| T-04-02-05 | Tampering | Fechas malformadas en fechaInicio/fechaFin | mitigate | Zod QuerySchema valida y transforma con `new Date(s)` + `isNaN()` check; retorna 400 con fieldErrors antes de llegar a Prisma. |
</threat_model>

<verification>
```bash
# 1. TypeScript compile sin errores
cd C:\Users\ilesm\Documents\SIAST && npx tsc --noEmit -p apps/api/tsconfig.json

# 2. Endpoint accesible (MySQL debe estar corriendo)
curl -s -H "Authorization: Bearer <TOKEN_ADMIN>" \
  "http://localhost:5101/api/metricas?tipo=area&fechaInicio=2026-01-01&fechaFin=2026-05-26" | jq .tipo

# 3. Fechas inválidas retornan 400
curl -s -H "Authorization: Bearer <TOKEN>" \
  "http://localhost:5101/api/metricas?tipo=area&fechaInicio=not-a-date" | jq .error

# 4. RESPONSABLE sin areaSoporteId → 403
# (usar token de RESPONSABLE_TI sin areaSoporteId en payload)
```
</verification>

<success_criteria>
- `GET /api/metricas?tipo=area` retorna JSON con campo `tipo: "area"` y datos reales de DB
- `GET /api/metricas?tipo=area&fechaInicio=invalid` retorna HTTP 400
- RESPONSABLE_* no puede enviar areaId distinto al suyo — backend lo ignora y usa el del JWT
- TECNICO_* no puede enviar tecnicoId distinto al suyo — backend lo reemplaza con user.id
- TypeScript build de apps/api sin errores de compilación
</success_criteria>

<output>
Al completar, crear `.planning/phases/04-metricas-operacionales/04-02-SUMMARY.md`
</output>
