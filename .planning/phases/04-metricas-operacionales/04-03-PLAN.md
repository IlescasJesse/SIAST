---
phase: 04-metricas-operacionales
plan: "03"
type: execute
wave: 3
depends_on:
  - "04-02"
files_modified:
  - apps/web/src/api/metricas.js
  - apps/web/src/components/metricas/SlaIndicator.jsx
  - apps/web/src/components/metricas/RechartsBarChart.jsx
  - apps/web/src/components/metricas/RechartsLineChart.jsx
  - apps/web/src/components/metricas/RechartsPieChart.jsx
  - apps/web/src/components/metricas/DateRangeFilter.jsx
  - apps/web/src/components/metricas/EficienciaTable.jsx
  - apps/web/src/components/metricas/RendimientoTecnicoTable.jsx
autonomous: true
requirements:
  - MET-01
  - MET-02
  - MET-03
  - MET-04

must_haves:
  truths:
    - "recharts está instalado en apps/web y los 3 chart wrappers se importan sin errores"
    - "SlaIndicator muestra Chip success/warning/error según pct >= 90 / 70-89 / <70"
    - "RechartsBarChart envuelve ResponsiveContainer en Box con height={260} explícito"
    - "DateRangeFilter abre Popover con dos DatePicker (Desde/Hasta) y botón Aplicar"
    - "getMetricas(params) llama GET /api/metricas y retorna r.data"
  artifacts:
    - path: "apps/web/src/api/metricas.js"
      provides: "Función getMetricas para consumir el endpoint unificado"
      contains: "getMetricas"
    - path: "apps/web/src/components/metricas/RechartsBarChart.jsx"
      provides: "Wrapper reutilizable BarChart con Box height explícito"
      contains: "ResponsiveContainer"
    - path: "apps/web/src/components/metricas/SlaIndicator.jsx"
      provides: "Chip con colores SLA OK/En riesgo/Incumplido"
      contains: "SlaIndicator"
    - path: "apps/web/src/components/metricas/DateRangeFilter.jsx"
      provides: "Popover con DatePicker Desde/Hasta y botón Aplicar"
      contains: "DateRangeFilter"
  key_links:
    - from: "apps/web/src/components/metricas/MetricasOperacionalesSection.jsx"
      to: "apps/web/src/api/metricas.js"
      via: "import { getMetricas } from '../../api/metricas.js'"
      pattern: "getMetricas"
    - from: "apps/web/src/components/metricas/RechartsBarChart.jsx"
      to: "recharts"
      via: "import { BarChart, Bar, ... } from 'recharts'"
      pattern: "from .recharts."
---

<objective>
Instalar recharts, crear el cliente API de métricas y todos los componentes base reutilizables:
wrappers de Recharts (BarChart, LineChart, PieChart), SlaIndicator, DateRangeFilter,
EficienciaTable y RendimientoTecnicoTable.

Estos componentes son los "átomos" que consumen los tres tabs del dashboard. El plan siguiente (04-04)
los ensambla en MetricasOperacionalesSection y los integra en DashboardPage.

Propósito: Separar la creación de componentes atómicos de la integración en la página; permite
desarrollo limpio sin tocar DashboardPage hasta que todos los átomos estén listos.

Salida: 8 archivos nuevos en apps/web/src/components/metricas/ + 1 en apps/web/src/api/.
</objective>

<execution_context>
@C:\Users\ilesm\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\ilesm\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\ilesm\Documents\SIAST\.planning\PROJECT.md
@C:\Users\ilesm\Documents\SIAST\.planning\ROADMAP.md
@C:\Users\ilesm\Documents\SIAST\.planning\phases\04-metricas-operacionales\04-CONTEXT.md
@C:\Users\ilesm\Documents\SIAST\.planning\phases\04-metricas-operacionales\04-RESEARCH.md
@C:\Users\ilesm\Documents\SIAST\.planning\phases\04-metricas-operacionales\04-UI-SPEC.md
@C:\Users\ilesm\Documents\SIAST\.planning\phases\04-metricas-operacionales\04-PATTERNS.md

<interfaces>
<!-- API client pattern (de apps/web/src/api/tickets.js) -->
```javascript
import { api } from "./client.js";
export const getTickets = (params) => api.get("/api/tickets", { params }).then((r) => r.data);
```

<!-- StatusChip analog para SlaIndicator -->
```jsx
// apps/web/src/components/common/StatusChip.jsx
import { Chip } from "@mui/material";
export const StatusChip = ({ estado, size = "small" }) => (
  <Chip label={LABELS[estado] ?? estado} size={size} sx={{ ... }} />
);
```

<!-- Patrón MUI x DatePicker disponible en proyecto -->
```jsx
import { LocalizationProvider } from "@mui/x-date-pickers/LocalizationProvider";
import { DatePicker } from "@mui/x-date-pickers/DatePicker";
import { AdapterDateFns } from "@mui/x-date-pickers/AdapterDateFns";
import { es } from "date-fns/locale";
```

<!-- Pitfall crítico Recharts (de RESEARCH.md Pattern 6) -->
// CORRECTO: Box padre con height explícito
// <Box sx={{ width: "100%", height: 260 }}>
//   <ResponsiveContainer width="100%" height="100%">
//     <BarChart>...</BarChart>
//   </ResponsiveContainer>
// </Box>
// INCORRECTO: sin Box padre con altura → chart colapsa a 0

<!-- Pitfall z-order Recharts 3.x (RESEARCH.md Pitfall 7) -->
// Tooltip DEBE ir ANTES de Legend en JSX — z-order SVG
// <Tooltip ... />
// <Legend ... />

<!-- Colores semánticos de la UI-SPEC -->
// Recharts palette: #9d2449 (serie 1), #b56e85 (serie 2), #742035 (serie 3), #c8a0b0 (serie 4), #a83e6a (serie 5)
// TECNOLOGIAS: #1565c0, SERVICIOS: #2e7d32, RECURSOS_MATERIALES: #e65100
// SLA: success.main #2e7d32, warning.main #ed6c02, error.main #d32f2f
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: Instalar recharts y crear cliente API + componentes visuales base</name>
  <files>
    apps/web/src/api/metricas.js
    apps/web/src/components/metricas/SlaIndicator.jsx
    apps/web/src/components/metricas/RechartsBarChart.jsx
    apps/web/src/components/metricas/RechartsLineChart.jsx
    apps/web/src/components/metricas/RechartsPieChart.jsx
  </files>
  <read_first>
    - apps/web/src/api/tickets.js — leer las primeras 15 líneas para el import pattern de `api` y el estilo de funciones exportadas
    - apps/web/src/api/client.js — leer para confirmar el export `api` (o `default`) y `API_BASE`
    - apps/web/package.json — verificar que recharts NO está instalado (confirmar antes de instalar)
  </read_first>
  <action>
**Paso 0: Instalar recharts**
Desde el directorio apps/web:
```bash
cd apps/web && npm install recharts
```
Verificar que `recharts` aparece en apps/web/package.json después de instalar.

**Paso 1: Crear apps/web/src/api/metricas.js**
```javascript
import { api } from "./client.js";

/**
 * Llama GET /api/metricas con los parámetros dados.
 * @param {Object} params - { tipo, fechaInicio, fechaFin, areaId?, tecnicoId? }
 * @returns {Promise<import('@stf/shared').MetricasResponse>}
 */
export const getMetricas = (params) =>
  api.get("/api/metricas", { params }).then((r) => r.data);
```

**Paso 2: Crear apps/web/src/components/metricas/SlaIndicator.jsx**
```jsx
import { Chip } from "@mui/material";
import PropTypes from "prop-types";

/**
 * Chip indicador de SLA con colores semánticos.
 * pct >= 90 → success (SLA OK)
 * pct 70-89 → warning (En riesgo)
 * pct < 70  → error (Incumplido)
 */
export function SlaIndicator({ pct }) {
  const config =
    pct >= 90
      ? { label: `SLA OK ${pct}%`, color: "success" }
      : pct >= 70
      ? { label: `En riesgo ${pct}%`, color: "warning" }
      : { label: `Incumplido ${pct}%`, color: "error" };

  return <Chip label={config.label} color={config.color} size="small" variant="filled" />;
}

SlaIndicator.propTypes = { pct: PropTypes.number.isRequired };
```

**Paso 3: Crear apps/web/src/components/metricas/RechartsBarChart.jsx**

CRÍTICO — dos reglas obligatorias de RESEARCH.md:
1. Box padre con `height={260}` ANTES de ResponsiveContainer (Pitfall 2)
2. `<Tooltip>` ANTES de `<Legend>` en JSX (Pitfall 7 z-order Recharts 3.x)

```jsx
import {
  BarChart, Bar, XAxis, YAxis, CartesianGrid,
  Tooltip, Legend, ResponsiveContainer,
} from "recharts";
import { Box } from "@mui/material";
import PropTypes from "prop-types";

const TICK_STYLE = { fontFamily: "'Inter', 'Roboto', sans-serif", fontSize: 12 };

/**
 * Wrapper reutilizable para gráficas de barras Recharts.
 * @param {{ data: object[], xKey: string, bars: Array<{key:string, label:string, color:string}> }} props
 */
export function RechartsBarChart({ data, xKey, bars }) {
  return (
    <Box sx={{ width: "100%", height: 260 }}>
      <ResponsiveContainer width="100%" height="100%">
        <BarChart data={data} barSize={28} margin={{ top: 4, right: 8, left: 0, bottom: 4 }}>
          <CartesianGrid strokeDasharray="3 3" stroke="rgba(0,0,0,0.08)" />
          <XAxis dataKey={xKey} tick={TICK_STYLE} />
          <YAxis tick={TICK_STYLE} />
          {/* Tooltip ANTES de Legend — z-order Recharts 3.x */}
          <Tooltip
            contentStyle={{
              background: "#fff",
              border: "1px solid rgba(0,0,0,0.08)",
              borderRadius: 4,
              fontFamily: "'Inter', 'Roboto', sans-serif",
              fontSize: 12,
            }}
          />
          <Legend wrapperStyle={TICK_STYLE} />
          {bars.map((b) => (
            <Bar key={b.key} dataKey={b.key} name={b.label} fill={b.color} />
          ))}
        </BarChart>
      </ResponsiveContainer>
    </Box>
  );
}

RechartsBarChart.propTypes = {
  data: PropTypes.arrayOf(PropTypes.object).isRequired,
  xKey: PropTypes.string.isRequired,
  bars: PropTypes.arrayOf(
    PropTypes.shape({ key: PropTypes.string, label: PropTypes.string, color: PropTypes.string }),
  ).isRequired,
};
```

**Paso 4: Crear apps/web/src/components/metricas/RechartsLineChart.jsx**

```jsx
import {
  LineChart, Line, XAxis, YAxis, CartesianGrid,
  Tooltip, Legend, ResponsiveContainer,
} from "recharts";
import { Box } from "@mui/material";
import PropTypes from "prop-types";

const TICK_STYLE = { fontFamily: "'Inter', 'Roboto', sans-serif", fontSize: 12 };

/**
 * Wrapper reutilizable para gráficas de líneas Recharts.
 * @param {{ data: object[], xKey: string, lines: Array<{key:string, label:string, color:string}> }} props
 */
export function RechartsLineChart({ data, xKey, lines }) {
  return (
    <Box sx={{ width: "100%", height: 260 }}>
      <ResponsiveContainer width="100%" height="100%">
        <LineChart data={data} margin={{ top: 4, right: 8, left: 0, bottom: 4 }}>
          <CartesianGrid strokeDasharray="3 3" stroke="rgba(0,0,0,0.08)" />
          <XAxis dataKey={xKey} tick={TICK_STYLE} />
          <YAxis tick={TICK_STYLE} />
          {/* Tooltip ANTES de Legend — z-order Recharts 3.x */}
          <Tooltip
            contentStyle={{
              background: "#fff",
              border: "1px solid rgba(0,0,0,0.08)",
              borderRadius: 4,
              fontFamily: "'Inter', 'Roboto', sans-serif",
              fontSize: 12,
            }}
          />
          <Legend wrapperStyle={TICK_STYLE} />
          {lines.map((l) => (
            <Line
              key={l.key}
              type="monotone"
              dataKey={l.key}
              name={l.label}
              stroke={l.color}
              strokeWidth={2}
              dot={{ r: 3 }}
              activeDot={{ r: 5 }}
            />
          ))}
        </LineChart>
      </ResponsiveContainer>
    </Box>
  );
}

RechartsLineChart.propTypes = {
  data: PropTypes.arrayOf(PropTypes.object).isRequired,
  xKey: PropTypes.string.isRequired,
  lines: PropTypes.arrayOf(
    PropTypes.shape({ key: PropTypes.string, label: PropTypes.string, color: PropTypes.string }),
  ).isRequired,
};
```

**Paso 5: Crear apps/web/src/components/metricas/RechartsPieChart.jsx**

```jsx
import {
  PieChart, Pie, Cell, Tooltip, Legend, ResponsiveContainer,
} from "recharts";
import { Box } from "@mui/material";
import PropTypes from "prop-types";

const TICK_STYLE = { fontFamily: "'Inter', 'Roboto', sans-serif", fontSize: 12 };

/**
 * Wrapper reutilizable para gráficas de pastel (donut) Recharts.
 * @param {{ data: Array<{name:string, value:number, color:string}> }} props
 */
export function RechartsPieChart({ data }) {
  return (
    <Box sx={{ width: "100%", height: 220 }}>
      <ResponsiveContainer width="100%" height="100%">
        <PieChart>
          {/* Tooltip ANTES de Legend — z-order Recharts 3.x */}
          <Tooltip
            contentStyle={{
              fontFamily: "'Inter', 'Roboto', sans-serif",
              fontSize: 12,
              border: "1px solid rgba(0,0,0,0.08)",
              borderRadius: 4,
            }}
          />
          <Legend wrapperStyle={TICK_STYLE} />
          <Pie
            data={data}
            dataKey="value"
            nameKey="name"
            innerRadius={50}
            outerRadius={90}
            paddingAngle={2}
          >
            {data.map((entry, i) => (
              <Cell key={i} fill={entry.color} />
            ))}
          </Pie>
        </PieChart>
      </ResponsiveContainer>
    </Box>
  );
}

RechartsPieChart.propTypes = {
  data: PropTypes.arrayOf(
    PropTypes.shape({
      name: PropTypes.string.isRequired,
      value: PropTypes.number.isRequired,
      color: PropTypes.string.isRequired,
    }),
  ).isRequired,
};
```
  </action>
  <verify>
    <automated>
      cd C:\Users\ilesm\Documents\SIAST\apps\web && node -e "require('./node_modules/recharts')" 2>&1 | head -5
    </automated>
  </verify>
  <acceptance_criteria>
    - "recharts" aparece en apps/web/package.json dependencies (grep -c '"recharts"' apps/web/package.json devuelve 1)
    - El directorio apps/web/src/components/metricas/ existe con 4 archivos: SlaIndicator.jsx, RechartsBarChart.jsx, RechartsLineChart.jsx, RechartsPieChart.jsx
    - grep -c "Box sx={{ width: .100%., height: 260" apps/web/src/components/metricas/RechartsBarChart.jsx devuelve 1 (pitfall 2 aplicado)
    - grep -c "Box sx={{ width: .100%., height: 260" apps/web/src/components/metricas/RechartsLineChart.jsx devuelve 1
    - grep -c "Box sx={{ width: .100%., height: 220" apps/web/src/components/metricas/RechartsPieChart.jsx devuelve 1
    - En cada chart, Tooltip aparece ANTES que Legend en el archivo (grep -n "Tooltip\|Legend" en cada archivo muestra Tooltip con línea menor)
    - grep -c "pct >= 90" apps/web/src/components/metricas/SlaIndicator.jsx devuelve 1
    - grep -c "getMetricas" apps/web/src/api/metricas.js devuelve 1
    - apps/web/src/api/metricas.js importa desde "./client.js" (grep -c "from .\"./client.js\"." apps/web/src/api/metricas.js devuelve 1)
  </acceptance_criteria>
  <done>recharts instalado. 4 componentes visuales creados con pitfalls de altura y z-order aplicados. Cliente API getMetricas creado.</done>
</task>

<task type="auto">
  <name>Tarea 2: Crear DateRangeFilter, EficienciaTable y RendimientoTecnicoTable</name>
  <files>
    apps/web/src/components/metricas/DateRangeFilter.jsx
    apps/web/src/components/metricas/EficienciaTable.jsx
    apps/web/src/components/metricas/RendimientoTecnicoTable.jsx
  </files>
  <read_first>
    - apps/web/src/pages/DashboardPage.jsx — leer líneas 1-110 para ver los imports MUI disponibles, patrón StatCard, patrón TableContainer/TableHead/TableRow/TableCell ya en uso
    - apps/web/src/components/metricas/SlaIndicator.jsx — leer para usar SlaIndicator en las tablas (ya creado en Tarea 1)
  </read_first>
  <action>
**Paso 1: Crear apps/web/src/components/metricas/DateRangeFilter.jsx**

Trigger: IconButton con TuneIcon (D-16). Popover con dos DatePicker. Botón "Aplicar".
DatePicker usa AdapterDateFns (no AdapterDayjs — UI-SPEC nota 4).

```jsx
import { useState } from "react";
import {
  IconButton, Popover, Box, Button, Typography, Badge, Tooltip,
} from "@mui/material";
import TuneIcon from "@mui/icons-material/Tune";
import { LocalizationProvider } from "@mui/x-date-pickers/LocalizationProvider";
import { DatePicker } from "@mui/x-date-pickers/DatePicker";
import { AdapterDateFns } from "@mui/x-date-pickers/AdapterDateFns";
import { es } from "date-fns/locale";
import { subDays, isEqual, startOfDay } from "date-fns";
import PropTypes from "prop-types";

const DEFAULT_START = () => subDays(new Date(), 30);
const DEFAULT_END = () => new Date();

/**
 * Filtro de rango de fechas como settings dropdown.
 * Muestra badge cuando el rango no es el default (últimos 30 días).
 * @param {{ value: {start: Date, end: Date}, onChange: Function }} props
 */
export function DateRangeFilter({ value, onChange }) {
  const [anchorEl, setAnchorEl] = useState(null);
  const [draft, setDraft] = useState(value);

  const isDefault =
    isEqual(startOfDay(value.start), startOfDay(DEFAULT_START())) &&
    isEqual(startOfDay(value.end), startOfDay(DEFAULT_END()));

  const handleApply = () => {
    onChange(draft);
    setAnchorEl(null);
  };

  const handleCancel = () => {
    setDraft(value);
    setAnchorEl(null);
  };

  return (
    <LocalizationProvider dateAdapter={AdapterDateFns} adapterLocale={es}>
      <Tooltip title="Filtrar por periodo">
        <Badge color="primary" variant="dot" invisible={isDefault}>
          <IconButton
            size="small"
            onClick={(e) => { setDraft(value); setAnchorEl(e.currentTarget); }}
            aria-label="Filtrar por periodo"
          >
            <TuneIcon fontSize="small" />
          </IconButton>
        </Badge>
      </Tooltip>

      <Popover
        open={Boolean(anchorEl)}
        anchorEl={anchorEl}
        onClose={handleCancel}
        anchorOrigin={{ vertical: "bottom", horizontal: "right" }}
        transformOrigin={{ vertical: "top", horizontal: "right" }}
      >
        <Box sx={{ p: 2, minWidth: 280 }}>
          <Typography variant="subtitle2" fontWeight={700} sx={{ mb: 2 }}>
            Filtrar por periodo
          </Typography>
          <Box sx={{ display: "flex", flexDirection: "column", gap: 2 }}>
            <DatePicker
              label="Desde"
              value={draft.start}
              onChange={(v) => v && setDraft((d) => ({ ...d, start: v }))}
              maxDate={draft.end}
              slotProps={{ textField: { size: "small", fullWidth: true } }}
            />
            <DatePicker
              label="Hasta"
              value={draft.end}
              onChange={(v) => v && setDraft((d) => ({ ...d, end: v }))}
              minDate={draft.start}
              maxDate={new Date()}
              slotProps={{ textField: { size: "small", fullWidth: true } }}
            />
          </Box>
          <Box sx={{ display: "flex", gap: 1, mt: 2, justifyContent: "flex-end" }}>
            <Button size="small" onClick={handleCancel}>Cancelar</Button>
            <Button size="small" variant="contained" onClick={handleApply}>Aplicar</Button>
          </Box>
        </Box>
      </Popover>
    </LocalizationProvider>
  );
}

DateRangeFilter.propTypes = {
  value: PropTypes.shape({ start: PropTypes.instanceOf(Date), end: PropTypes.instanceOf(Date) }).isRequired,
  onChange: PropTypes.func.isRequired,
};
```

**Paso 2: Crear apps/web/src/components/metricas/EficienciaTable.jsx**

Tabla de eficiencia de responsables (Tab Global). Filas clickeables con drill-down a tab Por Responsable.
Copywriting del UI-SPEC: "Responsable", "Resueltos", "Tiempo promedio", "SLA %".

```jsx
import {
  Table, TableBody, TableCell, TableContainer, TableHead, TableRow,
  Paper, Typography, Box, Tooltip,
} from "@mui/material";
import { SlaIndicator } from "./SlaIndicator.jsx";
import PropTypes from "prop-types";

/**
 * Tabla de eficiencia de responsables para Tab Global (ADMIN).
 * Filas clickeables → drill-down al tab Por Responsable del área.
 * @param {{ rows: import('@stf/shared').EficienciaResponsable[], onRowClick: Function }} props
 */
export function EficienciaTable({ rows, onRowClick }) {
  if (!rows || rows.length === 0) {
    return (
      <Typography variant="body2" color="text.secondary" sx={{ py: 2, textAlign: "center" }}>
        Sin datos para el período seleccionado
      </Typography>
    );
  }

  return (
    <TableContainer component={Paper} variant="outlined" sx={{ borderRadius: 1 }}>
      <Table size="small">
        <TableHead>
          <TableRow sx={{ bgcolor: "primary.main" }}>
            {["Responsable", "Resueltos", "Tiempo promedio", "SLA %"].map((h) => (
              <TableCell key={h} sx={{ color: "common.white", fontWeight: 700, fontSize: 12 }}>
                {h}
              </TableCell>
            ))}
          </TableRow>
        </TableHead>
        <TableBody>
          {rows.map((row) => (
            <Tooltip key={row.id} title={`Ver detalle de ${row.nombre} ${row.apellidos}`} placement="left">
              <TableRow
                onClick={() => onRowClick(row)}
                sx={{
                  cursor: "pointer",
                  "&:hover": { bgcolor: "primary.main", opacity: 0.04 },
                  "& td": { fontSize: 13 },
                }}
              >
                <TableCell>
                  <Box>
                    <Typography variant="body2" fontWeight={600}>
                      {row.nombre} {row.apellidos}
                    </Typography>
                    <Typography variant="caption" color="text.secondary">
                      {row.areaNombre}
                    </Typography>
                  </Box>
                </TableCell>
                <TableCell>{row.ticketsResueltos}</TableCell>
                <TableCell>
                  {row.tiempoPromedioHoras != null
                    ? `${row.tiempoPromedioHoras}h`
                    : "—"}
                </TableCell>
                <TableCell>
                  <SlaIndicator pct={row.slaGlobal} />
                </TableCell>
              </TableRow>
            </Tooltip>
          ))}
        </TableBody>
      </Table>
    </TableContainer>
  );
}

EficienciaTable.propTypes = {
  rows: PropTypes.array.isRequired,
  onRowClick: PropTypes.func.isRequired,
};
```

**Paso 3: Crear apps/web/src/components/metricas/RendimientoTecnicoTable.jsx**

Tabla de rendimiento de técnicos (Tab Por Responsable). Filas clickeables → drill-down a Tab Por Técnico.
Copywriting: "Técnico", "Completados", "Tiempo promedio", "Resuelto / Cancelado".

```jsx
import {
  Table, TableBody, TableCell, TableContainer, TableHead, TableRow,
  Paper, Typography, Tooltip,
} from "@mui/material";
import PropTypes from "prop-types";

/**
 * Tabla de rendimiento de técnicos para Tab Por Responsable.
 * Filas clickeables → drill-down al tab Por Técnico.
 * @param {{ rows: import('@stf/shared').RendimientoTecnico[], onRowClick: Function }} props
 */
export function RendimientoTecnicoTable({ rows, onRowClick }) {
  if (!rows || rows.length === 0) {
    return (
      <Typography variant="body2" color="text.secondary" sx={{ py: 2, textAlign: "center" }}>
        No hay técnicos registrados en esta área
      </Typography>
    );
  }

  return (
    <TableContainer component={Paper} variant="outlined" sx={{ borderRadius: 1 }}>
      <Table size="small">
        <TableHead>
          <TableRow sx={{ bgcolor: "primary.main" }}>
            {["Técnico", "Completados", "Tiempo promedio", "Resuelto / Cancelado"].map((h) => (
              <TableCell key={h} sx={{ color: "common.white", fontWeight: 700, fontSize: 12 }}>
                {h}
              </TableCell>
            ))}
          </TableRow>
        </TableHead>
        <TableBody>
          {rows.map((row) => (
            <Tooltip key={row.id} title={`Ver detalle de ${row.nombre} ${row.apellidos}`} placement="left">
              <TableRow
                onClick={() => onRowClick(row)}
                sx={{
                  cursor: "pointer",
                  "&:hover": { bgcolor: "primary.main", opacity: 0.04 },
                  "& td": { fontSize: 13 },
                }}
              >
                <TableCell>
                  <Typography variant="body2" fontWeight={600}>
                    {row.nombre} {row.apellidos}
                  </Typography>
                  <Typography variant="caption" color="text.secondary">
                    {row.rol}
                  </Typography>
                </TableCell>
                <TableCell>{row.ticketsCompletados}</TableCell>
                <TableCell>
                  {row.tiempoPromedioHoras != null
                    ? `${row.tiempoPromedioHoras}h`
                    : "—"}
                </TableCell>
                <TableCell>
                  {row.ratioResueltosCancelados != null
                    ? `${Math.round(row.ratioResueltosCancelados * 100)}%`
                    : "—"}
                </TableCell>
              </TableRow>
            </Tooltip>
          ))}
        </TableBody>
      </Table>
    </TableContainer>
  );
}

RendimientoTecnicoTable.propTypes = {
  rows: PropTypes.array.isRequired,
  onRowClick: PropTypes.func.isRequired,
};
```
  </action>
  <verify>
    <automated>
      cd C:\Users\ilesm\Documents\SIAST\apps\web && npm run build 2>&1 | tail -20
    </automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/components/metricas/DateRangeFilter.jsx existe
    - grep -c "AdapterDateFns" apps/web/src/components/metricas/DateRangeFilter.jsx devuelve 1 (no AdapterDayjs — UI-SPEC nota 4)
    - grep -c "TuneIcon" apps/web/src/components/metricas/DateRangeFilter.jsx devuelve 1
    - grep -c "Aplicar" apps/web/src/components/metricas/DateRangeFilter.jsx devuelve 1
    - grep -c "Cancelar" apps/web/src/components/metricas/DateRangeFilter.jsx devuelve 1
    - apps/web/src/components/metricas/EficienciaTable.jsx existe con SlaIndicator importado
    - apps/web/src/components/metricas/RendimientoTecnicoTable.jsx existe
    - grep -c "onRowClick" apps/web/src/components/metricas/EficienciaTable.jsx devuelve al menos 1 (drill-down)
    - grep -c "onRowClick" apps/web/src/components/metricas/RendimientoTecnicoTable.jsx devuelve al menos 1 (drill-down)
    - npm run build en apps/web termina sin errores de compilación relacionados a los nuevos componentes
  </acceptance_criteria>
  <done>DateRangeFilter con AdapterDateFns + TuneIcon + Aplicar/Cancelar creado. EficienciaTable y RendimientoTecnicoTable con drill-down click creados. Build de apps/web sin errores.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend → GET /api/metricas | getMetricas envía params como query string; el backend ya los valida (Plan 02) |
| Propiedades de row.areaSoporteId en tables | Datos vienen del backend autenticado — scoping ya garantizado por Plan 02 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Information Disclosure | EficienciaTable — click drill-down revela areaId | accept | areaId es interno al sistema y no es PII; ya está disponible en el token del usuario. El drill-down solo navega entre tabs del mismo dashboard. |
| T-04-03-02 | Tampering | DateRangeFilter — fechas enviadas como params | accept | La validación de fechas ocurre en el backend (Plan 02, Zod validation). Frontend envía lo que el DatePicker produce (siempre Date válido). |
| T-04-03-03 | Information Disclosure | recharts library | accept | Vetting completado en UI-SPEC §Registry Safety: no network calls, no eval, no process.env access. Approved. |
</threat_model>

<verification>
```bash
# Build de apps/web sin errores
cd C:\Users\ilesm\Documents\SIAST\apps\web && npm run build

# Verificar componentes existen
ls apps/web/src/components/metricas/

# Recharts instalado
cat apps/web/package.json | grep recharts

# SlaIndicator tiene 3 rangos de color
grep -c "pct >= 90\|pct >= 70" apps/web/src/components/metricas/SlaIndicator.jsx

# RechartsBarChart tiene Box height explícito
grep "height: 260" apps/web/src/components/metricas/RechartsBarChart.jsx
```
</verification>

<success_criteria>
- recharts instalado en apps/web/node_modules y en package.json
- 8 archivos creados en apps/web/src/components/metricas/ + apps/web/src/api/metricas.js
- RechartsBarChart, RechartsLineChart: Box padre con height={260} antes de ResponsiveContainer
- RechartsPieChart: Box padre con height={220}
- Tooltip antes de Legend en los 3 chart components
- DateRangeFilter usa AdapterDateFns (no AdapterDayjs)
- EficienciaTable y RendimientoTecnicoTable tienen onRowClick en filas (drill-down)
- npm run build de apps/web pasa sin errores
</success_criteria>

<output>
Al completar, crear `.planning/phases/04-metricas-operacionales/04-03-SUMMARY.md`
</output>
