---
phase: 04-metricas-operacionales
plan: "04"
type: execute
wave: 4
depends_on:
  - "04-03"
files_modified:
  - apps/web/src/components/metricas/MetricasTabGlobal.jsx
  - apps/web/src/components/metricas/MetricasTabResponsable.jsx
  - apps/web/src/components/metricas/MetricasTabTecnico.jsx
  - apps/web/src/components/metricas/MetricasOperacionalesSection.jsx
  - apps/web/src/pages/DashboardPage.jsx
  - apps/api/src/index.ts
autonomous: true
requirements:
  - MET-01
  - MET-02
  - MET-03
  - MET-04

must_haves:
  truths:
    - "ADMIN ve los 3 tabs (Global, Por Responsable, Por Técnico) en DashboardPage"
    - "RESPONSABLE_* ve solo el tab Por Responsable filtrado a su área"
    - "TECNICO_* ve solo el tab Por Técnico con sus propios datos"
    - "Cambiar el rango de fechas dispara refetch de métricas"
    - "Cuando ticketsVersion cambia (cualquier evento socket), las métricas se refrescan automáticamente"
    - "Click en fila de responsable en Tab Global navega a Tab Por Responsable del área"
    - "Click en fila de técnico en Tab Por Responsable navega a Tab Por Técnico del técnico"
    - "El dashboard se puede imprimir con Ctrl+P mostrando todos los tabs visibles simultáneamente"
    - "Job diario guarda un snapshot en MetricasHistorial cada 24h desde arranque del servidor"
  artifacts:
    - path: "apps/web/src/components/metricas/MetricasOperacionalesSection.jsx"
      provides: "Contenedor principal con tabs, DateRangeFilter, refetch por ticketsVersion"
      contains: "MetricasOperacionalesSection"
    - path: "apps/web/src/components/metricas/MetricasTabGlobal.jsx"
      provides: "Tab Global para ADMIN: KPIs + 3 charts + tabla eficiencia responsables"
      contains: "MetricasTabGlobal"
    - path: "apps/web/src/components/metricas/MetricasTabResponsable.jsx"
      provides: "Tab Por Responsable: KPIs + 3 charts + tabla rendimiento técnicos"
      contains: "MetricasTabResponsable"
    - path: "apps/web/src/components/metricas/MetricasTabTecnico.jsx"
      provides: "Tab Por Técnico: KPIs + 3 charts"
      contains: "MetricasTabTecnico"
    - path: "apps/web/src/pages/DashboardPage.jsx"
      provides: "DashboardPage con MetricasOperacionalesSection insertada al final"
      contains: "MetricasOperacionalesSection"
  key_links:
    - from: "apps/web/src/components/metricas/MetricasOperacionalesSection.jsx"
      to: "apps/web/src/store/notificaciones.js"
      via: "useNotifStore(s => s.ticketsVersion)"
      pattern: "ticketsVersion"
    - from: "apps/web/src/components/metricas/MetricasOperacionalesSection.jsx"
      to: "apps/web/src/api/metricas.js"
      via: "getMetricas({ tipo, fechaInicio, fechaFin })"
      pattern: "getMetricas"
    - from: "apps/web/src/pages/DashboardPage.jsx"
      to: "apps/web/src/components/metricas/MetricasOperacionalesSection.jsx"
      via: "import { MetricasOperacionalesSection } + condicional por rol"
      pattern: "MetricasOperacionalesSection"
    - from: "apps/api/src/index.ts"
      to: "metricas.service.ts"
      via: "setInterval job diario que llama obtenerMetricasGlobal y guarda en MetricasHistorial"
      pattern: "MetricasHistorial"
---

<objective>
Ensamblar los tres tabs del dashboard (MetricasTabGlobal, MetricasTabResponsable, MetricasTabTecnico),
el contenedor principal MetricasOperacionalesSection con refetch via ticketsVersion, integrar la sección
en DashboardPage con visibilidad por rol, agregar CSS print-friendly, y arrancar el job diario de
snapshots MetricasHistorial en el startup del API.

Propósito: Este plan es la "integración final" que conecta todos los átomos de Plan 03 con la página
existente y el backend.

Salida: Dashboard de métricas completamente funcional en DashboardPage para ADMIN, RESPONSABLE_* y TECNICO_*.
</objective>

<execution_context>
@C:\Users\ilesm\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\ilesm\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\ilesm\Documents\SIAST\.planning\PROJECT.md
@C:\Users\ilesm\Documents\SIAST\.planning\ROADMAP.md
@C:\Users\ilesm\Documents\SIAST\.planning\phases\04-metricas-operacionales\04-CONTEXT.md
@C:\Users\ilesm\Documents\SIAST\.planning\phases\04-metricas-operacionales\04-RESEARCH.md
@C:\Users\ilesm\Documents\SIAST\.planning\phases\04-metricas-operacionales\04-UI-SPEC.md
@C:\Users\ilesm\Documents\SIAST\.planning\phases\04-metricas-operacionales\04-PATTERNS.md
@C:\Users\ilesm\Documents\SIAST\.planning\phases\04-metricas-operacionales\04-03-SUMMARY.md

<interfaces>
<!-- useNotifStore — ticketsVersion counter (de apps/web/src/store/notificaciones.js) -->
```javascript
export const useNotifStore = create((set, get) => ({
  ticketsVersion: 0,   // se incrementa con cada evento de ticket
  // ...
}));
// Uso en componente:
const ticketsVersion = useNotifStore((s) => s.ticketsVersion);
useEffect(() => { /* refetch */ }, [ticketsVersion, dateRange, activeTab]);
```

<!-- useAuthStore — para obtener rol y areaSoporteId -->
```javascript
// apps/web/src/store/auth.js
const { user } = useAuthStore();
// user.rol, user.id, user.areaSoporteId
```

<!-- Componentes base ya creados (Plan 03) -->
```jsx
import { SlaIndicator } from "./SlaIndicator.jsx";
import { RechartsBarChart } from "./RechartsBarChart.jsx";
import { RechartsLineChart } from "./RechartsLineChart.jsx";
import { RechartsPieChart } from "./RechartsPieChart.jsx";
import { DateRangeFilter } from "./DateRangeFilter.jsx";
import { EficienciaTable } from "./EficienciaTable.jsx";
import { RendimientoTecnicoTable } from "./RendimientoTecnicoTable.jsx";
```

<!-- StatCard disponible en DashboardPage.jsx (extraer o reusar como subcomponente) -->
```jsx
const StatCard = ({ icon, label, value, color, subtitle }) => (
  <Card sx={{ height: "100%" }}>
    <CardContent sx={{ display: "flex", alignItems: "flex-start", gap: 2, py: "16px !important" }}>
      <Box sx={{ p: 1, borderRadius: 2, bgcolor: `${color}18`, color, mt: 0.3, flexShrink: 0 }}>
        {icon}
      </Box>
      <Box>
        <Typography variant="h4" fontWeight={700} lineHeight={1}>{value}</Typography>
        <Typography variant="body2" color="text.secondary" sx={{ mt: 0.4 }}>{label}</Typography>
        {subtitle && <Typography variant="caption" color="text.disabled">{subtitle}</Typography>}
      </Box>
    </CardContent>
  </Card>
);
```

<!-- Tab visibility por rol (D-14, D-15) -->
// ADMIN: ve 3 tabs (Global=0, Por Responsable=1, Por Técnico=2)
// RESPONSABLE_*: ve solo tab 1 (Por Responsable), con activeTab forzado a 1
// TECNICO_*: ve solo tab 2 (Por Técnico), con activeTab forzado a 2

<!-- Colores Recharts palette (UI-SPEC) -->
// Serie 1: #9d2449, Serie 2: #b56e85, Serie 3: #742035, Serie 4: #c8a0b0, Serie 5: #a83e6a

<!-- Responsable_* roles list para condicionales en DashboardPage -->
const ROLES_RESPONSABLE = ["RESPONSABLE_TI","RESPONSABLE_REDES","RESPONSABLE_MANTENIMIENTO","RESPONSABLE_RECURSOS_MATERIALES"];
const ROLES_TECNICO = ["TECNICO_TI","TECNICO_REDES","TECNICO_SERVICIOS","TECNICO_ELECTRICISTA","TECNICO_PLOMERO","TECNICO_MOVILIDAD"];

<!-- Copywriting de tabs (UI-SPEC) -->
// "Global", "Por Responsable", "Por Técnico"
// Section heading: "Métricas Operacionales"
// Subheading: "Últimos 30 días · actualización en tiempo real"

<!-- Job diario en apps/api/src/index.ts — patrón setInterval -->
// Ejecutar después de que el server arranca
// Delay inicial: 0 ms (inmediato en dev, en prod usar Math.round para medianoche)
// Interval: 24 * 60 * 60 * 1000 ms
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: Crear los tres tabs de contenido y MetricasOperacionalesSection</name>
  <files>
    apps/web/src/components/metricas/MetricasTabGlobal.jsx
    apps/web/src/components/metricas/MetricasTabResponsable.jsx
    apps/web/src/components/metricas/MetricasTabTecnico.jsx
    apps/web/src/components/metricas/MetricasOperacionalesSection.jsx
  </files>
  <read_first>
    - apps/web/src/components/metricas/ — listar archivos para confirmar que los 8 componentes del Plan 03 están presentes antes de importarlos
    - apps/web/src/store/auth.js — leer completo para confirmar cómo acceder a user.rol y user.areaSoporteId desde useAuthStore
    - apps/web/src/store/notificaciones.js — leer líneas 39-50 para confirmar el nombre exacto del campo ticketsVersion y el selector
    - apps/web/src/api/metricas.js — verificar la signatura de getMetricas (ya creada en Plan 03)
  </read_first>
  <action>
**Paso 1: Crear MetricasTabGlobal.jsx (Tab Global — ADMIN)**

KPIs: Total solicitudes, Activos ahora, Resueltos (período), Cumplimiento SLA.
Charts: Barras comparativo por área, Líneas tendencia diaria, Pastel distribución categoría.
Tabla: eficiencia responsables con drill-down.

```jsx
import { Grid, Card, CardContent, Typography, Box, Skeleton } from "@mui/material";
import ConfirmationNumberIcon from "@mui/icons-material/ConfirmationNumber";
import HourglassEmptyIcon from "@mui/icons-material/HourglassEmpty";
import CheckCircleIcon from "@mui/icons-material/CheckCircle";
import VerifiedIcon from "@mui/icons-material/Verified";
import { RechartsBarChart } from "./RechartsBarChart.jsx";
import { RechartsLineChart } from "./RechartsLineChart.jsx";
import { RechartsPieChart } from "./RechartsPieChart.jsx";
import { SlaIndicator } from "./SlaIndicator.jsx";
import { EficienciaTable } from "./EficienciaTable.jsx";
import PropTypes from "prop-types";

const StatCard = ({ icon, label, value, color }) => (
  <Card sx={{ height: "100%" }}>
    <CardContent sx={{ display: "flex", alignItems: "flex-start", gap: 2, py: "16px !important" }}>
      <Box sx={{ p: 1, borderRadius: 2, bgcolor: `${color}18`, color, mt: 0.3, flexShrink: 0 }}>
        {icon}
      </Box>
      <Box>
        <Typography variant="h4" fontWeight={700} lineHeight={1}>{value}</Typography>
        <Typography variant="body2" color="text.secondary" sx={{ mt: 0.4 }}>{label}</Typography>
      </Box>
    </CardContent>
  </Card>
);

// Colores por categoría (UI-SPEC)
const CAT_COLORS = { TECNOLOGIAS: "#1565c0", SERVICIOS: "#2e7d32", RECURSOS_MATERIALES: "#e65100" };

export function MetricasTabGlobal({ data, loading, onResponsableClick }) {
  if (loading || !data) {
    return (
      <Grid container spacing={2}>
        {[1, 2, 3, 4].map((i) => (
          <Grid item xs={6} sm={4} md={3} key={i}>
            <Skeleton variant="rectangular" height={96} sx={{ borderRadius: 1 }} />
          </Grid>
        ))}
        <Grid item xs={12}><Skeleton variant="rectangular" height={260} sx={{ borderRadius: 1 }} /></Grid>
      </Grid>
    );
  }

  const barDataArea = data.comparativoPorArea.map((a) => ({
    area: a.areaNombre,
    Total: a.total,
    Resueltos: a.resueltos,
  }));

  const lineData = data.tendenciaDiaria.map((d) => ({
    dia: d.dia.slice(5), // MM-DD
    Creados: d.creados,
    Resueltos: d.resueltos,
  }));

  const pieData = data.distribucionCategoria.map((c) => ({
    name: c.categoria,
    value: c.total,
    color: CAT_COLORS[c.categoria] ?? "#9e9e9e",
  }));

  return (
    <Box sx={{ "@media print": { "& .no-print": { display: "none" } } }}>
      {/* KPI Cards */}
      <Grid container spacing={2} sx={{ mb: 3 }}>
        <Grid item xs={6} sm={4} md={3}>
          <StatCard icon={<ConfirmationNumberIcon />} label="Total solicitudes" value={data.totalTickets} color="#9d2449" />
        </Grid>
        <Grid item xs={6} sm={4} md={3}>
          <StatCard icon={<HourglassEmptyIcon />} label="Activos ahora" value={data.ticketsActivos} color="#ea580c" />
        </Grid>
        <Grid item xs={6} sm={4} md={3}>
          <StatCard icon={<CheckCircleIcon />} label="Resueltos (período)" value={data.ticketsResueltos} color="#2e7d32" />
        </Grid>
        <Grid item xs={6} sm={4} md={3}>
          <Card sx={{ height: "100%" }}>
            <CardContent sx={{ py: "16px !important" }}>
              <Typography variant="body2" color="text.secondary" sx={{ mb: 0.5 }}>Cumplimiento SLA</Typography>
              <SlaIndicator pct={data.slaGlobal} />
            </CardContent>
          </Card>
        </Grid>
      </Grid>

      {/* Charts */}
      <Grid container spacing={2} sx={{ mb: 3 }}>
        <Grid item xs={12} md={6}>
          <Typography variant="subtitle2" fontWeight={700} color="primary.main" sx={{ mb: 1 }}>
            SOLICITUDES POR ÁREA
          </Typography>
          <RechartsBarChart
            data={barDataArea}
            xKey="area"
            bars={[
              { key: "Total", label: "Total", color: "#9d2449" },
              { key: "Resueltos", label: "Resueltos", color: "#2e7d32" },
            ]}
          />
        </Grid>
        <Grid item xs={12} md={6}>
          <Typography variant="subtitle2" fontWeight={700} color="primary.main" sx={{ mb: 1 }}>
            TENDENCIA DIARIA (CREADOS VS RESUELTOS)
          </Typography>
          <RechartsLineChart
            data={lineData}
            xKey="dia"
            lines={[
              { key: "Creados", label: "Creados", color: "#9d2449" },
              { key: "Resueltos", label: "Resueltos", color: "#2e7d32" },
            ]}
          />
        </Grid>
        <Grid item xs={12} md={6}>
          <Typography variant="subtitle2" fontWeight={700} color="primary.main" sx={{ mb: 1 }}>
            DISTRIBUCIÓN POR CATEGORÍA
          </Typography>
          <RechartsPieChart data={pieData} />
        </Grid>
      </Grid>

      {/* Tabla eficiencia responsables */}
      <Typography variant="subtitle2" fontWeight={700} color="primary.main" sx={{ mb: 1 }}>
        EFICIENCIA DE RESPONSABLES
      </Typography>
      <EficienciaTable rows={data.eficienciaResponsables} onRowClick={onResponsableClick} />
    </Box>
  );
}

MetricasTabGlobal.propTypes = {
  data: PropTypes.object,
  loading: PropTypes.bool.isRequired,
  onResponsableClick: PropTypes.func.isRequired,
};
```

**Paso 2: Crear MetricasTabResponsable.jsx (Tab Por Responsable)**

KPIs: Tickets activos, Tiempo promedio, SLA%, Tickets reabiertos.
Charts: Barras carga de técnicos, Líneas creados vs resueltos, Pastel subcategorías.
Tabla: rendimiento técnicos con drill-down.

```jsx
import { Grid, Card, CardContent, Typography, Box, Skeleton } from "@mui/material";
import HourglassEmptyIcon from "@mui/icons-material/HourglassEmpty";
import UpdateIcon from "@mui/icons-material/Update";
import ReplayIcon from "@mui/icons-material/Replay";
import { RechartsBarChart } from "./RechartsBarChart.jsx";
import { RechartsLineChart } from "./RechartsLineChart.jsx";
import { RechartsPieChart } from "./RechartsPieChart.jsx";
import { SlaIndicator } from "./SlaIndicator.jsx";
import { RendimientoTecnicoTable } from "./RendimientoTecnicoTable.jsx";
import PropTypes from "prop-types";

const StatCard = ({ icon, label, value, color }) => (
  <Card sx={{ height: "100%" }}>
    <CardContent sx={{ display: "flex", alignItems: "flex-start", gap: 2, py: "16px !important" }}>
      <Box sx={{ p: 1, borderRadius: 2, bgcolor: `${color}18`, color, mt: 0.3, flexShrink: 0 }}>
        {icon}
      </Box>
      <Box>
        <Typography variant="h4" fontWeight={700} lineHeight={1}>{value}</Typography>
        <Typography variant="body2" color="text.secondary" sx={{ mt: 0.4 }}>{label}</Typography>
      </Box>
    </CardContent>
  </Card>
);

// Recharts palette para subcategorías
const PALETTE = ["#9d2449", "#b56e85", "#742035", "#c8a0b0", "#a83e6a", "#1565c0", "#2e7d32", "#e65100"];

export function MetricasTabResponsable({ data, loading, onTecnicoClick }) {
  if (loading || !data) {
    return (
      <Grid container spacing={2}>
        {[1, 2, 3, 4].map((i) => (
          <Grid item xs={6} sm={4} key={i}>
            <Skeleton variant="rectangular" height={96} sx={{ borderRadius: 1 }} />
          </Grid>
        ))}
        <Grid item xs={12}><Skeleton variant="rectangular" height={260} sx={{ borderRadius: 1 }} /></Grid>
      </Grid>
    );
  }

  const barCarga = data.cargaTecnicos.map((t) => ({
    tecnico: t.tecnicoNombre.split(" ")[0], // primer nombre para brevedad
    Activos: t.activos,
    Completados: t.completados,
  }));

  const lineData = data.tendenciaDiaria.map((d) => ({
    dia: d.dia.slice(5),
    Creados: d.creados,
    Resueltos: d.resueltos,
  }));

  const pieData = data.distribucionSubcategoria.map((c, i) => ({
    name: c.categoria,
    value: c.total,
    color: PALETTE[i % PALETTE.length],
  }));

  return (
    <Box>
      <Typography variant="h6" fontWeight={700} color="primary.main" sx={{ mb: 2 }}>
        {data.areaNombre}
      </Typography>

      {/* KPI Cards */}
      <Grid container spacing={2} sx={{ mb: 3 }}>
        <Grid item xs={6} sm={4} md={3}>
          <StatCard icon={<HourglassEmptyIcon />} label="Tickets activos" value={data.ticketsActivos} color="#ea580c" />
        </Grid>
        <Grid item xs={6} sm={4} md={3}>
          <StatCard
            icon={<UpdateIcon />}
            label="Tiempo promedio resolución"
            value={data.tiempoPromedioHoras != null ? `${data.tiempoPromedioHoras}h` : "—"}
            color="#9d2449"
          />
        </Grid>
        <Grid item xs={6} sm={4} md={3}>
          <Card sx={{ height: "100%" }}>
            <CardContent sx={{ py: "16px !important" }}>
              <Typography variant="body2" color="text.secondary" sx={{ mb: 0.5 }}>Cumplimiento SLA</Typography>
              <SlaIndicator pct={data.slaGlobal} />
            </CardContent>
          </Card>
        </Grid>
        <Grid item xs={6} sm={4} md={3}>
          <StatCard icon={<ReplayIcon />} label="Tickets reabiertos" value={data.ticketsReabiertos} color="#ca8a04" />
        </Grid>
      </Grid>

      {/* Charts */}
      <Grid container spacing={2} sx={{ mb: 3 }}>
        <Grid item xs={12} md={6}>
          <Typography variant="subtitle2" fontWeight={700} color="primary.main" sx={{ mb: 1 }}>
            CARGA DE TÉCNICOS
          </Typography>
          <RechartsBarChart
            data={barCarga}
            xKey="tecnico"
            bars={[
              { key: "Activos", label: "Activos", color: "#ea580c" },
              { key: "Completados", label: "Completados", color: "#2e7d32" },
            ]}
          />
        </Grid>
        <Grid item xs={12} md={6}>
          <Typography variant="subtitle2" fontWeight={700} color="primary.main" sx={{ mb: 1 }}>
            TENDENCIA DIARIA (CREADOS VS RESUELTOS)
          </Typography>
          <RechartsLineChart
            data={lineData}
            xKey="dia"
            lines={[
              { key: "Creados", label: "Creados", color: "#9d2449" },
              { key: "Resueltos", label: "Resueltos", color: "#2e7d32" },
            ]}
          />
        </Grid>
        <Grid item xs={12} md={6}>
          <Typography variant="subtitle2" fontWeight={700} color="primary.main" sx={{ mb: 1 }}>
            DISTRIBUCIÓN SUBCATEGORÍAS
          </Typography>
          <RechartsPieChart data={pieData} />
        </Grid>
      </Grid>

      {/* Tabla rendimiento técnicos */}
      <Typography variant="subtitle2" fontWeight={700} color="primary.main" sx={{ mb: 1 }}>
        RENDIMIENTO DE TÉCNICOS
      </Typography>
      <RendimientoTecnicoTable rows={data.rendimientoTecnicos} onRowClick={onTecnicoClick} />
    </Box>
  );
}

MetricasTabResponsable.propTypes = {
  data: PropTypes.object,
  loading: PropTypes.bool.isRequired,
  onTecnicoClick: PropTypes.func.isRequired,
};
```

**Paso 3: Crear MetricasTabTecnico.jsx (Tab Por Técnico)**

KPIs: Completados, Tiempo promedio, Ratio resuelto/cancelado.
Charts: Barras comparativa vs promedio área, Pastel resueltos/cancelados, Línea productividad.

```jsx
import { Grid, Card, CardContent, Typography, Box, Skeleton } from "@mui/material";
import CheckCircleIcon from "@mui/icons-material/CheckCircle";
import UpdateIcon from "@mui/icons-material/Update";
import BalanceIcon from "@mui/icons-material/Balance";
import { RechartsBarChart } from "./RechartsBarChart.jsx";
import { RechartsLineChart } from "./RechartsLineChart.jsx";
import { RechartsPieChart } from "./RechartsPieChart.jsx";
import PropTypes from "prop-types";

const StatCard = ({ icon, label, value, color }) => (
  <Card sx={{ height: "100%" }}>
    <CardContent sx={{ display: "flex", alignItems: "flex-start", gap: 2, py: "16px !important" }}>
      <Box sx={{ p: 1, borderRadius: 2, bgcolor: `${color}18`, color, mt: 0.3, flexShrink: 0 }}>
        {icon}
      </Box>
      <Box>
        <Typography variant="h4" fontWeight={700} lineHeight={1}>{value}</Typography>
        <Typography variant="body2" color="text.secondary" sx={{ mt: 0.4 }}>{label}</Typography>
      </Box>
    </CardContent>
  </Card>
);

export function MetricasTabTecnico({ data, loading }) {
  if (loading || !data) {
    return (
      <Grid container spacing={2}>
        {[1, 2, 3].map((i) => (
          <Grid item xs={6} sm={4} key={i}>
            <Skeleton variant="rectangular" height={96} sx={{ borderRadius: 1 }} />
          </Grid>
        ))}
        <Grid item xs={12}><Skeleton variant="rectangular" height={260} sx={{ borderRadius: 1 }} /></Grid>
      </Grid>
    );
  }

  const lineData = data.tendenciaProductividad.map((d) => ({
    dia: d.dia.slice(5),
    Completados: d.resueltos,
  }));

  return (
    <Box>
      <Typography variant="h6" fontWeight={700} color="primary.main" sx={{ mb: 2 }}>
        {data.tecnicoNombre}
      </Typography>

      {/* KPI Cards */}
      <Grid container spacing={2} sx={{ mb: 3 }}>
        <Grid item xs={6} sm={4}>
          <StatCard icon={<CheckCircleIcon />} label="Completados (período)" value={data.ticketsCompletados} color="#2e7d32" />
        </Grid>
        <Grid item xs={6} sm={4}>
          <StatCard
            icon={<UpdateIcon />}
            label="Tiempo promedio resolución"
            value={data.tiempoPromedioHoras != null ? `${data.tiempoPromedioHoras}h` : "—"}
            color="#9d2449"
          />
        </Grid>
        <Grid item xs={6} sm={4}>
          <StatCard
            icon={<BalanceIcon />}
            label="Ratio resuelto / cancelado"
            value={data.ratioResueltosCancelados != null ? `${Math.round(data.ratioResueltosCancelados * 100)}%` : "—"}
            color="#1565c0"
          />
        </Grid>
      </Grid>

      {/* Charts */}
      <Grid container spacing={2}>
        <Grid item xs={12} md={6}>
          <Typography variant="subtitle2" fontWeight={700} color="primary.main" sx={{ mb: 1 }}>
            TENDENCIA DE PRODUCTIVIDAD
          </Typography>
          <RechartsLineChart
            data={lineData}
            xKey="dia"
            lines={[{ key: "Completados", label: "Completados", color: "#9d2449" }]}
          />
        </Grid>
        <Grid item xs={12} md={6}>
          <Typography variant="subtitle2" fontWeight={700} color="primary.main" sx={{ mb: 1 }}>
            RESUELTOS VS CANCELADOS
          </Typography>
          <RechartsPieChart data={data.distribucionResultado} />
        </Grid>
        {data.comparativaVsArea?.length > 0 && (
          <Grid item xs={12} md={6}>
            <Typography variant="subtitle2" fontWeight={700} color="primary.main" sx={{ mb: 1 }}>
              COMPARATIVA VS PROMEDIO ÁREA
            </Typography>
            <RechartsBarChart
              data={data.comparativaVsArea}
              xKey="label"
              bars={[
                { key: "tecnico", label: "Este técnico", color: "#9d2449" },
                { key: "promedioArea", label: "Promedio área", color: "#b56e85" },
              ]}
            />
          </Grid>
        )}
      </Grid>
    </Box>
  );
}

MetricasTabTecnico.propTypes = {
  data: PropTypes.object,
  loading: PropTypes.bool.isRequired,
};
```

**Paso 4: Crear MetricasOperacionalesSection.jsx (contenedor principal)**

Responsabilidades: tabs por rol, DateRangeFilter, refetch por ticketsVersion y dateRange, LinearProgress durante carga, drill-down state management, empty/error states.

```jsx
import { useEffect, useState, useMemo } from "react";
import {
  Box, Tabs, Tab, Typography, LinearProgress, Alert, Button, Divider, Badge,
} from "@mui/material";
import BarChartIcon from "@mui/icons-material/BarChart";
import { subDays, format } from "date-fns";
import { useNotifStore } from "../../store/notificaciones.js";
import { getMetricas } from "../../api/metricas.js";
import { DateRangeFilter } from "./DateRangeFilter.jsx";
import { MetricasTabGlobal } from "./MetricasTabGlobal.jsx";
import { MetricasTabResponsable } from "./MetricasTabResponsable.jsx";
import { MetricasTabTecnico } from "./MetricasTabTecnico.jsx";
import PropTypes from "prop-types";

const ROLES_RESPONSABLE = [
  "RESPONSABLE_TI", "RESPONSABLE_REDES",
  "RESPONSABLE_MANTENIMIENTO", "RESPONSABLE_RECURSOS_MATERIALES",
];
const ROLES_TECNICO = [
  "TECNICO_TI", "TECNICO_REDES", "TECNICO_SERVICIOS",
  "TECNICO_ELECTRICISTA", "TECNICO_PLOMERO", "TECNICO_MOVILIDAD",
];

function tipoFromTab(tab) {
  if (tab === 0) return "area";
  if (tab === 1) return "tecnico";
  return "proceso";
}

/**
 * Sección de métricas operacionales dentro de DashboardPage.
 * Gestiona el estado de tabs, filtro de fechas, y refetch por ticketsVersion.
 */
export function MetricasOperacionalesSection({ rol, areaSoporteId, userId }) {
  const ticketsVersion = useNotifStore((s) => s.ticketsVersion);
  const [data, setData] = useState(null);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState(null);
  const [dateRange, setDateRange] = useState({
    start: subDays(new Date(), 30),
    end: new Date(),
  });

  // Determinar tab inicial y visible según rol
  const initialTab = useMemo(() => {
    if (ROLES_RESPONSABLE.includes(rol)) return 1;
    if (ROLES_TECNICO.includes(rol)) return 2;
    return 0; // ADMIN / MESA_AYUDA
  }, [rol]);

  const [activeTab, setActiveTab] = useState(initialTab);
  const [selectedAreaId, setSelectedAreaId] = useState(areaSoporteId ?? null);
  const [selectedTecnicoId, setSelectedTecnicoId] = useState(null);

  // Determinar visibilidad de tabs según rol
  const showGlobal = rol === "ADMIN" || rol === "MESA_AYUDA";
  const showResponsable = showGlobal || ROLES_RESPONSABLE.includes(rol);
  const showTecnico = showGlobal || ROLES_TECNICO.includes(rol);

  useEffect(() => {
    setLoading(true);
    setError(null);

    const tipo = tipoFromTab(activeTab);
    const params = {
      tipo,
      fechaInicio: format(dateRange.start, "yyyy-MM-dd"),
      fechaFin: format(dateRange.end, "yyyy-MM-dd"),
      ...(tipo === "tecnico" && selectedAreaId ? { areaId: selectedAreaId } : {}),
      ...(tipo === "proceso" && selectedTecnicoId ? { tecnicoId: selectedTecnicoId } : {}),
    };

    getMetricas(params)
      .then(setData)
      .catch((err) => setError(err?.response?.data?.error ?? "Error al cargar métricas"))
      .finally(() => setLoading(false));
  }, [ticketsVersion, dateRange.start, dateRange.end, activeTab, selectedAreaId, selectedTecnicoId]);

  // Drill-down: click en responsable desde Tab Global → Tab Por Responsable
  const handleResponsableClick = (row) => {
    setSelectedAreaId(row.areaSoporteId);
    setActiveTab(1);
  };

  // Drill-down: click en técnico desde Tab Por Responsable → Tab Por Técnico
  const handleTecnicoClick = (row) => {
    setSelectedTecnicoId(row.id);
    setActiveTab(2);
  };

  return (
    <Box sx={{ mt: 4 }}>
      {/* Divisor */}
      <Divider sx={{ mb: 3 }} />

      {/* Encabezado de sección */}
      <Box
        sx={{
          display: "flex",
          alignItems: "center",
          justifyContent: "space-between",
          mb: 2,
          "@media print": { mb: 1 },
        }}
      >
        <Box sx={{ display: "flex", alignItems: "center", gap: 1 }}>
          <BarChartIcon sx={{ color: "primary.main" }} />
          <Box>
            <Typography variant="h5" fontWeight={700}>Métricas Operacionales</Typography>
            <Typography variant="caption" color="text.secondary">
              Últimos 30 días · actualización en tiempo real
            </Typography>
          </Box>
        </Box>
        <Box sx={{ "@media print": { display: "none" } }}>
          <DateRangeFilter value={dateRange} onChange={setDateRange} />
        </Box>
      </Box>

      {/* LinearProgress durante refetch (D-16 UI-SPEC) */}
      <Box sx={{ height: 2, mb: 1 }}>
        {loading && <LinearProgress sx={{ height: 2 }} />}
      </Box>

      {/* Tabs (ocultos en print — todos los panels se muestran) */}
      <Box sx={{ borderBottom: 1, borderColor: "divider", "@media print": { display: "none" } }}>
        <Tabs
          value={activeTab}
          onChange={(_, v) => setActiveTab(v)}
          variant="scrollable"
          scrollButtons="auto"
          indicatorColor="primary"
          textColor="primary"
        >
          {showGlobal && <Tab label="Global" value={0} />}
          {showResponsable && <Tab label="Por Responsable" value={1} />}
          {showTecnico && <Tab label="Por Técnico" value={2} />}
        </Tabs>
      </Box>

      {/* Error state */}
      {error && (
        <Alert
          severity="error"
          sx={{ mt: 2 }}
          action={
            <Button size="small" onClick={() => setError(null)}>
              Reintentar
            </Button>
          }
        >
          <Typography variant="body2" fontWeight={600}>No se pudieron cargar las métricas</Typography>
          <Typography variant="caption">{error}</Typography>
        </Alert>
      )}

      {/* Tab panels */}
      {!error && (
        <Box sx={{ mt: 3 }}>
          {/* Tab Global — solo ADMIN/MESA_AYUDA */}
          {showGlobal && (
            <Box
              sx={{
                display: activeTab === 0 ? "block" : "none",
                "@media print": { display: "block" },
              }}
            >
              <MetricasTabGlobal
                data={data?.tipo === "area" ? data : null}
                loading={loading && activeTab === 0}
                onResponsableClick={handleResponsableClick}
              />
            </Box>
          )}

          {/* Tab Por Responsable */}
          {showResponsable && (
            <Box
              sx={{
                display: activeTab === 1 ? "block" : "none",
                "@media print": { display: "block", mt: 4 },
              }}
            >
              <MetricasTabResponsable
                data={data?.tipo === "tecnico" ? data : null}
                loading={loading && activeTab === 1}
                onTecnicoClick={handleTecnicoClick}
              />
            </Box>
          )}

          {/* Tab Por Técnico */}
          {showTecnico && (
            <Box
              sx={{
                display: activeTab === 2 ? "block" : "none",
                "@media print": { display: "block", mt: 4 },
              }}
            >
              <MetricasTabTecnico
                data={data?.tipo === "proceso" ? data : null}
                loading={loading && activeTab === 2}
              />
            </Box>
          )}
        </Box>
      )}

      {/* Print: mostrar rango de fechas como texto */}
      <Box sx={{ display: "none", "@media print": { display: "block", mt: 2 } }}>
        <Typography variant="caption" color="text.secondary">
          Período: {format(dateRange.start, "dd/MM/yyyy")} — {format(dateRange.end, "dd/MM/yyyy")}
        </Typography>
      </Box>
    </Box>
  );
}

MetricasOperacionalesSection.propTypes = {
  rol: PropTypes.string.isRequired,
  areaSoporteId: PropTypes.number,
  userId: PropTypes.number,
};
```
  </action>
  <verify>
    <automated>
      cd C:\Users\ilesm\Documents\SIAST\apps\web && npm run build 2>&1 | tail -20
    </automated>
  </verify>
  <acceptance_criteria>
    - Los 4 archivos existen en apps/web/src/components/metricas/
    - grep -c "ticketsVersion" apps/web/src/components/metricas/MetricasOperacionalesSection.jsx devuelve al menos 1 (D-18)
    - grep -c "dateRange.start, dateRange.end" apps/web/src/components/metricas/MetricasOperacionalesSection.jsx devuelve 1 (pitfall 5 — ambas fechas en deps)
    - grep -c "handleResponsableClick" apps/web/src/components/metricas/MetricasOperacionalesSection.jsx devuelve al menos 1 (drill-down D-14)
    - grep -c "handleTecnicoClick" apps/web/src/components/metricas/MetricasOperacionalesSection.jsx devuelve al menos 1 (drill-down D-14)
    - grep -c "@media print" apps/web/src/components/metricas/MetricasOperacionalesSection.jsx devuelve al menos 1 (D-06 print-friendly)
    - grep -c "LinearProgress" apps/web/src/components/metricas/MetricasOperacionalesSection.jsx devuelve 1
    - npm run build en apps/web sin errores
  </acceptance_criteria>
  <done>4 componentes de tabs + contenedor principal creados. ticketsVersion en deps del useEffect. Drill-down responsable→tab1 y tecnico→tab2 implementados. CSS @media print en MetricasOperacionalesSection.</done>
</task>

<task type="auto">
  <name>Tarea 2: Integrar MetricasOperacionalesSection en DashboardPage y agregar job diario en API</name>
  <files>
    apps/web/src/pages/DashboardPage.jsx
    apps/api/src/index.ts
  </files>
  <read_first>
    - apps/web/src/pages/DashboardPage.jsx — leer completo para encontrar: (a) el final del JSX return donde insertar MetricasOperacionalesSection, (b) cómo se obtiene user/rol desde useAuthStore, (c) imports existentes para no duplicar
    - apps/api/src/index.ts — leer completo para encontrar dónde agregar el job diario después del app.listen() y los imports necesarios
    - apps/web/src/store/auth.js — verificar el nombre del campo areaSoporteId en el objeto user del store
  </read_first>
  <action>
**Paso 1: Modificar apps/web/src/pages/DashboardPage.jsx**

a) Agregar el import de MetricasOperacionalesSection al inicio del archivo (junto a los otros imports existentes):
```jsx
import { MetricasOperacionalesSection } from "../components/metricas/MetricasOperacionalesSection.jsx";
```

b) Localizar el objeto `user` desde `useAuthStore`. Verificar si ya existe `const { user } = useAuthStore()`
o si solo se extrae `rol`. Si solo se extrae `rol`, cambiar a extraer también `areaSoporteId` y `id`.

c) Localizar el `return (` del componente principal `DashboardPage`. Antes del cierre del `</Box>` exterior
(o justo antes del `</Box>` final del return), agregar:

```jsx
{/* Sección Métricas Operacionales — visible para ADMIN, RESPONSABLE_*, TECNICO_* */}
{(rol === "ADMIN" ||
  rol === "MESA_AYUDA" ||
  rol.startsWith("RESPONSABLE_") ||
  rol.startsWith("TECNICO_")) && (
  <MetricasOperacionalesSection
    rol={rol}
    areaSoporteId={user?.areaSoporteId}
    userId={user?.id}
  />
)}
```

NOTA: Insertar DESPUÉS de todas las secciones existentes (MetricasAdmin, MetricasTecnico,
MetricasMesaAyuda, etc.) para no alterar el contenido existente del dashboard.

**Paso 2: Modificar apps/api/src/index.ts — agregar job diario de snapshots (D-20)**

a) Agregar los imports necesarios al inicio:
```typescript
import * as metricasService from "./services/metricas.service.js";
import { prisma } from "./config/database.js";
```

b) Después de `app.listen(...)` (o después del callback del listen), agregar:

```typescript
// ── Job diario: snapshot de métricas en MetricasHistorial (Phase 4, D-20) ─────
async function ejecutarSnapshotMetricas(): Promise<void> {
  try {
    const hoy = new Date();
    hoy.setHours(0, 0, 0, 0); // inicio del día

    // Snapshot global (sin área)
    const global = await metricasService.obtenerMetricasGlobal();
    await prisma.metricasHistorial.upsert({
      where: { fecha_areaSoporteId: { fecha: hoy, areaSoporteId: null as any } },
      create: {
        fecha: hoy,
        areaSoporteId: null,
        totalTickets: global.totalTickets,
        ticketsResueltos: global.ticketsResueltos,
        ticketsActivos: global.ticketsActivos,
        slaGlobal: global.slaGlobal,
        tiempoPromedioHoras: global.tiempoPromedioHoras,
      },
      update: {
        totalTickets: global.totalTickets,
        ticketsResueltos: global.ticketsResueltos,
        ticketsActivos: global.ticketsActivos,
        slaGlobal: global.slaGlobal,
        tiempoPromedioHoras: global.tiempoPromedioHoras,
      },
    });

    // Snapshot por área
    const areas = await prisma.areaSoporte.findMany({ where: { activo: true }, select: { id: true } });
    for (const area of areas) {
      const areaData = await metricasService.obtenerMetricasPorArea(area.id);
      await prisma.metricasHistorial.upsert({
        where: { fecha_areaSoporteId: { fecha: hoy, areaSoporteId: area.id } },
        create: {
          fecha: hoy,
          areaSoporteId: area.id,
          totalTickets: areaData.ticketsActivos + areaData.ticketsReabiertos,
          ticketsResueltos: 0, // calculado en próximas iteraciones si es necesario
          ticketsActivos: areaData.ticketsActivos,
          slaGlobal: areaData.slaGlobal,
          tiempoPromedioHoras: areaData.tiempoPromedioHoras,
        },
        update: {
          ticketsActivos: areaData.ticketsActivos,
          slaGlobal: areaData.slaGlobal,
          tiempoPromedioHoras: areaData.tiempoPromedioHoras,
        },
      });
    }

    console.log(`[MetricasHistorial] Snapshot guardado — ${hoy.toISOString().slice(0, 10)}`);
  } catch (err) {
    console.error("[MetricasHistorial] Error al guardar snapshot:", err);
  }
}

// Ejecutar inmediatamente y luego cada 24h (D-20, sin node-cron — setInterval)
ejecutarSnapshotMetricas();
const VEINTICUATRO_HORAS = 24 * 60 * 60 * 1000;
setInterval(ejecutarSnapshotMetricas, VEINTICUATRO_HORAS);
```

NOTA: El `@@unique([fecha, areaSoporteId])` genera el nombre `fecha_areaSoporteId` en el cliente Prisma
(Prisma usa los nombres de campo TypeScript del schema, no los nombres de columna SQL). El `where` del
upsert usa exactamente `fecha_areaSoporteId` — no hay ambigüedad.
  </action>
  <verify>
    <automated>
      cd C:\Users\ilesm\Documents\SIAST && npx tsc --noEmit -p apps/api/tsconfig.json 2>&1 | head -20
    </automated>
  </verify>
  <acceptance_criteria>
    - grep -c "MetricasOperacionalesSection" apps/web/src/pages/DashboardPage.jsx devuelve al menos 2 (import + uso)
    - grep -c "areaSoporteId" apps/web/src/pages/DashboardPage.jsx devuelve al menos 1 (pasado como prop)
    - grep -c "MetricasHistorial\|ejecutarSnapshotMetricas" apps/api/src/index.ts devuelve al menos 1 (job diario presente)
    - grep -c "setInterval" apps/api/src/index.ts devuelve al menos 1 (D-20 — sin node-cron)
    - npm run build en apps/web sin errores relacionados a DashboardPage.jsx
    - npx tsc --noEmit en apps/api sin errores relacionados a index.ts o metricas.service.ts
    - El dashboard arranca (npm run dev:api + npm run dev:web) y DashboardPage carga sin errores en consola del navegador para rol ADMIN
  </acceptance_criteria>
  <done>MetricasOperacionalesSection insertada en DashboardPage con condicional por rol. Job diario en apps/api/src/index.ts con setInterval cada 24h. Build y TypeScript sin errores.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DashboardPage → MetricasOperacionalesSection | rol y areaSoporteId vienen del JWT en useAuthStore — no del DOM ni URL params |
| API job (setInterval) → DB | El job corre en el proceso del servidor con acceso completo a Prisma — no hay input de usuario |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Information Disclosure | MetricasOperacionalesSection — tabs visibles | mitigate | showGlobal/showResponsable/showTecnico controlados por `rol` del JWT (useAuthStore). Frontend no muestra ni renderiza tabs fuera del scope del rol. Backend ya valida el scope — doble capa. |
| T-04-04-02 | Elevation of Privilege | DashboardPage — condicional de visibilidad | mitigate | La condición `rol.startsWith("RESPONSABLE_")` en DashboardPage es un guard UI. El backend (Plan 02) ya fuerza el scope en el servidor. Si el frontend falla, el backend protege. |
| T-04-04-03 | Denial of Service | setInterval job diario | accept | Si el job falla, `console.error` registra el error y continúa. No bloquea el servidor. El catch en `ejecutarSnapshotMetricas` es explícito. |
| T-04-04-04 | Tampering | Print mode — CSS @media print | accept | Print mode muestra datos ya cargados y validados desde el servidor. No hay bypass de autenticación; el dashboard ya está autenticado. |
</threat_model>

<verification>
```bash
# 1. Build frontend sin errores
cd C:\Users\ilesm\Documents\SIAST\apps\web && npm run build

# 2. TypeScript API sin errores
cd C:\Users\ilesm\Documents\SIAST && npx tsc --noEmit -p apps/api/tsconfig.json

# 3. DashboardPage importa MetricasOperacionalesSection
grep "MetricasOperacionalesSection" apps/web/src/pages/DashboardPage.jsx

# 4. Job diario en index.ts
grep "setInterval\|ejecutarSnapshotMetricas" apps/api/src/index.ts

# 5. UAT manual (con MySQL + servidor corriendo):
# - Loguear como ADMIN → DashboardPage debe mostrar sección "Métricas Operacionales" con 3 tabs
# - Loguear como RESPONSABLE_TI → solo tab "Por Responsable" visible
# - Loguear como TECNICO_TI → solo tab "Por Técnico" visible
# - Cambiar fechas en DateRangeFilter → datos se actualizan
# - Completar un ticket → métricas se refrescan sin recargar página (ticketsVersion)
```
</verification>

<success_criteria>
- DashboardPage muestra "Métricas Operacionales" con tabs apropiados por rol
- ADMIN: 3 tabs visibles, click en responsable navega a tab Por Responsable
- RESPONSABLE_*: solo tab Por Responsable con datos de su área filtrados
- TECNICO_*: solo tab Por Técnico con sus propios datos
- Cambiar filtro de fechas actualiza las gráficas
- ticketsVersion de useNotifStore dispara refetch automático
- CSS @media print oculta tabs y muestra todos los panels
- Job diario imprime en consola del servidor al arrancar y cada 24h
- npm run build de toda la app sin errores
</success_criteria>

<output>
Al completar, crear `.planning/phases/04-metricas-operacionales/04-04-SUMMARY.md`
</output>
