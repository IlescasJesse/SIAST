---
phase: 04-metricas-operacionales
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/database/prisma/schema.prisma
  - packages/shared/src/index.ts
autonomous: true
requirements:
  - MET-01
  - MET-02
  - MET-03
  - MET-04

must_haves:
  truths:
    - "La tabla metricas_historial existe en MySQL con columnas tipadas y constraint UNIQUE(fecha, areaSoporteId)"
    - "El tipo MetricasResponse existe en @stf/shared exportando los 3 shapes del endpoint unificado"
    - "Prisma client regenerado reconoce el modelo MetricasHistorial"
  artifacts:
    - path: "packages/database/prisma/schema.prisma"
      provides: "Modelo MetricasHistorial con columnas tipadas"
      contains: "model MetricasHistorial"
    - path: "packages/database/prisma/migrations/"
      provides: "Migración SQL add_metricas_historial"
      contains: "metricas_historial"
    - path: "packages/shared/src/index.ts"
      provides: "Tipos TypeScript para el endpoint unificado de métricas"
      contains: "MetricasGlobalResponse"
  key_links:
    - from: "packages/database/prisma/schema.prisma"
      to: "packages/database/prisma/migrations/"
      via: "npm run db:migrate en packages/database"
      pattern: "metricas_historial"
    - from: "packages/shared/src/index.ts"
      to: "apps/api/src/services/metricas.service.ts"
      via: "import type { MetricasGlobalResponse } from '@stf/shared'"
      pattern: "MetricasGlobalResponse"
---

<objective>
Agregar el modelo Prisma `MetricasHistorial` al schema y ejecutar la migración de base de datos.
Definir los tipos TypeScript en `@stf/shared` que describen las tres formas de respuesta del endpoint
unificado (`tipo=area`, `tipo=tecnico`, `tipo=proceso`).

Propósito: Todos los planes siguientes (backend y frontend) dependen de estos contratos de datos.
Plan 02 importa los tipos de `@stf/shared`. Plan 03/04 los usan para props y serialización.

Salida: `MetricasHistorial` en MySQL, migración confirmada, tipos exportados desde `@stf/shared`.
</objective>

<execution_context>
@C:\Users\ilesm\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\ilesm\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\ilesm\Documents\SIAST\.planning\PROJECT.md
@C:\Users\ilesm\Documents\SIAST\.planning\ROADMAP.md
@C:\Users\ilesm\Documents\SIAST\.planning\STATE.md
@C:\Users\ilesm\Documents\SIAST\.planning\phases\04-metricas-operacionales\04-CONTEXT.md
@C:\Users\ilesm\Documents\SIAST\.planning\phases\04-metricas-operacionales\04-RESEARCH.md

<interfaces>
<!-- Modelo AreaSoporte existente en schema.prisma — referencia para relaciones -->
```prisma
model AreaSoporte {
  id             Int      @id @default(autoincrement())
  nombre         String   @unique @db.VarChar(50)
  subcategorias  Json
  rolesIncluidos Json
  activo         Boolean  @default(true)
  createdAt      DateTime @default(now()) @map("created_at")
  usuarios       Usuario[]
  @@map("areas_soporte")
}
```

<!-- Modelo Ticket — campos clave para aggregation -->
```prisma
model Ticket {
  id              Int                @id @default(autoincrement())
  folio           String             @unique
  categoria       CategoriaTicket
  subcategoria    SubcategoriaTicket
  estado          EstadoTicket       @default(ABIERTO)
  tecnicoId       Int?
  tecnico         Usuario?           @relation("TecnicoAsignado", fields: [tecnicoId], references: [id])
  fechaAsignacion DateTime?
  fechaResolucion DateTime?
  activo          Boolean            @default(true)
  createdAt       DateTime           @default(now())
  historial       HistorialTicket[]
  @@map("tickets")
}
```

<!-- Patrón de modelo existente para convenciones de naming -->
```prisma
// Convenciones observadas:
// - @map("snake_case") en campos camelCase
// - @@map("nombre_tabla") en todos los modelos
// - @default(now()) @map("created_at") para timestamps
// - @@unique([campo1, campo2]) para constraints compuestos
// - @@index([campo]) para índices de query
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: Agregar modelo MetricasHistorial al schema Prisma y ejecutar migración</name>
  <files>
    packages/database/prisma/schema.prisma
  </files>
  <read_first>
    - packages/database/prisma/schema.prisma — leer completo para insertar el nuevo modelo al final, antes del cierre, respetando el estilo de naming (@@map snake_case, @map para campos, @@unique, @@index)
  </read_first>
  <action>
Al final de `packages/database/prisma/schema.prisma`, agregar el bloque:

```prisma
// ============================================================
// MÉTRICAS HISTORIAL (Phase 4 — daily snapshots)
// ============================================================

model MetricasHistorial {
  id                  Int       @id @default(autoincrement())
  fecha               DateTime  @db.Date
  areaSoporteId       Int?      @map("area_soporte_id")
  totalTickets        Int       @map("total_tickets")
  ticketsResueltos    Int       @map("tickets_resueltos")
  ticketsActivos      Int       @map("tickets_activos")
  slaGlobal           Float     @map("sla_global")
  tiempoPromedioHoras Float?    @map("tiempo_promedio_horas")
  extras              Json?
  createdAt           DateTime  @default(now()) @map("created_at")

  @@unique([fecha, areaSoporteId])
  @@index([fecha])
  @@map("metricas_historial")
}
```

NOTA: No agregar relación `areaSoporte AreaSoporte?` — `areaSoporteId` es nullable y se usa como
referencia suelta intencional (los snapshots se conservan aunque se elimine un área).

Luego ejecutar la migración desde `packages/database`:
```
cd packages/database
npm run db:migrate
```
Cuando Prisma pida nombre de migración, ingresar: `add_metricas_historial`

Después de la migración exitosa, regenerar el cliente:
```
npm run db:generate
```
  </action>
  <verify>
    <automated>
      cd C:\Users\ilesm\Documents\SIAST\packages\database && npx prisma validate
    </automated>
  </verify>
  <acceptance_criteria>
    - grep -c "model MetricasHistorial" packages/database/prisma/schema.prisma devuelve 1
    - grep -c "metricas_historial" packages/database/prisma/schema.prisma devuelve 1
    - grep -c "@@unique(\[fecha, areaSoporteId\])" packages/database/prisma/schema.prisma devuelve 1
    - Existe al menos un archivo en packages/database/prisma/migrations/ cuyo nombre contiene "add_metricas_historial"
    - npx prisma validate no muestra errores
    - npm run db:generate en packages/database termina sin error (cliente regenerado)
  </acceptance_criteria>
  <done>Modelo MetricasHistorial presente en schema.prisma, migración SQL aplicada a MySQL, cliente Prisma regenerado.</done>
</task>

<task type="auto">
  <name>Tarea 2: Definir tipos TypeScript del endpoint unificado en @stf/shared</name>
  <files>
    packages/shared/src/index.ts
  </files>
  <read_first>
    - packages/shared/src/index.ts — leer completo para encontrar el bloque de tipos de métricas existentes (MetricasSolicitudesResponse, MetricaTecnico, MetricaProceso) y el patrón de export; los tipos nuevos se añaden después de los existentes
  </read_first>
  <action>
Leer `packages/shared/src/index.ts`. Localizar los tipos existentes de métricas
(`MetricasSolicitudesResponse`, `MetricaTecnico`, `MetricaProceso`) — mantenerlos para
retrocompatibilidad (el controlador viejo aún los usa hasta que Plan 02 lo reemplace).

Agregar al final del archivo los nuevos tipos para el endpoint unificado:

```typescript
// ============================================================
// MÉTRICAS OPERACIONALES — Phase 4 (endpoint unificado)
// ============================================================

/** Rango de fechas para filtrado */
export interface MetricasDateRange {
  fechaInicio?: string; // YYYY-MM-DD
  fechaFin?: string;    // YYYY-MM-DD
}

/** Parámetros de consulta del endpoint unificado */
export interface MetricasQueryParams extends MetricasDateRange {
  tipo: "area" | "tecnico" | "proceso";
  areaId?: string;
  tecnicoId?: string;
}

/** Un punto de la tendencia diaria (para gráficas de línea) */
export interface TendenciaDia {
  dia: string;        // "YYYY-MM-DD"
  creados: number;
  resueltos: number;
}

/** Resumen de eficiencia de un responsable (para tabla Tab Global) */
export interface EficienciaResponsable {
  id: number;
  nombre: string;
  apellidos: string;
  areaNombre: string;
  areaSoporteId: number;
  ticketsResueltos: number;
  tiempoPromedioHoras: number | null;
  slaGlobal: number; // porcentaje 0-100
}

/** Rendimiento de un técnico (para tabla Tab Por Responsable) */
export interface RendimientoTecnico {
  id: number;
  nombre: string;
  apellidos: string;
  rol: string;
  ticketsActivos: number;
  ticketsCompletados: number;
  tiempoPromedioHoras: number | null;
  tiemprimeraRespuestaHoras: number | null;
  ratioResueltosCancelados: number | null; // resueltos / (resueltos + cancelados)
}

/** Distribución de tickets por categoría (para gráfica de pastel) */
export interface DistribucionCategoria {
  categoria: string;
  total: number;
  color?: string;
}

/** Respuesta del endpoint unificado cuando tipo=area (Tab Global) */
export interface MetricasGlobalResponse {
  tipo: "area";
  // KPIs tarjetas
  totalTickets: number;
  ticketsActivos: number;
  ticketsResueltos: number;
  slaGlobal: number; // %
  tiempoPromedioHoras: number | null;
  // Charts
  tendenciaDiaria: TendenciaDia[];
  distribucionCategoria: DistribucionCategoria[];
  comparativoPorArea: Array<{ areaNombre: string; areaSoporteId: number; total: number; resueltos: number }>;
  // Tabla
  eficienciaResponsables: EficienciaResponsable[];
}

/** Respuesta cuando tipo=tecnico (Tab Por Responsable) */
export interface MetricasPorAreaResponse {
  tipo: "tecnico";
  areaId: number;
  areaNombre: string;
  // KPIs tarjetas
  ticketsActivos: number;
  tiempoPromedioHoras: number | null;
  slaGlobal: number;
  ticketsReabiertos: number;
  // Charts
  tendenciaDiaria: TendenciaDia[];
  distribucionSubcategoria: DistribucionCategoria[];
  cargaTecnicos: Array<{ tecnicoNombre: string; tecnicoId: number; activos: number; completados: number }>;
  // Tabla
  rendimientoTecnicos: RendimientoTecnico[];
}

/** Respuesta cuando tipo=proceso (Tab Por Técnico) */
export interface MetricasPorTecnicoResponse {
  tipo: "proceso";
  tecnicoId: number;
  tecnicoNombre: string;
  // KPIs tarjetas
  ticketsCompletados: number;
  tiempoPromedioHoras: number | null;
  tiemprimeraRespuestaHoras: number | null;
  ratioResueltosCancelados: number | null;
  // Charts
  tendenciaProductividad: TendenciaDia[];
  comparativaVsArea: Array<{ label: string; tecnico: number; promedioArea: number }>;
  distribucionResultado: Array<{ name: string; value: number; color: string }>;
}

export type MetricasResponse =
  | MetricasGlobalResponse
  | MetricasPorAreaResponse
  | MetricasPorTecnicoResponse;
```
  </action>
  <verify>
    <automated>
      cd C:\Users\ilesm\Documents\SIAST && npx tsc --noEmit -p packages/shared/tsconfig.json
    </automated>
  </verify>
  <acceptance_criteria>
    - grep -c "MetricasGlobalResponse" packages/shared/src/index.ts devuelve 1
    - grep -c "MetricasPorAreaResponse" packages/shared/src/index.ts devuelve 1
    - grep -c "MetricasPorTecnicoResponse" packages/shared/src/index.ts devuelve 1
    - grep -c "MetricasResponse" packages/shared/src/index.ts devuelve al menos 1
    - grep -c "TendenciaDia" packages/shared/src/index.ts devuelve 1
    - Los tipos existentes MetricasSolicitudesResponse, MetricaTecnico, MetricaProceso siguen presentes (grep devuelve >=1 para cada uno)
    - npx tsc --noEmit en packages/shared no arroja errores de TypeScript
  </acceptance_criteria>
  <done>Tipos del endpoint unificado exportados desde @stf/shared. Tipos legados conservados. Build de @stf/shared sin errores.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema.prisma → MySQL | Prisma validate garantiza coherencia de tipos antes de migrar |
| @stf/shared → API y frontend | Los tipos son contratos — errores aquí propagan a todos los consumidores |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | schema.prisma @@unique | mitigate | Constraint UNIQUE([fecha, areaSoporteId]) previene duplicados en snapshots diarios; si se viola → error Prisma capturado en job diario |
| T-04-01-02 | Information Disclosure | @stf/shared tipos exportados | accept | Los tipos son interfaces TypeScript (solo en build time); no exponen datos de runtime ni secretos |
</threat_model>

<verification>
- `npx prisma validate` desde packages/database termina sin errores
- `npm run db:generate` produce cliente con `prisma.metricasHistorial` disponible
- `npx tsc --noEmit` en packages/shared termina sin errores
- Archivo de migración `*add_metricas_historial*` existe en packages/database/prisma/migrations/
- `MetricasGlobalResponse`, `MetricasPorAreaResponse`, `MetricasPorTecnicoResponse` y `MetricasResponse` exportados desde @stf/shared
</verification>

<success_criteria>
- Tabla `metricas_historial` existe en MySQL con columnas: id, fecha (DATE), area_soporte_id (nullable), total_tickets, tickets_resueltos, tickets_activos, sla_global, tiempo_promedio_horas, extras, created_at
- UNIQUE constraint en (fecha, area_soporte_id) activo en MySQL
- `@stf/shared` exporta los 6 tipos nuevos de Phase 4 sin romper los 3 tipos legacy
- Prisma client regenerado — `prisma.metricasHistorial` disponible para Plan 02
</success_criteria>

<output>
Al completar, crear `.planning/phases/04-metricas-operacionales/04-01-SUMMARY.md`
</output>
