---
phase: 03-roles-y-areas-de-soporte
plan: "03"
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - apps/api/src/middleware/roles.middleware.ts
  - apps/api/src/services/tickets.service.ts
  - apps/api/src/controllers/usuarios.controller.ts
  - apps/api/src/routes/tickets.routes.ts
  - apps/api/src/routes/admin.routes.ts
autonomous: true
requirements:
  - ROL-03
  - ROL-04

must_haves:
  truths:
    - "requireResponsableDeArea() existe en roles.middleware.ts y hace DB lookup de areaSoporteId"
    - "RESPONSABLE_* solo puede asignar tickets si el técnico pertenece a su AreaSoporte (asignarTicket guard)"
    - "RESPONSABLE_* solo puede cerrar/cancelar tickets si la subcategoría está en su AreaSoporte (cambiarEstado guard)"
    - "listarTickets retorna solo tickets de las subcategorias del area del RESPONSABLE_* (no todos los tickets)"
    - "TECNICO_ELECTRICISTA, TECNICO_PLOMERO, TECNICO_MOVILIDAD ven sus propios tickets asignados"
    - "CATEGORIA_ROL_MAP.SERVICIOS incluye los 3 nuevos tecnicos de mantenimiento"
    - "crearTicket genera pasos para tickets de categoria SERVICIOS (bug fix: no solo TECNOLOGIAS)"
    - "usuarios.controller.ts incluye areaSoporteId en userSelect, crear, y actualizar"
    - "GET /api/admin/areas-soporte retorna las 4 areas con status 200"
    - "Las rutas de tickets tienen requireRol actualizado para incluir RESPONSABLE_* y nuevos TECNICO_*"
  artifacts:
    - path: "apps/api/src/middleware/roles.middleware.ts"
      provides: "requireResponsableDeArea() — middleware async con DB read para scope de área"
      contains: "requireResponsableDeArea"
    - path: "apps/api/src/services/tickets.service.ts"
      provides: "Guards de área en asignarTicket + cambiarEstado + branch RESPONSABLE_* en listarTickets + bug fix crearTicket"
      contains: "ROLES_RESPONSABLE"
    - path: "apps/api/src/controllers/usuarios.controller.ts"
      provides: "areaSoporteId en userSelect + validación en crear + limpieza en actualizar"
      contains: "areaSoporteId"
    - path: "apps/api/src/routes/tickets.routes.ts"
      provides: "requireRol extendido con RESPONSABLE_* y nuevos TECNICO_*"
      contains: "RESPONSABLE_TI"
    - path: "apps/api/src/routes/admin.routes.ts"
      provides: "GET /areas-soporte endpoint para el frontend"
      contains: "areas-soporte"
  key_links:
    - from: "apps/api/src/routes/tickets.routes.ts"
      to: "apps/api/src/middleware/roles.middleware.ts"
      via: "requireRol() + requireResponsableDeArea() en cadena de middleware"
      pattern: "requireResponsableDeArea"
    - from: "apps/api/src/services/tickets.service.ts"
      to: "packages/database/prisma/areaSoporte"
      via: "prisma.areaSoporte.findUnique() para scope check"
      pattern: "prisma\\.areaSoporte\\.findUnique"
    - from: "apps/api/src/routes/admin.routes.ts"
      to: "GET /api/admin/areas-soporte"
      via: "handler que hace findMany({ where: { activo: true } })"
      pattern: "areas-soporte"
---

<objective>
Implementar los guards de area en el backend: `requireResponsableDeArea()` en el middleware, extensión de `asignarTicket` y `cambiarEstado` con scope check de AreaSoporte, branch RESPONSABLE_* en `listarTickets`, bug fix en `crearTicket` para SERVICIOS, actualización de `usuarios.controller.ts` para incluir `areaSoporteId`, y el endpoint `GET /api/admin/areas-soporte`.

Purpose: Habilitar los permisos específicos por área (D-07, D-08) y garantizar que RESPONSABLE_* solo opera sobre solicitudes de su AreaSoporte. Sin este plan, el frontend no puede cargar áreas ni crear usuarios RESPONSABLE_*.

Output:
- requireResponsableDeArea() disponible para rutas que necesiten scope check
- asignarTicket, cambiarEstado con guards de área
- listarTickets con branch RESPONSABLE_* filtrado por subcategorías
- crearTicket genera pasos para SERVICIOS (bug fix)
- usuarios.controller.ts con areaSoporteId en todos los handlers
- GET /api/admin/areas-soporte funcionando
</objective>

<execution_context>
@C:/Users/ilesm/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ilesm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-roles-y-areas-de-soporte/03-CONTEXT.md
@.planning/phases/03-roles-y-areas-de-soporte/03-RESEARCH.md
@.planning/phases/03-roles-y-areas-de-soporte/03-PATTERNS.md
@.planning/phases/03-roles-y-areas-de-soporte/03-01-SUMMARY.md
@.planning/phases/03-roles-y-areas-de-soporte/03-02-SUMMARY.md

<interfaces>
<!-- Interfaces clave que el executor necesita — extraídas de la investigación del codebase -->

requireRol actual en apps/api/src/middleware/roles.middleware.ts:
```typescript
import type { Response, NextFunction } from "express";
import type { Rol } from "@stf/shared";
import type { AuthRequest } from "../types/index.js";

export const requireRol =
  (...roles: Rol[]) =>
  (req: AuthRequest, res: Response, next: NextFunction): void => {
    if (!req.user || !roles.includes(req.user.rol)) {
      res.status(403).json({ error: "Sin permisos para esta acción" });
      return;
    }
    next();
  };
```

Patrón async middleware con DB read (de permisos.middleware.ts):
```typescript
import { prisma } from "../config/database.js";
// ...
export const requirePermiso =
  (perm: Permiso) =>
  async (req: AuthRequest, res: Response, next: NextFunction): Promise<void> => {
    if (!req.user) { res.status(401).json({ error: "No autenticado" }); return; }
    try {
      const usuario = await prisma.usuario.findUnique({
        where: { id: req.user.id },
        select: { permisos: true, activo: true },
      });
      // ... validaciones ...
      next();
    } catch (err) { next(err); }
  };
```

asignarTicket firma actual (verificada, línea 329 tickets.service.ts):
```typescript
export const asignarTicket = async (id: number, tecnicoId: number, user: JwtPayload)
```

CATEGORIA_ROL_MAP actual (verificada, línea 323):
```typescript
const CATEGORIA_ROL_MAP: Record<string, string[]> = {
  TECNOLOGIAS: ["TECNICO_TI", "TECNICO_REDES"],
  SERVICIOS: ["TECNICO_SERVICIOS"],
  RECURSOS_MATERIALES: ["GESTOR_RECURSOS_MATERIALES"],
};
```

cambiarEstado firma actual (verificada, línea 396):
```typescript
export const cambiarEstado = async (id: number, body: { estado: string; comentario?: string }, user: JwtPayload)
```

Error pattern (usado en tickets.service.ts):
```typescript
throw Object.assign(new Error("mensaje"), { status: 403 });
```

listarTickets branches actuales (verificadas, líneas 72-85):
```typescript
if (user.rol === "EMPLEADO") { where.empleadoRfc = user.rfc; }
else if (user.rol === "TECNICO_TI" || user.rol === "TECNICO_REDES" || user.rol === "TECNICO_SERVICIOS") {
  where.tecnicoId = user.id;
  where.estado = { notIn: ["RESUELTO", "CANCELADO"] };
} else if (user.rol === "GESTOR_RECURSOS_MATERIALES") { where.categoria = "RECURSOS_MATERIALES"; }
// ADMIN, MESA_AYUDA → sin filtro
```

crearTicket — condición actual con bug (verificada, línea ~247):
```typescript
if (categoriaVal === "TECNOLOGIAS") {
  // ... genera pasos ...
}
// SERVICIOS tickets nunca pasan aquí → bug
```

userSelect actual en usuarios.controller.ts (verificada, líneas 8-22):
```typescript
const userSelect = {
  id: true, nombre: true, apellidos: true, usuario: true,
  email: true, telefono: true, rol: true, activo: true,
  permisos: true, esEmpleadoEstructura: true, empleadoId: true, rfc: true, createdAt: true,
};
```

Patrón de ruta en admin.routes.ts (verificado, líneas 25-29):
```typescript
router.get("/usuarios", usuariosCtrl.listar);
router.post("/usuarios", usuariosCtrl.crear);
```

Singleton prisma import:
```typescript
import { prisma } from "../config/database.js";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: roles.middleware.ts — agregar requireResponsableDeArea() y actualizar tickets.routes.ts</name>
  <read_first>
    apps/api/src/middleware/roles.middleware.ts
    apps/api/src/routes/tickets.routes.ts
  </read_first>
  <files>
    apps/api/src/middleware/roles.middleware.ts
    apps/api/src/routes/tickets.routes.ts
  </files>
  <action>
**Archivo 1: apps/api/src/middleware/roles.middleware.ts**

Leer el archivo completo. Agregar import de `prisma` (si no existe) y la función `requireResponsableDeArea` después de `requireRol`:

```typescript
import { prisma } from "../config/database.js";

const ROLES_RESPONSABLE = [
  "RESPONSABLE_TI",
  "RESPONSABLE_REDES",
  "RESPONSABLE_MANTENIMIENTO",
  "RESPONSABLE_RECURSOS_MATERIALES",
] as const satisfies Rol[];

export { ROLES_RESPONSABLE };

export const requireResponsableDeArea =
  (getTicketSubcategoria: (req: AuthRequest) => Promise<string | null>) =>
  async (req: AuthRequest, res: Response, next: NextFunction): Promise<void> => {
    if (!req.user) {
      res.status(401).json({ error: "No autenticado" });
      return;
    }

    // ADMIN y MESA_AYUDA tienen scope global — bypass inmediato
    if (req.user.rol === "ADMIN" || req.user.rol === "MESA_AYUDA") {
      next();
      return;
    }

    if (!(ROLES_RESPONSABLE as readonly string[]).includes(req.user.rol)) {
      res.status(403).json({ error: "Sin permisos para esta acción" });
      return;
    }

    try {
      const usuarioDb = await prisma.usuario.findUnique({
        where: { id: req.user.id },
        select: { areaSoporteId: true, activo: true },
      });

      if (!usuarioDb?.activo || !usuarioDb.areaSoporteId) {
        res.status(403).json({ error: "Responsable sin área asignada" });
        return;
      }

      const areaSoporte = await prisma.areaSoporte.findUnique({
        where: { id: usuarioDb.areaSoporteId },
      });

      if (!areaSoporte) {
        res.status(403).json({ error: "Área de soporte no encontrada" });
        return;
      }

      const subcategoriaTicket = await getTicketSubcategoria(req);
      const subcategorias = areaSoporte.subcategorias as string[];

      if (subcategoriaTicket && !subcategorias.includes(subcategoriaTicket)) {
        res.status(403).json({ error: "Solicitud fuera del área de soporte asignada" });
        return;
      }

      // Adjuntar areaSoporte al request para uso downstream (service layer)
      (req as any).areaSoporte = areaSoporte;
      next();
    } catch (err) {
      next(err);
    }
  };
```

Si `satisfies` causa error de compilación en la versión de TypeScript del proyecto, reemplazar `as const satisfies Rol[]` por simplemente `as const`.

**Archivo 2: apps/api/src/routes/tickets.routes.ts**

Leer el archivo completo. Actualizar los guardias de requireRol en cada ruta:

```typescript
// /:id/asignar — RESPONSABLE_* pueden asignar (scope check en service layer)
router.patch(
  "/:id/asignar",
  requireRol(
    "ADMIN",
    "RESPONSABLE_TI", "RESPONSABLE_REDES", "RESPONSABLE_MANTENIMIENTO", "RESPONSABLE_RECURSOS_MATERIALES",
  ),
  ctrl.asignar,
);

// /:id/estado — RESPONSABLE_* pueden cerrar/cancelar, nuevos TECNICO_* pueden cambiar estado
router.patch(
  "/:id/estado",
  requireRol(
    "ADMIN",
    "TECNICO_TI", "TECNICO_REDES", "TECNICO_SERVICIOS",
    "TECNICO_ELECTRICISTA", "TECNICO_PLOMERO", "TECNICO_MOVILIDAD",
    "RESPONSABLE_TI", "RESPONSABLE_REDES", "RESPONSABLE_MANTENIMIENTO", "RESPONSABLE_RECURSOS_MATERIALES",
    "EMPLEADO",
  ),
  ctrl.cambiarEstado,
);

// /:id/comentarios — nuevos TECNICO_* y RESPONSABLE_* pueden comentar
router.post(
  "/:id/comentarios",
  requireRol(
    "ADMIN", "MESA_AYUDA",
    "TECNICO_TI", "TECNICO_REDES", "TECNICO_SERVICIOS",
    "TECNICO_ELECTRICISTA", "TECNICO_PLOMERO", "TECNICO_MOVILIDAD",
    "RESPONSABLE_TI", "RESPONSABLE_REDES", "RESPONSABLE_MANTENIMIENTO", "RESPONSABLE_RECURSOS_MATERIALES",
  ),
  ctrl.comentar,
);

// /:id/pasos/:pasoId/completar — nuevos TECNICO_* completan sus propios pasos
router.patch(
  "/:id/pasos/:pasoId/completar",
  requireRol(
    "TECNICO_TI", "TECNICO_REDES", "TECNICO_SERVICIOS",
    "TECNICO_ELECTRICISTA", "TECNICO_PLOMERO", "TECNICO_MOVILIDAD",
  ),
  ctrl.completarPaso,
);

// /:id/pasos/:pasoId/asignar — RESPONSABLE_* pueden asignar pasos de su área
router.patch(
  "/:id/pasos/:pasoId/asignar",
  requireRol(
    "ADMIN", "MESA_AYUDA",
    "RESPONSABLE_TI", "RESPONSABLE_REDES", "RESPONSABLE_MANTENIMIENTO", "RESPONSABLE_RECURSOS_MATERIALES",
  ),
  ctrl.asignarPaso,
);
```

NO agregar `requireResponsableDeArea()` como middleware de ruta — la validación de scope se hace dentro del service layer (tickets.service.ts) para mantener el código más simple y consistente con el patrón existente.
  </action>
  <verify>
    <automated>
      cd "C:/Users/ilesm/Documents/SIAST" && npm run build --workspace=apps/api 2>&1 | grep -E "error|Error|warning" | head -20
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep "requireResponsableDeArea" apps/api/src/middleware/roles.middleware.ts` encuentra la función exportada
    - `grep "ROLES_RESPONSABLE" apps/api/src/middleware/roles.middleware.ts` encuentra el array exportado
    - `grep "prisma" apps/api/src/middleware/roles.middleware.ts` encuentra el import del singleton
    - `grep "RESPONSABLE_TI" apps/api/src/routes/tickets.routes.ts` encuentra al menos 3 ocurrencias (en rutas asignar, estado, comentarios, pasos/asignar)
    - `grep "TECNICO_ELECTRICISTA" apps/api/src/routes/tickets.routes.ts` encuentra al menos 2 ocurrencias
    - Build de apps/api sin errores de TypeScript relacionados con Rol o requireRol
  </acceptance_criteria>
  <done>requireResponsableDeArea() disponible en middleware. Rutas de tickets con requireRol completo para todos los roles nuevos.</done>
</task>

<task type="auto">
  <name>Task 2: tickets.service.ts — guards de área, bug fix crearTicket, branch listarTickets para RESPONSABLE_*</name>
  <read_first>
    apps/api/src/services/tickets.service.ts
  </read_first>
  <files>apps/api/src/services/tickets.service.ts</files>
  <action>
Leer el archivo completo para ubicar exactamente las líneas de cada función a modificar. Hay 4 cambios independientes en este archivo:

**Cambio 1 — Actualizar CATEGORIA_ROL_MAP** (línea ~323):
Reemplazar la entrada SERVICIOS para incluir los 3 nuevos roles (mantener TECNICO_SERVICIOS para compatibilidad):
```typescript
const CATEGORIA_ROL_MAP: Record<string, string[]> = {
  TECNOLOGIAS: ["TECNICO_TI", "TECNICO_REDES"],
  SERVICIOS: ["TECNICO_ELECTRICISTA", "TECNICO_PLOMERO", "TECNICO_MOVILIDAD", "TECNICO_SERVICIOS"],
  RECURSOS_MATERIALES: ["GESTOR_RECURSOS_MATERIALES"],
};
```

**Cambio 2 — asignarTicket: agregar guard de scope para RESPONSABLE_*** (después del check existente de rolesPermitidos, antes del update):
```typescript
// Guard: si el usuario es RESPONSABLE_*, verificar que el técnico pertenece a su área
const ROLES_RESPONSABLE = [
  "RESPONSABLE_TI", "RESPONSABLE_REDES",
  "RESPONSABLE_MANTENIMIENTO", "RESPONSABLE_RECURSOS_MATERIALES",
];
if (ROLES_RESPONSABLE.includes(user.rol)) {
  const usuarioDb = await prisma.usuario.findUnique({
    where: { id: user.id },
    select: { areaSoporteId: true },
  });
  const areaSoporte = usuarioDb?.areaSoporteId
    ? await prisma.areaSoporte.findUnique({ where: { id: usuarioDb.areaSoporteId } })
    : null;
  if (!areaSoporte) {
    throw Object.assign(new Error("Responsable sin área asignada"), { status: 403 });
  }
  const rolesArea = areaSoporte.rolesIncluidos as string[];
  if (!rolesArea.includes(tecnico.rol)) {
    throw Object.assign(
      new Error("El técnico no pertenece al área de soporte del responsable"),
      { status: 403 },
    );
  }
}
```

**Cambio 3 — cambiarEstado: agregar scope check para RESPONSABLE_*** (después de verificar que el ticket existe y antes de verificar la transición de estado):
```typescript
// Guard: si el usuario es RESPONSABLE_*, verificar que el ticket pertenece a su área
if (ROLES_RESPONSABLE.includes(user.rol)) {
  const usuarioDb = await prisma.usuario.findUnique({
    where: { id: user.id },
    select: { areaSoporteId: true },
  });
  const areaSoporte = usuarioDb?.areaSoporteId
    ? await prisma.areaSoporte.findUnique({ where: { id: usuarioDb.areaSoporteId } })
    : null;
  const subcategorias = (areaSoporte?.subcategorias as string[]) ?? [];
  if (!subcategorias.includes(ticket.subcategoria)) {
    throw Object.assign(
      new Error("Solicitud fuera del área de soporte asignada"),
      { status: 403 },
    );
  }
}
```

Definir `ROLES_RESPONSABLE` como constante de módulo al inicio del archivo (junto a CATEGORIA_ROL_MAP) para evitar duplicación:
```typescript
const ROLES_RESPONSABLE = [
  "RESPONSABLE_TI", "RESPONSABLE_REDES",
  "RESPONSABLE_MANTENIMIENTO", "RESPONSABLE_RECURSOS_MATERIALES",
] as const;
```

**Cambio 4 — listarTickets: agregar dos branches nuevos** en el bloque if/else existente (líneas ~72-85):
```typescript
// Branch 1: nuevos TECNICO_* ven sus propios tickets activos (igual que TECNICO_TI/REDES)
else if (
  user.rol === "TECNICO_ELECTRICISTA" ||
  user.rol === "TECNICO_PLOMERO" ||
  user.rol === "TECNICO_MOVILIDAD"
) {
  where.tecnicoId = user.id;
  where.estado = { notIn: ["RESUELTO", "CANCELADO"] };
}
// Branch 2: RESPONSABLE_* ven todos los tickets de las subcategorías de su área
else if (ROLES_RESPONSABLE.includes(user.rol as (typeof ROLES_RESPONSABLE)[number])) {
  const usuarioDb = await prisma.usuario.findUnique({
    where: { id: user.id },
    select: { areaSoporteId: true },
  });
  if (usuarioDb?.areaSoporteId) {
    const areaSoporte = await prisma.areaSoporte.findUnique({
      where: { id: usuarioDb.areaSoporteId },
    });
    if (areaSoporte) {
      where.subcategoria = { in: areaSoporte.subcategorias as string[] };
    }
  }
}
// ADMIN, MESA_AYUDA → sin filtro (fallthrough existente)
```

IMPORTANTE: El branch RESPONSABLE_* debe ir ANTES del fallthrough implícito (ADMIN/MESA_AYUDA no tienen branch explícito — si RESPONSABLE_* no se captura antes, verán todos los tickets).

**Cambio 5 — crearTicket: bug fix — extender condición a SERVICIOS** (línea ~247):
```typescript
// ANTES (bug):
if (categoriaVal === "TECNOLOGIAS") {

// DESPUÉS (fix):
if (["TECNOLOGIAS", "SERVICIOS"].includes(categoriaVal)) {
```

Este es el único cambio en la línea ~247 de crearTicket — el bloque interno no cambia.

Notas sobre `listarTickets` — la función debe ser `async` para usar `await prisma.usuario.findUnique`. Verificar si ya es async o si hay que agregar `async`.
  </action>
  <verify>
    <automated>
      cd "C:/Users/ilesm/Documents/SIAST" && npm run build --workspace=apps/api 2>&1 | grep -E "^.*error TS" | head -20
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep "TECNICO_ELECTRICISTA.*TECNICO_PLOMERO.*TECNICO_MOVILIDAD.*TECNICO_SERVICIOS" apps/api/src/services/tickets.service.ts` encuentra CATEGORIA_ROL_MAP actualizado
    - `grep "ROLES_RESPONSABLE" apps/api/src/services/tickets.service.ts` encuentra al menos 3 ocurrencias (definición + asignarTicket + cambiarEstado)
    - `grep -n "TECNOLOGIAS.*SERVICIOS\|SERVICIOS.*TECNOLOGIAS" apps/api/src/services/tickets.service.ts` encuentra el includes() en crearTicket
    - `grep "where.subcategoria" apps/api/src/services/tickets.service.ts` encuentra el filtro de área en listarTickets
    - Build de apps/api sin errores de TypeScript (verificado en Task 1, re-verificar si hubo cambios)
  </acceptance_criteria>
  <done>tickets.service.ts con guards de área en asignarTicket y cambiarEstado, branch RESPONSABLE_* en listarTickets, bug fix crearTicket para SERVICIOS, CATEGORIA_ROL_MAP actualizado con nuevos TECNICO_* de MANTENIMIENTO.</done>
</task>

<task type="auto">
  <name>Task 3: usuarios.controller.ts — areaSoporteId + admin.routes.ts — GET /areas-soporte</name>
  <read_first>
    apps/api/src/controllers/usuarios.controller.ts
    apps/api/src/routes/admin.routes.ts
  </read_first>
  <files>
    apps/api/src/controllers/usuarios.controller.ts
    apps/api/src/routes/admin.routes.ts
  </files>
  <action>
**Archivo 1: apps/api/src/controllers/usuarios.controller.ts**

Leer el archivo completo. Realizar 3 cambios:

**Cambio 1 — Extender userSelect** para incluir areaSoporteId y relación anidada de nombre:
```typescript
const userSelect = {
  id: true, nombre: true, apellidos: true, usuario: true,
  email: true, telefono: true, rol: true, activo: true,
  permisos: true, esEmpleadoEstructura: true, empleadoId: true, rfc: true, createdAt: true,
  areaSoporteId: true,
  areaSoporte: { select: { nombre: true } },
};
```

**Cambio 2 — Extender handler `crear`** para:
- Incluir `areaSoporteId` en la desestructuración de req.body
- Agregar validación: si rol empieza con "RESPONSABLE_", areaSoporteId es obligatorio
- Incluir areaSoporteId en prisma.usuario.create

En la desestructuración (buscar la línea con `const { password, esEmpleadoEstructura, ...`):
```typescript
const { password, esEmpleadoEstructura, empleadoId, rfc, permisos, areaSoporteId, ...rest } = req.body as {
  // ... tipos existentes ...
  areaSoporteId?: number;
};

// Agregar validación después de la desestructuración:
if ((rest.rol as string)?.startsWith("RESPONSABLE_") && !areaSoporteId) {
  res.status(400).json({ error: "El campo areaSoporteId es obligatorio para roles RESPONSABLE_*" });
  return;
}
```

En prisma.usuario.create data, agregar: `areaSoporteId: areaSoporteId ?? null,`

**Cambio 3 — Extender handler `actualizar`** para:
- Incluir `areaSoporteId` en la desestructuración de req.body
- Limpiar areaSoporteId cuando el rol cambia a un rol no-RESPONSABLE_*

En la desestructuración del handler actualizar:
```typescript
const { password, esEmpleadoEstructura, empleadoId, rfc, permisos, areaSoporteId, ...rest } = req.body;
const data: Record<string, unknown> = { ...rest };

// Si el rol cambia a un rol no-RESPONSABLE_*, limpiar areaSoporteId
if (rest.rol && !(rest.rol as string).startsWith("RESPONSABLE_")) {
  data.areaSoporteId = null;
} else if (areaSoporteId !== undefined) {
  data.areaSoporteId = areaSoporteId;
}
```

Si el handler actualizar usa `...rest` como data directamente (no un objeto separado), agregar la lógica de limpieza antes del prisma.usuario.update.

---

**Archivo 2: apps/api/src/routes/admin.routes.ts**

Leer el archivo completo. Agregar el endpoint GET /areas-soporte al router. El handler puede definirse inline en el archivo de rutas o en admin.controller.ts — verificar cuál es el patrón usado en el archivo actual.

Si el archivo tiene un admin controller, agregar el handler ahí. Si las rutas usan handlers de otros controllers, agregar el handler de áreas de soporte inline o en un nuevo archivo `areas-soporte.controller.ts`.

El handler es simple (3 líneas de lógica):
```typescript
// Handler para listar áreas de soporte
export const listarAreasSoporte = async (_req: Request, res: Response, next: NextFunction) => {
  try {
    const data = await prisma.areaSoporte.findMany({
      where: { activo: true },
      orderBy: { nombre: "asc" },
    });
    res.json({ data });
  } catch (err) {
    next(err);
  }
};
```

Agregar la ruta al router de admin (ya está protegido por requireRol("ADMIN") a nivel de router):
```typescript
// ── Áreas de Soporte ──────────────────────────────────────────────────────────
router.get("/areas-soporte", listarAreasSoporte);
```

Si el router de admin tiene un auth middleware a nivel de router (verificar con `router.use(...)`), el endpoint heredará esa protección. Si no, agregar `requireRol("ADMIN")` en la ruta específica.

Importar `prisma` en el archivo donde esté el handler, usando el singleton: `import { prisma } from "../config/database.js";`
  </action>
  <verify>
    <automated>
      cd "C:/Users/ilesm/Documents/SIAST" && npm run build --workspace=apps/api 2>&1 | grep -c "error TS"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep "areaSoporteId: true" apps/api/src/controllers/usuarios.controller.ts` encuentra el campo en userSelect
    - `grep "areaSoporte.*select.*nombre" apps/api/src/controllers/usuarios.controller.ts` encuentra la relación anidada
    - `grep "startsWith.*RESPONSABLE_" apps/api/src/controllers/usuarios.controller.ts` encuentra la validación en crear
    - `grep "areas-soporte" apps/api/src/routes/admin.routes.ts` encuentra la ruta GET
    - `grep "listarAreasSoporte\|areaSoporte.findMany" apps/api/src/routes/admin.routes.ts` o en el controller respectivo
    - Build sin errores TypeScript (grep -c "error TS" devuelve 0)
  </acceptance_criteria>
  <done>usuarios.controller.ts con areaSoporteId en userSelect, crear (con validación RESPONSABLE_*), y actualizar (con limpieza cuando rol cambia). Endpoint GET /api/admin/areas-soporte disponible y protegido.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request → requireRol() | Primera capa: verifica que el rol del JWT es permitido para la ruta |
| requireRol() → service layer guard | Segunda capa: verifica que el ticket/técnico está en el área del RESPONSABLE_* |
| areaSoporteId en JWT vs DB | areaSoporteId NO viene en el JWT — siempre se lee de DB para evitar tokens forjados |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Elevation of Privilege | asignarTicket — RESPONSABLE_* accede a tickets de otra área | mitigate | Guard en service: `areaSoporte.rolesIncluidos.includes(tecnico.rol)` — si el técnico no pertenece al área del responsable, 403 |
| T-03-03-02 | Elevation of Privilege | cambiarEstado — RESPONSABLE_* cancela ticket de otra área | mitigate | Guard en service: `areaSoporte.subcategorias.includes(ticket.subcategoria)` — si la subcategoría no está en el área, 403 |
| T-03-03-03 | Elevation of Privilege | listarTickets — RESPONSABLE_* ve todos los tickets (fallthrough) | mitigate | Branch explícito `else if (ROLES_RESPONSABLE.includes(user.rol))` debe aparecer ANTES del fallthrough implícito de ADMIN/MESA_AYUDA |
| T-03-03-04 | Tampering | areaSoporteId enviado por cliente en creación de usuario con rol no-RESPONSABLE_* | mitigate | En actualizar handler: `if (!rol.startsWith("RESPONSABLE_")) data.areaSoporteId = null` — limpieza activa del campo |
| T-03-03-05 | Information Disclosure | GET /api/admin/areas-soporte sin autenticación | mitigate | Ruta bajo router de admin que ya tiene `requireRol("ADMIN")` a nivel de router o agregar `requireRol("ADMIN")` en la ruta específica |
| T-03-03-06 | Spoofing | areaSoporteId forjado en JWT | mitigate | requireResponsableDeArea() siempre lee areaSoporteId del DB (prisma.usuario.findUnique), nunca del JWT payload |
</threat_model>

<verification>
```bash
# 1. Build limpio
cd "C:/Users/ilesm/Documents/SIAST" && npm run build --workspace=apps/api 2>&1 | grep "error TS"

# 2. requireResponsableDeArea exportada
grep "export.*requireResponsableDeArea" apps/api/src/middleware/roles.middleware.ts

# 3. Bug fix crearTicket
grep "SERVICIOS.*TECNOLOGIAS\|TECNOLOGIAS.*SERVICIOS" apps/api/src/services/tickets.service.ts

# 4. RESPONSABLE_* filtro en listarTickets
grep "where.subcategoria" apps/api/src/services/tickets.service.ts

# 5. areaSoporteId en userSelect
grep "areaSoporteId: true" apps/api/src/controllers/usuarios.controller.ts

# 6. Endpoint áreas de soporte
grep "areas-soporte" apps/api/src/routes/admin.routes.ts

# 7. Test manual del endpoint (requiere API corriendo)
# curl -H "Authorization: Bearer {admin_token}" http://localhost:5101/api/admin/areas-soporte
```
</verification>

<success_criteria>
- Build de apps/api sin errores de TypeScript
- requireResponsableDeArea() exportada en roles.middleware.ts con DB read
- CATEGORIA_ROL_MAP.SERVICIOS incluye TECNICO_ELECTRICISTA, TECNICO_PLOMERO, TECNICO_MOVILIDAD, TECNICO_SERVICIOS
- asignarTicket rechaza RESPONSABLE_* que intenta asignar técnico fuera de su área (403)
- cambiarEstado rechaza RESPONSABLE_* que intenta cerrar ticket de otra área (403)
- listarTickets retorna tickets filtrados por subcategorías del área para RESPONSABLE_*
- crearTicket genera pasos para tickets de categoría SERVICIOS
- GET /api/admin/areas-soporte devuelve 200 con las 4 áreas
- usuarios.controller.ts crea/actualiza areaSoporteId correctamente
</success_criteria>

<output>
After completion, create `.planning/phases/03-roles-y-areas-de-soporte/03-03-SUMMARY.md`
</output>
