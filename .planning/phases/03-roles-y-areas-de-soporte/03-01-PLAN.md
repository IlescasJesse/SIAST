---
phase: 03-roles-y-areas-de-soporte
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/database/prisma/schema.prisma
  - packages/shared/src/index.ts
autonomous: true
requirements:
  - ROL-01
  - ROL-02

must_haves:
  truths:
    - "El enum Rol en schema.prisma tiene 14 valores (7 existentes + 7 nuevos)"
    - "TECNICO_SERVICIOS permanece en el enum (no se elimina)"
    - "El modelo AreaSoporte existe en schema.prisma con campos subcategorias Json y rolesIncluidos Json"
    - "Usuario tiene campo areaSoporteId Int? opcional con relación a AreaSoporte"
    - "RolSchema en shared/index.ts tiene los 14 valores del enum"
    - "PERMISOS_DEFAULT cubre los 7 roles nuevos (build de TypeScript no falla)"
    - "LABEL_ROL tiene etiquetas para los 7 roles nuevos"
  artifacts:
    - path: "packages/database/prisma/schema.prisma"
      provides: "Enum Rol extendido + modelo AreaSoporte + FK areaSoporteId en Usuario"
      contains: "RESPONSABLE_TI"
    - path: "packages/shared/src/index.ts"
      provides: "RolSchema extendido + PERMISOS_DEFAULT completo + LABEL_ROL actualizado"
      contains: "RESPONSABLE_TI"
  key_links:
    - from: "packages/database/prisma/schema.prisma"
      to: "packages/shared/src/index.ts"
      via: "enum Rol — ambos deben tener los mismos 14 valores"
      pattern: "RESPONSABLE_TI.*RESPONSABLE_REDES.*RESPONSABLE_MANTENIMIENTO"
    - from: "packages/database/prisma/schema.prisma"
      to: "model AreaSoporte"
      via: "model Usuario campo areaSoporteId → @relation AreaSoporte"
      pattern: "areaSoporteId.*Int\\?"
---

<objective>
Extender el enum `Rol` de Prisma con 7 nuevos valores y crear el modelo `AreaSoporte`. Actualizar el espejo Zod en `@stf/shared` incluyendo `PERMISOS_DEFAULT` completo para evitar error de TypeScript en build.

Purpose: Establecer los contratos de tipos que todos los planes posteriores consumen. Sin este plan, la migración (Plan 02) y el backend (Plan 03) no pueden compilar.

Output:
- `schema.prisma` con enum Rol extendido (14 valores), modelo AreaSoporte, campo areaSoporteId en Usuario
- `shared/index.ts` con RolSchema, PERMISOS_DEFAULT, LABEL_ROL actualizados
</objective>

<execution_context>
@C:/Users/ilesm/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ilesm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-roles-y-areas-de-soporte/03-CONTEXT.md
@.planning/phases/03-roles-y-areas-de-soporte/03-RESEARCH.md
@.planning/phases/03-roles-y-areas-de-soporte/03-PATTERNS.md

<interfaces>
<!-- Estado actual del enum Rol en schema.prisma — el executor debe extender esto exactamente -->
Enum actual en packages/database/prisma/schema.prisma:
```prisma
enum Rol {
  ADMIN
  TECNICO_TI
  TECNICO_REDES
  TECNICO_SERVICIOS
  MESA_AYUDA
  GESTOR_RECURSOS_MATERIALES
  EMPLEADO
}
```

Modelo AreaEdificio como referencia de estructura (seguir mismas convenciones):
```prisma
model AreaEdificio {
  id    String       @id @db.VarChar(100)
  label String       @db.VarChar(200)
  piso  PisoEdificio
  floor Int
  activo Boolean @default(true)
  empleados Empleado[]
  tickets   Ticket[]
  @@index([piso])
  @@map("areas_edificio")
}
```

FK opcional en Empleado como referencia de patrón:
```prisma
areaId         String       @db.VarChar(100)
area           AreaEdificio @relation(fields: [areaId], references: [id])
```

RolSchema actual en packages/shared/src/index.ts (líneas 7-15):
```typescript
export const RolSchema = z.enum([
  "ADMIN",
  "TECNICO_TI",
  "TECNICO_REDES",
  "TECNICO_SERVICIOS",
  "MESA_AYUDA",
  "GESTOR_RECURSOS_MATERIALES",
  "EMPLEADO",
]);
```

PERMISOS_DEFAULT actual en shared/index.ts (líneas ~530-544):
```typescript
export const PERMISOS_DEFAULT: Record<Rol, Permiso[]> = {
  ADMIN: [...PERMISOS_LIST],
  MESA_AYUDA: [
    "solicitudes.ver_todas",
    "solicitudes.crear_empleado",
    "solicitudes.asignar",
    "pasos.asignar",
    "metricas.ver",
  ],
  TECNICO_TI: ["solicitudes.ver_todas", "metricas.ver"],
  TECNICO_REDES: ["solicitudes.ver_todas"],
  TECNICO_SERVICIOS: ["solicitudes.ver_todas"],
  GESTOR_RECURSOS_MATERIALES: ["solicitudes.ver_todas", "recursos.gestionar", "metricas.ver"],
  EMPLEADO: [],
};
```

LABEL_ROL actual en shared/index.ts (líneas ~309-317):
```typescript
export const LABEL_ROL: Record<string, string> = {
  ADMIN: "Administrador",
  TECNICO_TI: "Técnico TI",
  TECNICO_REDES: "Técnico de Redes",
  TECNICO_SERVICIOS: "Técnico de Servicios",
  MESA_AYUDA: "Mesa de Ayuda",
  GESTOR_RECURSOS_MATERIALES: "Gestor de Recursos Materiales",
  EMPLEADO: "Empleado",
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extender schema.prisma — enum Rol, modelo AreaSoporte, FK en Usuario</name>
  <read_first>
    packages/database/prisma/schema.prisma
  </read_first>
  <files>packages/database/prisma/schema.prisma</files>
  <action>
Leer el archivo completo primero. Realizar tres cambios en schema.prisma:

**Cambio 1 — Extender enum Rol** (agregar 7 valores después de EMPLEADO):
```prisma
enum Rol {
  ADMIN
  TECNICO_TI
  TECNICO_REDES
  TECNICO_SERVICIOS       // deprecated en Phase 3 — mantener para filas existentes
  MESA_AYUDA
  GESTOR_RECURSOS_MATERIALES
  EMPLEADO
  // ── Phase 3: nuevos roles ──
  RESPONSABLE_TI
  RESPONSABLE_REDES
  RESPONSABLE_MANTENIMIENTO
  RESPONSABLE_RECURSOS_MATERIALES
  TECNICO_ELECTRICISTA
  TECNICO_PLOMERO
  TECNICO_MOVILIDAD
}
```

**Cambio 2 — Agregar campo areaSoporteId al modelo Usuario** (después del campo `rfc` existente):
```prisma
areaSoporteId  Int?         @map("area_soporte_id")
areaSoporte    AreaSoporte? @relation(fields: [areaSoporteId], references: [id])
```

**Cambio 3 — Agregar modelo AreaSoporte** (después del último modelo existente, antes del último `//` si lo hay):
```prisma
model AreaSoporte {
  id             Int      @id @default(autoincrement())
  nombre         String   @unique @db.VarChar(50)
  subcategorias  Json
  rolesIncluidos Json
  activo         Boolean  @default(true)
  createdAt      DateTime @default(now()) @map("created_at")
  usuarios       Usuario[]

  @@map("areas_soporte")
}
```

Notas críticas:
- TECNICO_SERVICIOS DEBE PERMANECER en el enum — no eliminarlo
- El campo areaSoporteId en Usuario es `Int?` (opcional, no obligatorio a nivel de schema)
- El modelo AreaSoporte usa `Json` para subcategorias y rolesIncluidos — mismo patrón que `Usuario.permisos Json?`
- La relación Usuario ↔ AreaSoporte es bidireccional: `usuarios Usuario[]` en AreaSoporte y la FK en Usuario
  </action>
  <verify>
    <automated>
      cd "C:/Users/ilesm/Documents/SIAST/packages/database" && npx prisma validate
    </automated>
  </verify>
  <acceptance_criteria>
    - `npx prisma validate` termina sin errores
    - `grep -c "RESPONSABLE_TI" packages/database/prisma/schema.prisma` devuelve al menos 1
    - `grep "TECNICO_SERVICIOS" packages/database/prisma/schema.prisma` incluye el valor en el enum (no eliminado)
    - `grep "model AreaSoporte" packages/database/prisma/schema.prisma` encuentra la línea
    - `grep "areaSoporteId" packages/database/prisma/schema.prisma` encuentra el campo en Usuario
    - `grep "subcategorias.*Json" packages/database/prisma/schema.prisma` encuentra el campo
    - `grep "areas_soporte" packages/database/prisma/schema.prisma` encuentra el @@map
  </acceptance_criteria>
  <done>schema.prisma válido con 14 valores en Rol, modelo AreaSoporte, y FK areaSoporteId opcional en Usuario</done>
</task>

<task type="auto">
  <name>Task 2: Actualizar packages/shared/src/index.ts — RolSchema, PERMISOS_DEFAULT, LABEL_ROL</name>
  <read_first>
    packages/shared/src/index.ts
  </read_first>
  <files>packages/shared/src/index.ts</files>
  <action>
Leer el archivo completo primero para ubicar las líneas exactas de RolSchema, LABEL_ROL y PERMISOS_DEFAULT.

**Cambio 1 — Extender RolSchema** (el z.enum debe tener los 14 valores):
```typescript
export const RolSchema = z.enum([
  "ADMIN",
  "TECNICO_TI",
  "TECNICO_REDES",
  "TECNICO_SERVICIOS",
  "MESA_AYUDA",
  "GESTOR_RECURSOS_MATERIALES",
  "EMPLEADO",
  // Phase 3: 7 nuevos roles
  "RESPONSABLE_TI",
  "RESPONSABLE_REDES",
  "RESPONSABLE_MANTENIMIENTO",
  "RESPONSABLE_RECURSOS_MATERIALES",
  "TECNICO_ELECTRICISTA",
  "TECNICO_PLOMERO",
  "TECNICO_MOVILIDAD",
]);
```

**Cambio 2 — Extender LABEL_ROL** (agregar etiquetas para los 7 nuevos roles; LABEL_ROL es `Record<string, string>` por lo que no rompe TypeScript si faltan, pero agregar por consistencia de UI):
```typescript
// Agregar al objeto LABEL_ROL:
RESPONSABLE_TI: "Responsable de TI",
RESPONSABLE_REDES: "Responsable de Redes",
RESPONSABLE_MANTENIMIENTO: "Responsable de Mantenimiento",
RESPONSABLE_RECURSOS_MATERIALES: "Responsable de Recursos Materiales",
TECNICO_ELECTRICISTA: "Técnico Electricista",
TECNICO_PLOMERO: "Técnico Plomero",
TECNICO_MOVILIDAD: "Técnico de Movilidad",
```

**Cambio 3 — CRÍTICO: Extender PERMISOS_DEFAULT** (PERMISOS_DEFAULT es `Record<Rol, Permiso[]>` — TypeScript error si falta cualquier Rol):
```typescript
// Agregar estas 7 entradas al objeto PERMISOS_DEFAULT:
RESPONSABLE_TI: ["solicitudes.ver_todas", "solicitudes.asignar", "pasos.asignar", "metricas.ver"],
RESPONSABLE_REDES: ["solicitudes.ver_todas", "solicitudes.asignar", "pasos.asignar", "metricas.ver"],
RESPONSABLE_MANTENIMIENTO: ["solicitudes.ver_todas", "solicitudes.asignar", "pasos.asignar", "metricas.ver"],
RESPONSABLE_RECURSOS_MATERIALES: ["solicitudes.ver_todas", "solicitudes.asignar", "pasos.asignar", "metricas.ver"],
TECNICO_ELECTRICISTA: ["solicitudes.ver_todas"],
TECNICO_PLOMERO: ["solicitudes.ver_todas"],
TECNICO_MOVILIDAD: ["solicitudes.ver_todas"],
```

Notas:
- TECNICO_SERVICIOS ya existe en PERMISOS_DEFAULT — NO eliminarlo
- Los permisos de RESPONSABLE_* son intencionalmente amplios a nivel default; la restricción de scope se aplica en el backend vía `requireResponsableDeArea()`
- Los valores de Permiso deben ser strings del tipo `Permiso` ya definido en este archivo — verificar que "solicitudes.asignar", "pasos.asignar" y "metricas.ver" existen en `PermisosSchema`
  </action>
  <verify>
    <automated>
      cd "C:/Users/ilesm/Documents/SIAST" && npm run build --workspace=packages/shared 2>&1 | tail -20
    </automated>
  </verify>
  <acceptance_criteria>
    - `npm run build --workspace=packages/shared` termina sin errores de TypeScript
    - `grep -c "RESPONSABLE_TI" packages/shared/src/index.ts` devuelve al menos 3 (en RolSchema + LABEL_ROL + PERMISOS_DEFAULT)
    - `grep "TECNICO_ELECTRICISTA" packages/shared/src/index.ts` aparece en RolSchema y en PERMISOS_DEFAULT
    - `grep "TECNICO_SERVICIOS" packages/shared/src/index.ts` sigue presente (no eliminado)
    - Build de @stf/shared sin error "Type '...' is not assignable to type 'Record&lt;Rol, Permiso[]&gt;'"
  </acceptance_criteria>
  <done>@stf/shared build limpio con los 14 roles en RolSchema, 14 entradas en PERMISOS_DEFAULT, y 14 etiquetas en LABEL_ROL</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema.prisma → DB migration | El archivo schema.prisma es la fuente de verdad; la migración aplicará MODIFY ENUM en MySQL |
| shared/index.ts → frontend + backend | El enum Zod es consumido por ambas capas; un error aquí rompe todo el build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | schema.prisma enum Rol | mitigate | Mantener TECNICO_SERVICIOS en el enum — no eliminarlo aunque esté deprecated; la migración MySQL falla si filas existentes tienen ese valor |
| T-03-01-02 | Elevation of Privilege | PERMISOS_DEFAULT nuevos roles | mitigate | RESPONSABLE_* reciben "solicitudes.asignar" pero el scope real se aplica con requireResponsableDeArea() en Plan 03 — no confiar solo en permisos declarativos |
| T-03-01-03 | Denial of Service | TypeScript build | mitigate | PERMISOS_DEFAULT debe ser exhaustivo — si falta cualquier Rol nuevo, el build falla y bloquea todos los planes siguientes; verificar con `npm run build` antes de finalizar |
</threat_model>

<verification>
```bash
# 1. Prisma schema válido
cd "C:/Users/ilesm/Documents/SIAST/packages/database" && npx prisma validate

# 2. Shared package compila sin errores
cd "C:/Users/ilesm/Documents/SIAST" && npm run build --workspace=packages/shared

# 3. Los 7 nuevos roles están en schema.prisma
grep -E "RESPONSABLE_TI|RESPONSABLE_REDES|RESPONSABLE_MANTENIMIENTO|RESPONSABLE_RECURSOS_MATERIALES|TECNICO_ELECTRICISTA|TECNICO_PLOMERO|TECNICO_MOVILIDAD" "C:/Users/ilesm/Documents/SIAST/packages/database/prisma/schema.prisma"

# 4. AreaSoporte model existe
grep "model AreaSoporte" "C:/Users/ilesm/Documents/SIAST/packages/database/prisma/schema.prisma"

# 5. TECNICO_SERVICIOS no fue eliminado
grep "TECNICO_SERVICIOS" "C:/Users/ilesm/Documents/SIAST/packages/database/prisma/schema.prisma"
```
</verification>

<success_criteria>
- `npx prisma validate` pasa sin errores
- `npm run build --workspace=packages/shared` pasa sin errores de TypeScript
- Enum Rol en schema.prisma tiene exactamente 14 valores (7 existentes + 7 nuevos)
- TECNICO_SERVICIOS sigue en el enum (no eliminado)
- Modelo AreaSoporte existe con campos subcategorias Json, rolesIncluidos Json, activo Boolean, createdAt DateTime
- Usuario tiene campo areaSoporteId Int? con relación opcional a AreaSoporte
- PERMISOS_DEFAULT tiene entradas para los 14 roles
</success_criteria>

<output>
After completion, create `.planning/phases/03-roles-y-areas-de-soporte/03-01-SUMMARY.md`
</output>
