---
phase: 03-roles-y-areas-de-soporte
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/controllers/usuarios.controller.ts
  - apps/web/src/pages/AdminUsuariosPage.jsx
  - apps/web/src/pages/UsuariosPage.jsx
autonomous: true
gap_closure: true
requirements:
  - ROL-04
must_haves:
  truths:
    - "Crear/editar usuario sin 'usuario' muestra error de campo específico antes de enviar al backend"
    - "Crear/editar usuario siempre deriva areaSoporteId del rol en backend — no existe selector manual en frontend"
    - "El formulario AdminUsuariosPage valida nombre, apellidos, usuario y password (crear) antes de submit"
    - "El formulario UsuariosPage muestra errores por campo (helperText) además del Alert general"
    - "Backend rechaza PATCH /usuarios/:id con usuario vacío retornando 400 antes de tocar Prisma"
  artifacts:
    - path: "apps/api/src/controllers/usuarios.controller.ts"
      provides: "Validación de campos requeridos en actualizar(); areaSoporteId siempre derivado del rol"
      contains: "if (!usuario || !usuario.trim())"
    - path: "apps/web/src/pages/AdminUsuariosPage.jsx"
      provides: "Validación frontend con fieldErrors por campo antes de llamar guardar()"
      contains: "fieldErrors"
    - path: "apps/web/src/pages/UsuariosPage.jsx"
      provides: "helperText de error en campos requeridos + limpieza de form tras éxito"
      contains: "fieldErrors"
  key_links:
    - from: "AdminUsuariosPage.jsx guardar()"
      to: "PATCH /api/admin/usuarios/:id"
      via: "updateUsuario(editId, payload)"
      pattern: "fieldErrors.*usuario"
    - from: "usuarios.controller.ts actualizar()"
      to: "prisma.usuario.update()"
      via: "validación explícita antes del update"
      pattern: "if.*usuario.*trim"
---

<objective>
Gap closure para la fase 03: corregir diseño de areaSoporte (derivación implícita por rol, no selector manual)
y agregar validación de campos requeridos en formularios de usuario antes de que el error llegue a Prisma.

Purpose: El UAT detectó que (1) areaSoporteId debía derivarse del nombre de rol en backend sin selector frontend,
y (2) editar usuario con campo 'usuario' vacío lanza Prisma unique constraint error en lugar de un mensaje útil.

Output:
- Backend: validación de campos requeridos en actualizar() para prevenir errores Prisma
- Frontend AdminUsuariosPage: validación con fieldErrors por campo antes de submit
- Frontend UsuariosPage: fieldErrors por campo + limpieza de form tras éxito
- Confirmación: selector areaSoporte ya eliminado en ambas páginas (verificar y dejar limpio)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\Users\ilesm\Documents\SIAST\.planning\PROJECT.md
@C:\Users\ilesm\Documents\SIAST\.planning\ROADMAP.md

<!-- Implementación actual relevante -->
<!-- apps/api/src/controllers/usuarios.controller.ts -->
<!-- - ROL_AREA_MAP y resolveAreaId() ya existen y funcionan correctamente -->
<!-- - crear(): deriva areaSoporteId desde rol — OK -->
<!-- - actualizar(): deriva areaSoporteId si rol cambia — OK, pero NO valida campos requeridos -->
<!-- - Bug: si req.body.usuario es "" o undefined, prisma.usuario.update() lanza UniqueConstraint -->

<!-- apps/web/src/pages/AdminUsuariosPage.jsx -->
<!-- - guardar() actual: sin ninguna validación antes de submit -->
<!-- - No hay fieldErrors state -->
<!-- - EMPTY_FORM no tiene areaSoporteId — correcto, selector ya no existe -->

<!-- apps/web/src/pages/UsuariosPage.jsx -->
<!-- - handleGuardar() valida nombre/apellidos/usuario/rol pero solo con un Alert genérico -->
<!-- - No hay fieldErrors con helperText por campo -->
<!-- - emptyForm no tiene areaSoporteId — correcto, selector ya no existe -->

<interfaces>
<!-- Tipos usados por el controller -->
<!-- packages/shared/src/index.ts exporta RolSchema (z.enum con los 14 roles) -->

<!-- Estructura userSelect en usuarios.controller.ts: -->
<!-- { id, nombre, apellidos, usuario, email, telefono, rol, activo, permisos, -->
<!--   esEmpleadoEstructura, empleadoId, rfc, createdAt, areaSoporteId, -->
<!--   areaSoporte: { select: { nombre: true } } } -->

<!-- Endpoints afectados: -->
<!-- PATCH /api/usuarios/:id  → usuarios.routes.ts → ctrl.actualizar -->
<!-- PATCH /api/admin/usuarios/:id → admin.routes.ts → usuariosCtrl.actualizar -->
<!-- POST  /api/usuarios/      → ctrl.crear (ya valida password pero no usuario/nombre) -->
<!-- POST  /api/admin/usuarios/ → ctrl.crear (misma función) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validación de campos requeridos en backend (usuarios.controller.ts)</name>
  <files>apps/api/src/controllers/usuarios.controller.ts</files>
  <action>
Modificar las funciones `crear` y `actualizar` en `apps/api/src/controllers/usuarios.controller.ts`
para validar campos requeridos ANTES de cualquier operación async.

**En `crear` — agregar validación al inicio de la función, después de desestructurar req.body:**

```typescript
// Validar campos requeridos
const camposFaltantes: string[] = [];
if (!rest.nombre?.trim()) camposFaltantes.push("nombre");
if (!rest.apellidos?.trim()) camposFaltantes.push("apellidos");
if (!(rest as { usuario?: string }).usuario?.trim()) camposFaltantes.push("usuario");
if (!rest.rol) camposFaltantes.push("rol");
if (!password?.trim()) camposFaltantes.push("password");
if (camposFaltantes.length > 0) {
  res.status(400).json({ error: "Campos requeridos faltantes", campos: camposFaltantes });
  return;
}
```

NOTA: el campo `usuario` viene dentro de `rest` (no está desestructurado por separado). Ajustar
el acceso: `(rest as any).usuario` o agregar `usuario` a la desestructuración del body. Preferir
agregar `usuario` a la desestructuración explícita:

```typescript
const { password, usuario, esEmpleadoEstructura, empleadoId, rfc, permisos, ...rest } = req.body as {
  nombre: string; apellidos: string; usuario: string;
  password: string; rol: string; email?: string; telefono?: string;
  esEmpleadoEstructura?: boolean; empleadoId?: string; rfc?: string;
  permisos?: string[];
};
```

Luego usar `usuario` en la validación Y pasarlo al `prisma.usuario.create()`:
```typescript
data: {
  ...rest,
  usuario,           // <-- agregado explícitamente
  rol: rest.rol as never,
  ...
}
```

**En `actualizar` — agregar validación al inicio:**

```typescript
// Si viene usuario en el body, no puede ser vacío
const { password, usuario, esEmpleadoEstructura, empleadoId, rfc, permisos, ...rest } = req.body;

if (usuario !== undefined && !usuario?.trim()) {
  res.status(400).json({ error: "El campo usuario no puede estar vacío", campos: ["usuario"] });
  return;
}
if (rest.nombre !== undefined && !rest.nombre?.trim()) {
  res.status(400).json({ error: "El campo nombre no puede estar vacío", campos: ["nombre"] });
  return;
}
if (rest.apellidos !== undefined && !rest.apellidos?.trim()) {
  res.status(400).json({ error: "El campo apellidos no puede estar vacío", campos: ["apellidos"] });
  return;
}
```

Luego incluir `usuario` en el objeto `data` cuando venga en el body:
```typescript
const data: Record<string, unknown> = { ...rest };
if (usuario !== undefined) data.usuario = usuario.trim();
if (password) data.password = await bcrypt.hash(password, 10);
```

**IMPORTANTE:** No eliminar ni modificar `ROL_AREA_MAP`, `resolveAreaId`, ni la lógica de
derivación de `areaSoporteId`. Esa lógica es correcta y no debe tocarse.

**Verificar que el build TypeScript pasa:** `npm run build --workspace=apps/api`
  </action>
  <verify>
    <automated>cd C:\Users\ilesm\Documents\SIAST && npm run build --workspace=apps/api</automated>
  </verify>
  <done>
Build TypeScript pasa sin errores.
POST /api/usuarios con usuario="" retorna 400 con { error, campos: ["usuario"] }.
PATCH /api/usuarios/:id con body { usuario: "" } retorna 400 sin tocar Prisma.
ROL_AREA_MAP y resolveAreaId permanecen intactos.
  </done>
</task>

<task type="auto">
  <name>Task 2: Validación con fieldErrors en AdminUsuariosPage.jsx</name>
  <files>apps/web/src/pages/AdminUsuariosPage.jsx</files>
  <action>
Agregar validación de campos requeridos con errores por campo en `AdminUsuariosPage.jsx`.

**1. Agregar estado `fieldErrors` junto al resto de estados:**
```jsx
const [fieldErrors, setFieldErrors] = useState({});
```

**2. Limpiar fieldErrors al abrir dialogs:**
En `abrirCrear` y `abrirEditar`, agregar `setFieldErrors({});`

**3. Reemplazar la función `guardar` con versión que valida antes de submit:**
```jsx
const guardar = async () => {
  // Validar campos requeridos
  const errors = {};
  if (!form.nombre.trim()) errors.nombre = "El nombre es obligatorio";
  if (!form.apellidos.trim()) errors.apellidos = "Los apellidos son obligatorios";
  if (!form.usuario.trim()) errors.usuario = "El usuario de login es obligatorio";
  if (!editId && !form.password.trim()) errors.password = "La contraseña es obligatoria";

  if (Object.keys(errors).length > 0) {
    setFieldErrors(errors);
    return;
  }
  setFieldErrors({});

  setSaving(true);
  try {
    const payload = { ...form };
    if (editId && !payload.password) delete payload.password;
    // Eliminar areaSoporteId si viniera en payload (no debe existir — el backend lo deriva del rol)
    delete payload.areaSoporteId;
    if (editId) {
      await updateUsuario(editId, payload);
    } else {
      await createUsuario(payload);
    }
    setDialogOpen(false);
    setForm(EMPTY_FORM);
    cargar();
  } catch (e) {
    // Mostrar error del backend — si tiene campo específico, mapear a fieldErrors
    const errMsg = e?.response?.data?.error ?? "Error al guardar";
    const campos = e?.response?.data?.campos ?? [];
    if (campos.length > 0) {
      const backendErrors = {};
      campos.forEach((c) => { backendErrors[c] = errMsg; });
      setFieldErrors(backendErrors);
    } else {
      setError(errMsg);
    }
  } finally {
    setSaving(false);
  }
};
```

**4. Actualizar los TextField del dialog para mostrar error y helperText por campo:**

En el TextField de Nombre:
```jsx
<TextField
  label="Nombre"
  value={form.nombre}
  onChange={(e) => { setForm((f) => ({ ...f, nombre: e.target.value })); setFieldErrors((fe) => ({ ...fe, nombre: undefined })); }}
  size="small" fullWidth required
  error={Boolean(fieldErrors.nombre)}
  helperText={fieldErrors.nombre}
/>
```

En el TextField de Apellidos:
```jsx
<TextField
  label="Apellidos"
  value={form.apellidos}
  onChange={(e) => { setForm((f) => ({ ...f, apellidos: e.target.value })); setFieldErrors((fe) => ({ ...fe, apellidos: undefined })); }}
  size="small" fullWidth required
  error={Boolean(fieldErrors.apellidos)}
  helperText={fieldErrors.apellidos}
/>
```

En el TextField de Usuario:
```jsx
<TextField
  label="Usuario"
  value={form.usuario}
  onChange={(e) => { setForm((f) => ({ ...f, usuario: e.target.value })); setFieldErrors((fe) => ({ ...fe, usuario: undefined })); }}
  size="small" fullWidth required
  error={Boolean(fieldErrors.usuario)}
  helperText={fieldErrors.usuario ?? "Ej: jilescas"}
/>
```

En el TextField de Contraseña:
```jsx
<TextField
  label="Contraseña"
  type="password"
  value={form.password}
  onChange={(e) => { setForm((f) => ({ ...f, password: e.target.value })); setFieldErrors((fe) => ({ ...fe, password: undefined })); }}
  size="small" fullWidth
  placeholder={editId ? "Dejar vacío para no cambiar" : ""}
  required={!editId}
  error={Boolean(fieldErrors.password)}
  helperText={fieldErrors.password}
/>
```

**5. Verificar que NO existe ningún selector de areaSoporte en el JSX del dialog.**
Si existe algún `Select` o `MenuItem` relacionado con `areaSoporte` o `areaSoporteId`, eliminarlo.
El `handleRolChange` no debe tocar `areaSoporteId` — limpiar si existe esa lógica.

**6. Mover el `setError(null)` al abrir dialog (ya existe en el Alert del componente):**
Agregar `setError(null);` al inicio de `abrirCrear` y `abrirEditar` si no existe.

El estado `error` existente se mantiene para errores generales del backend que no sean de campo.
  </action>
  <verify>
    <automated>cd C:\Users\ilesm\Documents\SIAST && npm run build --workspace=apps/web</automated>
  </verify>
  <done>
Build Vite pasa sin errores.
Al hacer clic en "Crear usuario" con campos vacíos, los TextField muestran helperText de error en rojo sin llamar al backend.
Al corregir un campo con error, el helperText desaparece inmediatamente (onChange limpia ese campo del fieldErrors).
No existe selector de areaSoporte en el formulario.
  </done>
</task>

<task type="auto">
  <name>Task 3: fieldErrors y limpieza de form en UsuariosPage.jsx</name>
  <files>apps/web/src/pages/UsuariosPage.jsx</files>
  <action>
Mejorar la validación en `UsuariosPage.jsx` reemplazando el Alert genérico de error con fieldErrors
por campo, y agregar limpieza de form tras éxito.

**1. Agregar estado `fieldErrors`:**
```jsx
const [fieldErrors, setFieldErrors] = useState({});
```

**2. Limpiar fieldErrors al abrir dialogs:**
En `openCrear`: agregar `setFieldErrors({});`
En `openEditar`: agregar `setFieldErrors({});`

**3. Reemplazar la lógica de validación en `handleGuardar`:**
Cambiar el bloque actual de validación con `setError(...)` por:

```jsx
const handleGuardar = async () => {
  setError("");
  const errors = {};
  if (!form.nombre.trim()) errors.nombre = "El nombre es obligatorio";
  if (!form.apellidos.trim()) errors.apellidos = "Los apellidos son obligatorios";
  if (!form.usuario.trim()) errors.usuario = "El usuario de login es obligatorio";
  if (!form.rol) errors.rol = "El rol es obligatorio";
  if (dialog === "crear" && !form.password.trim()) errors.password = "La contraseña es obligatoria";

  if (Object.keys(errors).length > 0) {
    setFieldErrors(errors);
    return;
  }
  setFieldErrors({});

  setSaving(true);
  try {
    const payload = { ...form };
    if (!payload.password) delete payload.password;
    // Eliminar areaSoporteId si existiera en payload (el backend lo deriva del rol)
    delete payload.areaSoporteId;
    if (!payload.esEmpleadoEstructura) {
      payload.empleadoId = null;
      payload.rfc = null;
    }
    if (dialog === "crear") {
      await createUsuario(payload);
    } else {
      await updateUsuario(dialog.id, payload);
    }
    setDialog(null);
    setForm(emptyForm);      // limpiar form tras éxito
    setFieldErrors({});
    load();
  } catch (err) {
    const errMsg = err.response?.data?.error ?? "Error al guardar";
    const campos = err.response?.data?.campos ?? [];
    if (campos.length > 0) {
      const backendErrors = {};
      campos.forEach((c) => { backendErrors[c] = errMsg; });
      setFieldErrors(backendErrors);
    } else {
      setError(errMsg);
    }
  } finally {
    setSaving(false);
  }
};
```

**4. Agregar `error` y `helperText` a los TextField requeridos del dialog:**

TextField "Nombre(s)":
```jsx
error={Boolean(fieldErrors.nombre)}
helperText={fieldErrors.nombre}
onChange={(e) => { set("nombre", e.target.value); setFieldErrors((fe) => ({ ...fe, nombre: undefined })); }}
```

TextField "Apellidos":
```jsx
error={Boolean(fieldErrors.apellidos)}
helperText={fieldErrors.apellidos}
onChange={(e) => { set("apellidos", e.target.value); setFieldErrors((fe) => ({ ...fe, apellidos: undefined })); }}
```

TextField "Usuario (login)":
```jsx
error={Boolean(fieldErrors.usuario)}
helperText={fieldErrors.usuario ?? "Sugerencia: inicial del nombre + apellido paterno (ej: jilescas)"}
onChange={(e) => { set("usuario", e.target.value); setFieldErrors((fe) => ({ ...fe, usuario: undefined })); }}
```

TextField de contraseña:
```jsx
error={Boolean(fieldErrors.password)}
helperText={fieldErrors.password}
```

**5. Verificar que NO existe ningún selector de areaSoporte en el JSX del dialog.**
Si existe algún FormControl/Select relacionado con `areaSoporteId` o `areaSoporte`, eliminarlo.
Verificar que `emptyForm` no tiene campo `areaSoporteId`.

**6. Mantener el Alert de `error` genérico ya existente en el dialog** — sirve para errores
de backend que no son de campo específico (ej: "El usuario ya existe").

**IMPORTANTE:** Preservar toda la funcionalidad SIRH (búsqueda por RFC, tarjeta de sync,
`loadSyncStatus`, `handleSyncNow`, etc.) — no tocar esas secciones.
  </action>
  <verify>
    <automated>cd C:\Users\ilesm\Documents\SIAST && npm run build --workspace=apps/web</automated>
  </verify>
  <done>
Build Vite pasa sin errores.
Al intentar guardar con campos vacíos, los TextField muestran helperText de error sin llamar al backend.
Al corregir un campo, su error desaparece en el onChange.
Tras guardar exitosamente, el form queda en blanco (emptyForm).
No existe selector de areaSoporte en el formulario.
Funcionalidad SIRH intacta (tarjeta sync, búsqueda RFC).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→API | Payload de crear/editar usuario pasa por frontend sin garantía de campos limpios |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03gc-01 | Tampering | usuarios.controller.ts actualizar() | mitigate | Validar usuario no-vacío antes de prisma.update; nunca confiar en frontend |
| T-03gc-02 | Tampering | usuarios.controller.ts crear() | mitigate | Validar todos los campos requeridos retornando 400 antes de bcrypt/Prisma |
| T-03gc-03 | Tampering | areaSoporteId en payload | accept | Backend ignora areaSoporteId del body — siempre lo deriva del rol; frontend no lo envía |
</threat_model>

<verification>
```bash
# Build completo desde raíz del monorepo
cd C:\Users\ilesm\Documents\SIAST
npm run build

# Verificar que el controller valida campos vacíos (manual con curl o Postman):
# PATCH /api/admin/usuarios/1 con body { "usuario": "" }
# → debe retornar HTTP 400 { error: "El campo usuario no puede estar vacío", campos: ["usuario"] }

# Verificar en UI (AdminUsuariosPage):
# 1. Abrir dialog "Nuevo Usuario"
# 2. Hacer clic en "Crear usuario" con todos los campos vacíos
# → Campos nombre/apellidos/usuario/password muestran helperText de error en rojo
# → No hay llamada de red (verificar en DevTools Network)

# Verificar en UI (UsuariosPage):
# 1. Abrir dialog "Nuevo usuario"
# 2. Hacer clic en "Guardar" con campos vacíos
# → Los TextField muestran helperText de error por campo
# 3. Crear un usuario válido → dialog se cierra, form queda vacío para la próxima apertura
```
</verification>

<success_criteria>
- Build `npm run build` pasa para api y web sin errores TypeScript/Vite
- Backend retorna HTTP 400 con `{ error, campos }` cuando usuario/nombre/apellidos están vacíos en crear o actualizar
- AdminUsuariosPage.jsx muestra errores por campo (helperText rojo) antes de cualquier llamada HTTP
- UsuariosPage.jsx muestra errores por campo (helperText rojo) antes de cualquier llamada HTTP
- Tras submit exitoso en UsuariosPage, el form queda limpio (emptyForm)
- Ningún formulario tiene selector de areaSoporte — el área la deriva exclusivamente el backend
- Error de backend con `campos` array se mapea a fieldErrors (no a Alert genérico)
- Funcionalidad SIRH (sync, búsqueda RFC) en UsuariosPage intacta
</success_criteria>

<output>
Tras completar el plan, crear:
`.planning/phases/03-roles-y-areas-de-soporte/03-05-SUMMARY.md`

Con formato estándar: cambios realizados por archivo, verificaciones ejecutadas, resultado.
</output>
