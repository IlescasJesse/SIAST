---
phase: 03-roles-y-areas-de-soporte
plan: "04"
type: execute
wave: 4
depends_on:
  - "03-03"
files_modified:
  - apps/web/src/pages/UsuariosPage.jsx
  - apps/web/src/pages/AdminUsuariosPage.jsx
  - apps/web/src/api/catalogos.js
  - apps/web/src/api/admin.js
autonomous: true
requirements:
  - ROL-05

must_haves:
  truths:
    - "UsuariosPage.jsx muestra selector de AreaSoporte cuando el rol seleccionado es RESPONSABLE_*"
    - "AdminUsuariosPage.jsx muestra selector de AreaSoporte cuando el rol seleccionado es RESPONSABLE_*"
    - "El selector de AreaSoporte no aparece para roles no-RESPONSABLE_*"
    - "Al cambiar el rol de RESPONSABLE_* a otro rol, el campo areaSoporteId se limpia"
    - "ROLES_STAFF en UsuariosPage incluye los 7 nuevos roles (incluye TECNICO_REDES que faltaba)"
    - "ROLES en AdminUsuariosPage incluye los 7 nuevos roles"
    - "El payload enviado al crear/editar un usuario RESPONSABLE_* incluye areaSoporteId"
    - "El payload enviado al crear/editar un usuario no-RESPONSABLE_* tiene areaSoporteId = null"
    - "RESPONSABLE_* se une a la room admins en Socket.IO para recibir notificaciones ticket:nuevo"
  artifacts:
    - path: "apps/web/src/pages/UsuariosPage.jsx"
      provides: "ROLES_STAFF extendido + emptyForm con areaSoporteId + selector condicional + payload cleanup"
      contains: "RESPONSABLE_TI"
    - path: "apps/web/src/pages/AdminUsuariosPage.jsx"
      provides: "ROLES extendido + EMPTY_FORM con areaSoporteId + selector condicional + handleRolChange limpieza"
      contains: "RESPONSABLE_TI"
    - path: "apps/web/src/api/catalogos.js"
      provides: "getAreasSoporte() helper para UsuariosPage"
      contains: "getAreasSoporte"
    - path: "apps/web/src/api/admin.js"
      provides: "getAreasSoporte() helper para AdminUsuariosPage"
      contains: "getAreasSoporte"
  key_links:
    - from: "apps/web/src/pages/UsuariosPage.jsx"
      to: "apps/web/src/api/catalogos.js"
      via: "getAreasSoporte() llamado en useEffect al montar el componente"
      pattern: "getAreasSoporte"
    - from: "apps/web/src/pages/AdminUsuariosPage.jsx"
      to: "apps/web/src/api/admin.js"
      via: "getAreasSoporte() llamado en useEffect al montar el componente"
      pattern: "getAreasSoporte"
    - from: "Selector MUI <Select> areaSoporteId"
      to: "form.areaSoporteId"
      via: "onChange actualiza form state; RESPONSABLE_ROLES.includes(form.rol) controla visibilidad"
      pattern: "RESPONSABLE_ROLES.includes"
---

<objective>
Actualizar las dos páginas de gestión de usuarios para mostrar el selector de AreaSoporte condicionalmente cuando se crea o edita un usuario con rol RESPONSABLE_*. Extender los arrays de roles, agregar helpers de API para cargar las áreas de soporte, y limpiar el payload antes de enviar.

Purpose: Completar el requisito ROL-05 — el panel admin necesita este selector para que el administrador pueda asignar un área al crear usuarios RESPONSABLE_*. Sin esto, el backend rechaza la creación (areaSoporteId obligatorio para RESPONSABLE_*).

Output:
- UsuariosPage.jsx con selector AreaSoporte condicional
- AdminUsuariosPage.jsx con selector AreaSoporte condicional + limpieza en handleRolChange
- catalogos.js con getAreasSoporte()
- admin.js con getAreasSoporte()
</objective>

<execution_context>
@C:/Users/ilesm/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ilesm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-roles-y-areas-de-soporte/03-CONTEXT.md
@.planning/phases/03-roles-y-areas-de-soporte/03-RESEARCH.md
@.planning/phases/03-roles-y-areas-de-soporte/03-PATTERNS.md
@.planning/phases/03-roles-y-areas-de-soporte/03-03-SUMMARY.md

<interfaces>
<!-- Interfaces exactas de las páginas — extraídas de la investigación del codebase -->

ROLES_STAFF actual en UsuariosPage.jsx (línea 26):
```javascript
const ROLES_STAFF = ["ADMIN", "TECNICO_TI", "TECNICO_SERVICIOS", "MESA_AYUDA", "GESTOR_RECURSOS_MATERIALES"];
// NOTA: TECNICO_REDES faltaba — agregar también al extender
```

emptyForm actual en UsuariosPage.jsx (líneas 36-40):
```javascript
const emptyForm = {
  nombre: "", apellidos: "", usuario: "", password: "", rol: "MESA_AYUDA",
  telefono: "", email: "",
  esEmpleadoEstructura: false, empleadoId: "", rfc: "",
};
```

Patrón condicional en UsuariosPage.jsx (líneas ~447):
```jsx
{form.esEmpleadoEstructura && (
  <Box>
    <Box sx={{ display: "flex", gap: 1, alignItems: "flex-start" }}>
      <TextField label="RFC del empleado" ... />
      <Button>Buscar</Button>
    </Box>
  </Box>
)}
```

Payload cleanup en UsuariosPage.jsx handleGuardar (líneas 181-184):
```javascript
if (!payload.esEmpleadoEstructura) {
  payload.empleadoId = null;
  payload.rfc = null;
}
```

ROLES actual en AdminUsuariosPage.jsx (líneas 16-19):
```javascript
const ROLES = ["ADMIN", "MESA_AYUDA", "TECNICO_TI", "TECNICO_REDES", "TECNICO_SERVICIOS", "GESTOR_RECURSOS_MATERIALES"];
```

EMPTY_FORM actual en AdminUsuariosPage.jsx (líneas 21-24):
```javascript
const EMPTY_FORM = {
  nombre: "", apellidos: "", usuario: "", password: "", email: "",
  telefono: "", rol: "MESA_AYUDA", activo: true, permisos: [],
};
```

handleRolChange actual en AdminUsuariosPage.jsx (líneas 69-72):
```javascript
const handleRolChange = (newRol) => {
  setForm((f) => ({ ...f, rol: newRol, permisos: [] }));
};
```

abrirEditar actual en AdminUsuariosPage.jsx (líneas 58-67):
```javascript
const abrirEditar = (u) => {
  setEditId(u.id);
  setForm({
    nombre: u.nombre, apellidos: u.apellidos, usuario: u.usuario,
    password: "", email: u.email ?? "", telefono: u.telefono ?? "",
    rol: u.rol, activo: u.activo, permisos: u.permisos ?? [],
  });
};
```

Patrón de API call en catalogos.js (línea 3):
```javascript
export const getCatalogos = () => api.get("/api/catalogos").then((r) => r.data);
```

Patrón de API call en admin.js:
```javascript
export const getUsuarios = () => api.get("/api/admin/usuarios").then((r) => r.data);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Agregar getAreasSoporte() a los módulos de API</name>
  <read_first>
    apps/web/src/api/catalogos.js
    apps/web/src/api/admin.js
  </read_first>
  <files>
    apps/web/src/api/catalogos.js
    apps/web/src/api/admin.js
  </files>
  <action>
Leer ambos archivos completos para entender el patrón de exports y el nombre del objeto `api` (axios instance).

**Archivo 1: apps/web/src/api/catalogos.js**

Agregar al final del archivo (o junto a otros exports de catalog data):
```javascript
export const getAreasSoporte = () =>
  api.get("/api/admin/areas-soporte").then((r) => r.data.data);
```

`r.data.data` porque el backend responde con `{ data: AreaSoporte[] }` (patrón verificado en admin.routes.ts handler).

**Archivo 2: apps/web/src/api/admin.js**

Agregar la misma función al final del archivo (AdminUsuariosPage usa este módulo):
```javascript
export const getAreasSoporte = () =>
  api.get("/api/admin/areas-soporte").then((r) => r.data.data);
```

Notas:
- Las dos páginas usan módulos diferentes pero llaman al mismo endpoint — esto está documentado como Pitfall P6 en PATTERNS.md
- Verificar que el nombre de la instancia axios en cada archivo es `api` (puede variar)
- La URL es `/api/admin/areas-soporte` — incluye el prefijo `/api/admin` ya que está registrado bajo ese router
  </action>
  <verify>
    <automated>
      grep -n "getAreasSoporte" "C:/Users/ilesm/Documents/SIAST/apps/web/src/api/catalogos.js" "C:/Users/ilesm/Documents/SIAST/apps/web/src/api/admin.js"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep "getAreasSoporte" apps/web/src/api/catalogos.js` encuentra la función exportada
    - `grep "getAreasSoporte" apps/web/src/api/admin.js` encuentra la función exportada
    - Ambas funciones llaman a `/api/admin/areas-soporte` y retornan `r.data.data`
    - No se introducen imports nuevos (la instancia `api` ya existe en cada archivo)
  </acceptance_criteria>
  <done>getAreasSoporte() disponible en ambos módulos de API. UsuariosPage y AdminUsuariosPage pueden importarla desde su módulo respectivo.</done>
</task>

<task type="auto">
  <name>Task 2: UsuariosPage.jsx — extender roles, selector AreaSoporte, payload cleanup</name>
  <read_first>
    apps/web/src/pages/UsuariosPage.jsx
  </read_first>
  <files>apps/web/src/pages/UsuariosPage.jsx</files>
  <action>
Leer el archivo completo para ubicar exactamente: ROLES_STAFF (línea ~26), emptyForm (líneas ~36-40), handleGuardar (líneas ~177-184), bloque condicional esEmpleadoEstructura (líneas ~447), y el import de api module.

Realizar los siguientes cambios en orden:

**Cambio 1 — Importar getAreasSoporte** (agregar al import del módulo de API existente):
```javascript
import { ..., getAreasSoporte } from "../api/catalogos.js";
// O si el import es un objeto named export, agregar getAreasSoporte al destructure
```

**Cambio 2 — Extender ROLES_STAFF** (reemplazar la constante en línea ~26):
```javascript
const ROLES_STAFF = [
  "ADMIN", "MESA_AYUDA",
  "RESPONSABLE_TI", "RESPONSABLE_REDES", "RESPONSABLE_MANTENIMIENTO", "RESPONSABLE_RECURSOS_MATERIALES",
  "TECNICO_TI", "TECNICO_REDES",
  "TECNICO_ELECTRICISTA", "TECNICO_PLOMERO", "TECNICO_MOVILIDAD",
  "TECNICO_SERVICIOS",   // mantener — deprecated pero visible para reasignación manual
  "GESTOR_RECURSOS_MATERIALES",
];

const RESPONSABLE_ROLES = [
  "RESPONSABLE_TI", "RESPONSABLE_REDES",
  "RESPONSABLE_MANTENIMIENTO", "RESPONSABLE_RECURSOS_MATERIALES",
];
```

**Cambio 3 — Agregar areaSoporteId a emptyForm** (líneas ~36-40):
```javascript
const emptyForm = {
  nombre: "", apellidos: "", usuario: "", password: "", rol: "MESA_AYUDA",
  telefono: "", email: "",
  esEmpleadoEstructura: false, empleadoId: "", rfc: "",
  areaSoporteId: null,   // nuevo campo
};
```

**Cambio 4 — Agregar estado y useEffect para areasSoporte** (junto a otros useState/useEffect existentes):
```javascript
const [areasSoporte, setAreasSoporte] = useState([]);

useEffect(() => {
  getAreasSoporte().then(setAreasSoporte).catch(() => {});
}, []);
```

**Cambio 5 — Agregar selector condicional en el formulario** (DESPUÉS del bloque `{form.esEmpleadoEstructura && ...}` o antes del bloque de botones del formulario — en la sección del formulario de creación/edición):
```jsx
{RESPONSABLE_ROLES.includes(form.rol) && (
  <FormControl fullWidth required>
    <InputLabel>Área de Soporte</InputLabel>
    <Select
      value={form.areaSoporteId ?? ""}
      label="Área de Soporte"
      onChange={(e) => set("areaSoporteId", e.target.value || null)}
    >
      {areasSoporte.map((a) => (
        <MenuItem key={a.id} value={a.id}>{a.nombre}</MenuItem>
      ))}
    </Select>
  </FormControl>
)}
```

Verificar que `FormControl`, `InputLabel`, `Select`, `MenuItem` están importados de `@mui/material` (ya deberían estar — el formulario tiene otros Select).

**Cambio 6 — Payload cleanup en handleGuardar** (junto al cleanup de empleadoId/rfc, líneas ~181-184):
```javascript
// Después de la limpieza existente de empleadoId/rfc:
if (!RESPONSABLE_ROLES.includes(payload.rol)) {
  payload.areaSoporteId = null;
}
```

**Cambio 7 — Poblar areaSoporteId al abrir para edición** (en la función que carga los datos del usuario en el form — probablemente `handleEditar` o similar):
```javascript
// En el setForm de apertura de edición, agregar:
areaSoporteId: u.areaSoporteId ?? null,
```
  </action>
  <verify>
    <automated>
      grep -n "RESPONSABLE_ROLES\|areaSoporteId\|getAreasSoporte" "C:/Users/ilesm/Documents/SIAST/apps/web/src/pages/UsuariosPage.jsx"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep "RESPONSABLE_ROLES" apps/web/src/pages/UsuariosPage.jsx` encuentra la constante definida y usada en la condición del selector
    - `grep "areaSoporteId" apps/web/src/pages/UsuariosPage.jsx` encuentra al menos 4 ocurrencias: emptyForm, selector onChange, payload cleanup, edición
    - `grep "getAreasSoporte" apps/web/src/pages/UsuariosPage.jsx` encuentra el import y el useEffect
    - `grep "RESPONSABLE_TI.*RESPONSABLE_REDES" apps/web/src/pages/UsuariosPage.jsx` encuentra el ROLES_STAFF extendido
    - `grep "TECNICO_REDES" apps/web/src/pages/UsuariosPage.jsx` encuentra TECNICO_REDES en ROLES_STAFF (era el rol que faltaba)
    - Condición `RESPONSABLE_ROLES.includes(form.rol)` controla visibilidad del selector (no aparece para TECNICO_TI, etc.)
  </acceptance_criteria>
  <done>UsuariosPage.jsx con selector AreaSoporte condicional, ROLES_STAFF extendido con todos los 14 roles, payload cleanup correcto, y carga de áreas en useEffect.</done>
</task>

<task type="auto">
  <name>Task 3: AdminUsuariosPage.jsx — extender roles, selector AreaSoporte, handleRolChange, abrirEditar</name>
  <read_first>
    apps/web/src/pages/AdminUsuariosPage.jsx
  </read_first>
  <files>apps/web/src/pages/AdminUsuariosPage.jsx</files>
  <action>
Leer el archivo completo para ubicar exactamente: ROLES (líneas ~16-19), EMPTY_FORM (líneas ~21-24), handleRolChange (líneas ~69-72), abrirEditar (líneas ~58-67), y el bloque de permisos condicional (líneas ~248-287).

Realizar los siguientes cambios:

**Cambio 1 — Importar getAreasSoporte desde admin.js** (agregar al import del módulo admin):
```javascript
import { ..., getAreasSoporte } from "../api/admin.js";
```

**Cambio 2 — Extender ROLES y agregar RESPONSABLE_ROLES** (reemplazar constante ROLES en líneas ~16-19):
```javascript
const ROLES = [
  "ADMIN", "MESA_AYUDA",
  "RESPONSABLE_TI", "RESPONSABLE_REDES", "RESPONSABLE_MANTENIMIENTO", "RESPONSABLE_RECURSOS_MATERIALES",
  "TECNICO_TI", "TECNICO_REDES",
  "TECNICO_ELECTRICISTA", "TECNICO_PLOMERO", "TECNICO_MOVILIDAD",
  "TECNICO_SERVICIOS",
  "GESTOR_RECURSOS_MATERIALES",
];

const RESPONSABLE_ROLES = [
  "RESPONSABLE_TI", "RESPONSABLE_REDES",
  "RESPONSABLE_MANTENIMIENTO", "RESPONSABLE_RECURSOS_MATERIALES",
];
```

**Cambio 3 — Agregar areaSoporteId a EMPTY_FORM** (líneas ~21-24):
```javascript
const EMPTY_FORM = {
  nombre: "", apellidos: "", usuario: "", password: "", email: "",
  telefono: "", rol: "MESA_AYUDA", activo: true, permisos: [],
  areaSoporteId: null,   // nuevo campo
};
```

**Cambio 4 — Agregar estado y useEffect para areasSoporte** (junto a otros useState):
```javascript
const [areasSoporte, setAreasSoporte] = useState([]);

useEffect(() => {
  getAreasSoporte().then(setAreasSoporte).catch(() => {});
}, []);
```

**Cambio 5 — Extender handleRolChange** para limpiar areaSoporteId cuando el nuevo rol no es RESPONSABLE_*:
```javascript
const handleRolChange = (newRol) => {
  setForm((f) => ({
    ...f,
    rol: newRol,
    permisos: [],
    areaSoporteId: RESPONSABLE_ROLES.includes(newRol) ? f.areaSoporteId : null,
  }));
};
```

**Cambio 6 — Agregar areaSoporteId en abrirEditar** (líneas ~58-67):
```javascript
const abrirEditar = (u) => {
  setEditId(u.id);
  setForm({
    nombre: u.nombre, apellidos: u.apellidos, usuario: u.usuario,
    password: "", email: u.email ?? "", telefono: u.telefono ?? "",
    rol: u.rol, activo: u.activo, permisos: u.permisos ?? [],
    areaSoporteId: u.areaSoporteId ?? null,   // nuevo campo
  });
  // ... resto sin cambios
};
```

**Cambio 7 — Agregar selector condicional en el formulario** (ANTES del Divider y la sección de permisos — líneas ~248):
```jsx
{/* Selector AreaSoporte — solo para RESPONSABLE_* */}
{RESPONSABLE_ROLES.includes(form.rol) && (
  <FormControl size="small" fullWidth required>
    <InputLabel>Área de Soporte</InputLabel>
    <Select
      value={form.areaSoporteId ?? ""}
      label="Área de Soporte"
      onChange={(e) => setForm((f) => ({ ...f, areaSoporteId: e.target.value || null }))}
    >
      {areasSoporte.map((a) => (
        <MenuItem key={a.id} value={a.id}>{a.nombre}</MenuItem>
      ))}
    </Select>
  </FormControl>
)}
```

Verificar que FormControl, InputLabel, Select, MenuItem están importados de @mui/material.

**Actualización Socket.IO — RESPONSABLE_* debe unirse a la room admins:**
El archivo `apps/web/src/store/notificaciones.js` existe y fue verificado. En la función `conectar(user)`, dentro del handler `socket.on("connect", ...)` (línea 62), reemplazar:

```javascript
// ANTES:
if (user.rol === "ADMIN" || user.rol === "MESA_AYUDA") socket.emit("join:admin");

// DESPUÉS:
if (
  user.rol === "ADMIN" ||
  user.rol === "MESA_AYUDA" ||
  user.rol === "RESPONSABLE_TI" ||
  user.rol === "RESPONSABLE_REDES" ||
  user.rol === "RESPONSABLE_MANTENIMIENTO" ||
  user.rol === "RESPONSABLE_RECURSOS_MATERIALES"
) {
  socket.emit("join:admin");
}
```

Este cambio es incondicional — RESPONSABLE_* SIEMPRE debe unirse a la room admins en Phase 3.
  </action>
  <verify>
    <automated>
      grep -n "RESPONSABLE_ROLES\|areaSoporteId\|getAreasSoporte" "C:/Users/ilesm/Documents/SIAST/apps/web/src/pages/AdminUsuariosPage.jsx"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep "RESPONSABLE_ROLES" apps/web/src/pages/AdminUsuariosPage.jsx` encuentra la constante definida y usada en handleRolChange y en el selector JSX
    - `grep "areaSoporteId" apps/web/src/pages/AdminUsuariosPage.jsx` encuentra al menos 4 ocurrencias: EMPTY_FORM, handleRolChange, abrirEditar, selector onChange
    - `grep "getAreasSoporte" apps/web/src/pages/AdminUsuariosPage.jsx` encuentra el import y el useEffect
    - `grep "RESPONSABLE_TI" apps/web/src/pages/AdminUsuariosPage.jsx` encuentra RESPONSABLE_TI en el array ROLES extendido
    - handleRolChange limpia areaSoporteId cuando newRol no está en RESPONSABLE_ROLES (verificar manualmente en código)
    - abrirEditar incluye `areaSoporteId: u.areaSoporteId ?? null`
  </acceptance_criteria>
  <done>AdminUsuariosPage.jsx con selector AreaSoporte condicional, ROLES extendido, handleRolChange limpiando areaSoporteId, y abrirEditar poblando areaSoporteId desde datos del usuario. Socket.IO join:admin extendido para RESPONSABLE_* en apps/web/src/store/notificaciones.js — sin condicional.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser form → POST /api/admin/usuarios | El payload de creación de usuario pasa por el form del browser; areaSoporteId debe ser limpiado en frontend para roles no-RESPONSABLE_* |
| GET /api/admin/areas-soporte → browser | El endpoint devuelve datos de configuración; protegido por token ADMIN |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Tampering | Form frontend envía areaSoporteId para usuario TECNICO_TI | mitigate | Payload cleanup en handleGuardar: `if (!RESPONSABLE_ROLES.includes(payload.rol)) payload.areaSoporteId = null`; el backend también valida y limpia |
| T-03-04-02 | Information Disclosure | GET /api/admin/areas-soporte accesible sin autenticación | mitigate | El endpoint está bajo /api/admin router que requiere token ADMIN (verificado en Plan 03); el frontend solo llama esto después de autenticarse |
| T-03-04-03 | Elevation of Privilege | Usuario RESPONSABLE_* creado sin areaSoporteId por error de UI | mitigate | Campo `required` en el FormControl del selector; validación backend en usuarios.controller.ts rechaza creación sin areaSoporteId para RESPONSABLE_* (Plan 03) |
</threat_model>

<verification>
```bash
# 1. RESPONSABLE_ROLES en ambas páginas
grep "RESPONSABLE_ROLES" "C:/Users/ilesm/Documents/SIAST/apps/web/src/pages/UsuariosPage.jsx"
grep "RESPONSABLE_ROLES" "C:/Users/ilesm/Documents/SIAST/apps/web/src/pages/AdminUsuariosPage.jsx"

# 2. areaSoporteId en ambas páginas
grep -c "areaSoporteId" "C:/Users/ilesm/Documents/SIAST/apps/web/src/pages/UsuariosPage.jsx"
grep -c "areaSoporteId" "C:/Users/ilesm/Documents/SIAST/apps/web/src/pages/AdminUsuariosPage.jsx"

# 3. getAreasSoporte en los módulos de API
grep "getAreasSoporte" "C:/Users/ilesm/Documents/SIAST/apps/web/src/api/catalogos.js"
grep "getAreasSoporte" "C:/Users/ilesm/Documents/SIAST/apps/web/src/api/admin.js"

# 4. Socket.IO join:admin extendido (si aplica)
grep -rn "join:admin" "C:/Users/ilesm/Documents/SIAST/apps/web/src/"

# 5. TECNICO_REDES en ROLES_STAFF de UsuariosPage (era el rol faltante pre-Phase 3)
grep "TECNICO_REDES" "C:/Users/ilesm/Documents/SIAST/apps/web/src/pages/UsuariosPage.jsx"

# 6. Prueba visual (requiere dev server)
# npm run dev:web
# Navegar a /usuarios → crear usuario RESPONSABLE_TI → verificar que aparece selector AreaSoporte
# Cambiar a TECNICO_TI → verificar que selector desaparece
```
</verification>

<success_criteria>
- UsuariosPage.jsx y AdminUsuariosPage.jsx tienen ROLES extendidos con 14 roles incluyendo los 7 nuevos
- Selector "Área de Soporte" visible en el formulario solo cuando rol es RESPONSABLE_*
- Selector oculto para todos los demás roles
- Al cambiar rol de RESPONSABLE_* a otro, el campo areaSoporteId se resetea a null
- Payload de creación/edición incluye areaSoporteId para RESPONSABLE_*, null para otros roles
- getAreasSoporte() disponible en catalogos.js (para UsuariosPage) y admin.js (para AdminUsuariosPage)
- RESPONSABLE_* recibe notificaciones ticket:nuevo via room admins (join:admin emitido)
</success_criteria>

<output>
After completion, create `.planning/phases/03-roles-y-areas-de-soporte/03-04-SUMMARY.md`
</output>
