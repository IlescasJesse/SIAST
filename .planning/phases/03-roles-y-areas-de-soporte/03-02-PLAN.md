---
phase: 03-roles-y-areas-de-soporte
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - packages/database/prisma/seed.ts
  - packages/database/prisma/seed_procesos.ts
autonomous: true
requirements:
  - ROL-02
  - ROL-03

must_haves:
  truths:
    - "La migración de Prisma aplica el nuevo enum (14 valores) y crea la tabla areas_soporte con FK en usuarios"
    - "El seed crea 4 AreaSoporte: TI, REDES, MANTENIMIENTO, RECURSOS_MATERIALES"
    - "Cada AreaSoporte tiene sus subcategorias y rolesIncluidos correctos (según D-05)"
    - "El seed de ProcesoDefinicion incluye entradas para SANITARIOS, ILUMINACION y MOVILIDAD"
    - "Las entradas SANITARIOS/ILUMINACION/MOVILIDAD usan TECNICO_PLOMERO, TECNICO_ELECTRICISTA, TECNICO_MOVILIDAD respectivamente"
    - "El Prisma client está regenerado (db:generate) y tiene los tipos AreaSoporte"
  artifacts:
    - path: "packages/database/prisma/seed.ts"
      provides: "Seed upsert de 4 AreaSoporte en el orden correcto (después de areaEdificio, antes de admin usuario)"
      contains: "areaSoporte.upsert"
    - path: "packages/database/prisma/seed_procesos.ts"
      provides: "3 nuevas entradas en PROCESO_SEED_MAP para subcategorías de MANTENIMIENTO"
      contains: "TECNICO_PLOMERO"
    - path: "packages/database/prisma/migrations/"
      provides: "Migración con ALTER TABLE usuarios MODIFY rol ENUM + CREATE TABLE areas_soporte"
      contains: "areas_soporte"
  key_links:
    - from: "packages/database/prisma/schema.prisma"
      to: "packages/database/prisma/migrations/"
      via: "prisma migrate dev genera SQL desde el diff de schema"
      pattern: "RESPONSABLE_TI.*RESPONSABLE_REDES"
    - from: "packages/database/prisma/seed_procesos.ts"
      to: "ProcesoDefinicion tabla en DB"
      via: "seedProcesos() upsert por {subcategoria, subTipo}"
      pattern: "SANITARIOS.*TECNICO_PLOMERO"
---

<objective>
Aplicar la migración de base de datos (Prisma migrate dev), regenerar el client de Prisma, y actualizar los archivos de seed con las 4 AreaSoporte y los 3 nuevos ProcesoDefinicion de MANTENIMIENTO.

Purpose: La migración hace que los cambios de schema del Plan 01 sean efectivos en MySQL. El seed garantiza datos de configuración necesarios para que el backend (Plan 03) funcione correctamente.

Output:
- Migración aplicada en MySQL (ALTER TABLE usuarios MODIFY rol ENUM + CREATE TABLE areas_soporte + ALTER TABLE usuarios ADD COLUMN area_soporte_id)
- Prisma client regenerado con tipos AreaSoporte y campo areaSoporteId en Usuario
- seed.ts con 4 AreaSoporte upsertados idempotentemente
- seed_procesos.ts con entradas SANITARIOS, ILUMINACION, MOVILIDAD
- seed ejecutado exitosamente
</objective>

<execution_context>
@C:/Users/ilesm/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ilesm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-roles-y-areas-de-soporte/03-CONTEXT.md
@.planning/phases/03-roles-y-areas-de-soporte/03-RESEARCH.md
@.planning/phases/03-roles-y-areas-de-soporte/03-PATTERNS.md
@.planning/phases/03-roles-y-areas-de-soporte/03-01-SUMMARY.md

<interfaces>
<!-- Patrón de upsert en seed.ts — exactamente como areaEdificio -->
Patrón actual de upsert en packages/database/prisma/seed.ts (líneas 257-263):
```typescript
for (const area of areas) {
  await prisma.areaEdificio.upsert({
    where: { id: area.id },
    update: area,
    create: area,
  });
}
console.log(`${areas.length} areas del edificio sincronizadas`);
```

Orden actual de llamadas en seed.ts:
1. deleteMany — datos transaccionales (NO incluir AreaSoporte aquí)
2. upsert areaEdificio  ← INSERTAR AreaSoporte seed DESPUÉS de este bloque
3. create admin usuario
4. seedProcesos(prisma)

Patrón DIRECTO en seed_procesos.ts — ejemplo RED_INTERNET (líneas 127-131):
```typescript
RED_INTERNET: {
  nombre: "Red / Internet",
  tipoFlujo: "DIRECTO",
  pasos: [{ orden: 1, rolRequerido: "TECNICO_REDES", nombre: "Atención por Redes" }],
},
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Aplicar migración Prisma y regenerar client</name>
  <read_first>
    packages/database/prisma/schema.prisma
  </read_first>
  <files>packages/database/prisma/migrations/</files>
  <action>
Verificar que MySQL/XAMPP esté corriendo antes de ejecutar los comandos.

Ejecutar la migración desde el directorio packages/database para que Prisma lea el schema.prisma modificado en el Plan 01 y genere el SQL de migración:

```bash
cd "C:/Users/ilesm/Documents/SIAST/packages/database" && npx prisma migrate dev --name roles-y-areas-soporte
```

Si el comando pide confirmación interactiva, responder "y". En entorno no-TTY usar:
```bash
cd "C:/Users/ilesm/Documents/SIAST/packages/database" && npx prisma migrate dev --name roles-y-areas-soporte --create-only && npx prisma migrate deploy
```

Luego regenerar el client de Prisma:
```bash
cd "C:/Users/ilesm/Documents/SIAST/packages/database" && npm run db:generate
```

O desde la raíz del monorepo:
```bash
cd "C:/Users/ilesm/Documents/SIAST" && npm run db:generate --workspace=packages/database
```

La migración generada DEBE incluir:
- `ALTER TABLE usuarios MODIFY rol ENUM(...)` con los 14 valores — verificar que TECNICO_SERVICIOS sigue presente
- `CREATE TABLE areas_soporte (...)` con columnas id, nombre, subcategorias JSON, rolesIncluidos JSON, activo, created_at
- `ALTER TABLE usuarios ADD COLUMN area_soporte_id INTEGER NULL`
- `ALTER TABLE usuarios ADD CONSTRAINT usuarios_area_soporte_id_fkey FOREIGN KEY (area_soporte_id) REFERENCES areas_soporte(id)`

Si la migración falla con error de TECNICO_SERVICIOS eliminado del enum, es porque schema.prisma fue modificado incorrectamente en Plan 01 — NO continuar sin corregir schema.prisma primero.
  </action>
  <verify>
    <automated>
      cd "C:/Users/ilesm/Documents/SIAST/packages/database" && npx prisma migrate status
    </automated>
  </verify>
  <acceptance_criteria>
    - `npx prisma migrate status` muestra "Database schema is up to date"
    - Existe un nuevo directorio en packages/database/prisma/migrations/ con nombre que contiene "roles_y_areas_soporte"
    - El SQL del nuevo migration file contiene "areas_soporte" (buscar con grep en el directorio de migraciones)
    - El SQL contiene "RESPONSABLE_TI" dentro del MODIFY ENUM
    - El SQL contiene "TECNICO_SERVICIOS" dentro del MODIFY ENUM (no fue eliminado)
    - El Prisma client fue regenerado: `ls packages/database/node_modules/.prisma/client/` contiene archivos recientes
  </acceptance_criteria>
  <done>Migración aplicada exitosamente en MySQL, tabla areas_soporte creada, columna area_soporte_id en usuarios, Prisma client con tipos actualizados</done>
</task>

<task type="auto">
  <name>Task 2: Actualizar seed.ts con 4 AreaSoporte</name>
  <read_first>
    packages/database/prisma/seed.ts
  </read_first>
  <files>packages/database/prisma/seed.ts</files>
  <action>
Leer el archivo completo para ubicar exactamente dónde están los bloques de areaEdificio y la creación del admin usuario.

Agregar el bloque de AreaSoporte seed DESPUÉS del upsert de areaEdificio y ANTES de la creación del admin usuario (para que el FK esté disponible si algún usuario de prueba lo necesita). El bloque NO debe estar en el deleteMany inicial.

```typescript
// ── Áreas de Soporte (Phase 3) ───────────────────────────────────────────────
const areasSoporteData = [
  {
    nombre: "TI",
    subcategorias: ["SISTEMAS_INSTITUCIONALES", "EQUIPOS_DISPOSITIVOS", "CUENTAS_DOMINIO", "CORREO_OUTLOOK"],
    rolesIncluidos: ["RESPONSABLE_TI", "TECNICO_TI"],
  },
  {
    nombre: "REDES",
    subcategorias: ["RED_INTERNET"],
    rolesIncluidos: ["RESPONSABLE_REDES", "TECNICO_REDES"],
  },
  {
    nombre: "MANTENIMIENTO",
    subcategorias: ["SANITARIOS", "ILUMINACION", "MOVILIDAD"],
    rolesIncluidos: ["RESPONSABLE_MANTENIMIENTO", "TECNICO_ELECTRICISTA", "TECNICO_PLOMERO", "TECNICO_MOVILIDAD"],
  },
  {
    nombre: "RECURSOS_MATERIALES",
    subcategorias: ["SALA_JUNTAS", "EQUIPO_AUDIOVISUAL", "PRESTAMO_EQUIPO", "MOBILIARIO", "PAPELERIA"],
    rolesIncluidos: ["RESPONSABLE_RECURSOS_MATERIALES", "GESTOR_RECURSOS_MATERIALES"],
  },
];

for (const area of areasSoporteData) {
  await prisma.areaSoporte.upsert({
    where: { nombre: area.nombre },
    update: { subcategorias: area.subcategorias, rolesIncluidos: area.rolesIncluidos },
    create: { ...area, activo: true },
  });
}
console.log(`${areasSoporteData.length} áreas de soporte sincronizadas`);
```

Notas críticas:
- La clave de upsert es `nombre` (unique), NO `id` (a diferencia de areaEdificio que usa `id`)
- El bloque update solo actualiza subcategorias y rolesIncluidos (no sobreescribe activo o createdAt)
- NO agregar `areaSoporte` al bloque deleteMany — es dato de configuración como areaEdificio
- Verificar que el import de `prisma` ya existe en el archivo (debe estar al inicio del archivo)
  </action>
  <verify>
    <automated>
      cd "C:/Users/ilesm/Documents/SIAST/packages/database" && npx tsx prisma/seed.ts 2>&1 | tail -20
    </automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx prisma/seed.ts` termina sin errores
    - La salida del seed incluye "4 áreas de soporte sincronizadas" (o similar)
    - `grep "areaSoporte.upsert" packages/database/prisma/seed.ts` encuentra la llamada
    - `grep "MANTENIMIENTO" packages/database/prisma/seed.ts` encuentra el área con subcategorias SANITARIOS, ILUMINACION, MOVILIDAD
    - El bloque deleteMany NO contiene "areaSoporte" (verificar con grep)
  </acceptance_criteria>
  <done>seed.ts con 4 AreaSoporte upsertadas por nombre. Seed ejecutable idempotentemente sin errores.</done>
</task>

<task type="auto">
  <name>Task 3: Agregar ProcesoDefinicion para SANITARIOS, ILUMINACION y MOVILIDAD en seed_procesos.ts</name>
  <read_first>
    packages/database/prisma/seed_procesos.ts
  </read_first>
  <files>packages/database/prisma/seed_procesos.ts</files>
  <action>
Leer el archivo completo para entender la estructura del `PROCESO_SEED_MAP` y ubicar dónde están las entradas existentes (TECNOLOGIAS, REDES).

Agregar las 3 nuevas entradas al `PROCESO_SEED_MAP` bajo las subcategorías de SERVICIOS (MANTENIMIENTO):

```typescript
// Estas 3 entradas se agregan al objeto PROCESO_SEED_MAP
// (después de las entradas de TECNOLOGIAS/REDES, antes del cierre del objeto)

SANITARIOS: {
  nombre: "Sanitarios y plomería",
  tipoFlujo: "DIRECTO",
  pasos: [
    { orden: 1, rolRequerido: "TECNICO_PLOMERO", nombre: "Atención por Técnico Plomero" },
  ],
},
ILUMINACION: {
  nombre: "Iluminación y electricidad",
  tipoFlujo: "DIRECTO",
  pasos: [
    { orden: 1, rolRequerido: "TECNICO_ELECTRICISTA", nombre: "Atención por Técnico Electricista" },
  ],
},
MOVILIDAD: {
  nombre: "Movilidad y accesibilidad",
  tipoFlujo: "DIRECTO",
  pasos: [
    { orden: 1, rolRequerido: "TECNICO_MOVILIDAD", nombre: "Atención por Técnico de Movilidad" },
  ],
},
```

Notas:
- Las claves `SANITARIOS`, `ILUMINACION`, `MOVILIDAD` corresponden a valores del enum `SubcategoriaTicket` — verificar que coinciden exactamente (case sensitive)
- Estos son subcategoría del tipo `SERVICIOS` (categoria del ticket) — el campo `subcategoria` en ProcesoDefinicion usará estos valores
- La key del PROCESO_SEED_MAP puede ser "SANITARIOS" si no tiene subTipo, o "SERVICIOS:SANITARIOS" si el map incluye la categoria — leer el archivo para entender el patrón exacto de las keys existentes
- El loop de upsert en `seedProcesos()` ya maneja estas entradas automáticamente — no cambiar el loop
- Las entradas son `tipoFlujo: "DIRECTO"` con 1 paso cada una, siguiendo el patrón de RED_INTERNET

Nota importante sobre el crearTicket bug (ver Plan 03):
- Agregar estas ProcesoDefinicion al seed es necesario pero no suficiente
- El bug en `tickets.service.ts` donde `if (categoriaVal === "TECNOLOGIAS")` excluye SERVICIOS tickets se corrige en Plan 03
- Este seed es la condición previa para que el fix de Plan 03 funcione
  </action>
  <verify>
    <automated>
      cd "C:/Users/ilesm/Documents/SIAST/packages/database" && npx tsx prisma/seed.ts 2>&1 | grep -E "proceso|Proceso|SANITARIOS|ILUMINACION|MOVILIDAD"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep "TECNICO_PLOMERO" packages/database/prisma/seed_procesos.ts` encuentra la entrada SANITARIOS
    - `grep "TECNICO_ELECTRICISTA" packages/database/prisma/seed_procesos.ts` encuentra la entrada ILUMINACION
    - `grep "TECNICO_MOVILIDAD" packages/database/prisma/seed_procesos.ts` encuentra la entrada MOVILIDAD
    - Seed ejecutado completo sin errores (verificado en Task 2, re-ejecutar si es necesario)
    - Las 3 entradas siguen el mismo patrón tipoFlujo: "DIRECTO" con pasos array de 1 elemento
  </acceptance_criteria>
  <done>seed_procesos.ts tiene 3 entradas nuevas para subcategorías de MANTENIMIENTO. Cada una mapea a su rol específico (TECNICO_PLOMERO, TECNICO_ELECTRICISTA, TECNICO_MOVILIDAD).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prisma migrate → MySQL | La migración aplica DDL directamente; un error puede dejar la tabla en estado inconsistente |
| seed.ts → DB | El seed usa upsert — idempotente, pero si la tabla no existe (migración fallida), el seed falla con FK error |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | Migración MySQL — MODIFY ENUM | mitigate | Revisar el SQL generado por Prisma antes de aplicar; verificar que TECNICO_SERVICIOS permanece en todos los valores del ENUM en el ALTER TABLE |
| T-03-02-02 | Denial of Service | FK constraint areas_soporte en usuarios | mitigate | La FK es nullable (area_soporte_id INT NULL) — no afecta usuarios existentes; el seed crea las áreas ANTES de cualquier usuario que las referencie |
| T-03-02-03 | Information Disclosure | AreaSoporte seed — rolesIncluidos JSON | accept | Los datos de AreaSoporte son configuración organizacional no sensible; acceso controlado por requireRol("ADMIN") en el endpoint GET /api/admin/areas-soporte |
</threat_model>

<verification>
```bash
# 1. Estado de migraciones
cd "C:/Users/ilesm/Documents/SIAST/packages/database" && npx prisma migrate status

# 2. Tabla areas_soporte existe en MySQL (usando Prisma Studio o query directa)
cd "C:/Users/ilesm/Documents/SIAST/packages/database" && npx prisma db execute --stdin <<'SQL'
SHOW TABLES LIKE 'areas_soporte';
SQL

# 3. Columna area_soporte_id existe en usuarios
cd "C:/Users/ilesm/Documents/SIAST/packages/database" && npx prisma db execute --stdin <<'SQL'
DESCRIBE usuarios;
SQL

# 4. Las 4 áreas están en la DB
cd "C:/Users/ilesm/Documents/SIAST/packages/database" && npx prisma db execute --stdin <<'SQL'
SELECT nombre FROM areas_soporte;
SQL

# 5. ProcesoDefinicion para MANTENIMIENTO existe
cd "C:/Users/ilesm/Documents/SIAST/packages/database" && npx prisma db execute --stdin <<'SQL'
SELECT subcategoria, nombre FROM proceso_definicion WHERE subcategoria IN ('SANITARIOS','ILUMINACION','MOVILIDAD');
SQL
```
</verification>

<success_criteria>
- `npx prisma migrate status` → "Database schema is up to date"
- Tabla `areas_soporte` existe en MySQL con columnas: id, nombre, subcategorias JSON, rolesIncluidos JSON, activo, created_at
- Columna `area_soporte_id` existe en tabla `usuarios` (nullable, FK a areas_soporte)
- 4 registros en `areas_soporte`: TI, REDES, MANTENIMIENTO, RECURSOS_MATERIALES
- 3 registros en `proceso_definicion` para SANITARIOS, ILUMINACION, MOVILIDAD
- Prisma client regenerado con tipos AreaSoporte disponibles en TypeScript
</success_criteria>

<output>
After completion, create `.planning/phases/03-roles-y-areas-de-soporte/03-02-SUMMARY.md`
</output>
