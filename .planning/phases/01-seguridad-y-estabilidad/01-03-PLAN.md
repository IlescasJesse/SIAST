---
phase: 01-seguridad-y-estabilidad
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - packages/shared/src/index.ts
  - packages/database/package.json
autonomous: true
requirements:
  - STB-01
  - STB-02
  - STB-03

must_haves:
  truths:
    - "El Prisma client está sincronizado con el schema actual (migración unify_tecnico_ti_role incluida)"
    - "La generación de folios usa los keys correctos para todas las subcategorías actuales"
    - "Los tickets de categoría TECNOLOGIAS reciben prefijos específicos (TEC-SIS, TEC-EQP, etc.) en lugar del fallback TIC"
    - "El directorio Building3D fue verificado y documentado (archivos stale eliminados si existen)"
  artifacts:
    - path: "packages/shared/src/index.ts"
      provides: "FOLIO_PREFIX con keys correctos para SubcategoriaTicket actual"
      contains: "TECNOLOGIAS-SISTEMAS_INSTITUCIONALES"
    - path: "packages/database/node_modules/.prisma/client/index.js"
      provides: "Prisma client generado con schema actual"
  key_links:
    - from: "packages/shared/src/index.ts (FOLIO_PREFIX)"
      to: "apps/api/src/services/tickets.service.ts (generarFolio)"
      via: "import FOLIO_PREFIX de @stf/shared"
      pattern: "FOLIO_PREFIX"
---

<objective>
Resolver la deuda técnica de estabilidad bloqueante: sincronizar el Prisma client con el schema actual, corregir el mapa FOLIO_PREFIX para que los folios de tickets usen los prefijos correctos según los enums actuales, y verificar/limpiar archivos stale en Building3D.

Purpose: El Prisma client desincronizado puede causar type errors o resultados undefined en campos de la migración reciente. El FOLIO_PREFIX stale hace que todos los tickets de tecnología caigan al fallback "TIC" en lugar de sus prefijos específicos — STB-01 y STB-02 son bloqueantes para la correcta operación del sistema de tickets. STB-03 es una verificación de limpieza.

Output: Prisma client regenerado. FOLIO_PREFIX con keys que coinciden con SubcategoriaTicket. Building3D verificado y limpio.
</objective>

<execution_context>
@C:\Users\ilesm\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\ilesm\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-seguridad-y-estabilidad/01-01-SUMMARY.md
@.planning/phases/01-seguridad-y-estabilidad/01-02-SUMMARY.md
</context>

<interfaces>
<!-- Estado actual de los archivos a modificar, extraído del codebase. -->

De packages/shared/src/index.ts — SubcategoriaTicket enum actual (líneas ~19-36):
```typescript
export const SubcategoriaTicketSchema = z.enum([
  // Tecnologías
  "SISTEMAS_INSTITUCIONALES",
  "EQUIPOS_DISPOSITIVOS",
  "RED_INTERNET",
  "CUENTAS_DOMINIO",
  "CORREO_OUTLOOK",
  // Servicios Generales
  "SANITARIOS",
  "ILUMINACION",
  "MOVILIDAD",
  // Recursos Materiales
  "SALA_JUNTAS",
  "EQUIPO_AUDIOVISUAL",
  "PRESTAMO_EQUIPO",
  "MOBILIARIO",
  "PAPELERIA",
]);
```

De packages/shared/src/index.ts — FOLIO_PREFIX actual (líneas ~400-414) con keys stale:
```typescript
export const FOLIO_PREFIX: Record<string, string> = {
  "TECNOLOGIAS-SISTEMAS": "TEC-SIS",              // ← KEY STALE
  "TECNOLOGIAS-SOPORTE_TECNICO": "TEC-SOP",       // ← KEY STALE
  "TECNOLOGIAS-IMPRESORAS": "TEC-IMP",            // ← KEY STALE, subcategoría eliminada
  "TECNOLOGIAS-REDES_INTERNET": "TEC-RED",        // ← KEY STALE
  "TECNOLOGIAS-CONFIGURACION_CORREO_OUTLOOK": "TEC-COR", // ← KEY STALE
  "SERVICIOS-SANITARIOS": "SER-SAN",              // ← correcto
  "SERVICIOS-ILUMINACION": "SER-ILU",             // ← correcto
  "SERVICIOS-MOVILIDAD": "SER-MOV",               // ← correcto
  "RECURSOS_MATERIALES-SALA_JUNTAS": "REC-SAL",   // ← correcto
  "RECURSOS_MATERIALES-EQUIPO_AUDIOVISUAL": "REC-AUD", // ← correcto
  "RECURSOS_MATERIALES-PRESTAMO_EQUIPO": "REC-PRE", // ← correcto
  "RECURSOS_MATERIALES-MOBILIARIO": "REC-MOB",    // ← correcto
  "RECURSOS_MATERIALES-PAPELERIA": "REC-PAP",     // ← correcto
};
```

La función generarFolio en tickets.service.ts usa:
```typescript
const key = `${categoria}-${subcategoria}`;
const prefix = FOLIO_PREFIX[key] ?? "TIC";  // "TIC" es el fallback cuando el key no coincide
```

Mapa de corrección requerido (D-17):
- "TECNOLOGIAS-SISTEMAS" → "TECNOLOGIAS-SISTEMAS_INSTITUCIONALES" (mismo prefix TEC-SIS)
- "TECNOLOGIAS-SOPORTE_TECNICO" → "TECNOLOGIAS-EQUIPOS_DISPOSITIVOS" (nuevo prefix TEC-EQP)
- "TECNOLOGIAS-IMPRESORAS" → ELIMINAR (no existe como subcategoría en el enum actual)
- "TECNOLOGIAS-REDES_INTERNET" → "TECNOLOGIAS-RED_INTERNET" (mismo prefix TEC-RED)
- "TECNOLOGIAS-CONFIGURACION_CORREO_OUTLOOK" → "TECNOLOGIAS-CORREO_OUTLOOK" (mismo prefix TEC-COR)
- AGREGAR "TECNOLOGIAS-CUENTAS_DOMINIO": "TEC-DOM" (faltaba completamente)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Regenerar Prisma client y corregir FOLIO_PREFIX</name>
  <files>packages/shared/src/index.ts, packages/database/package.json</files>

  <read_first>
    - packages/shared/src/index.ts — leer líneas 395-420 para ver el FOLIO_PREFIX exacto y confirmar los line numbers actuales antes de modificar
    - packages/shared/src/index.ts — leer líneas 15-40 para confirmar el SubcategoriaTicketSchema actual (los valores del enum que determinan los keys correctos)
    - packages/database/package.json — ver el script db:generate exacto para confirmar el comando a ejecutar
  </read_first>

  <action>
**Paso 1: Regenerar Prisma client (STB-01)**

Ejecutar desde la raíz del monorepo:
```bash
npm run db:generate --workspace=packages/database
```

Si el comando anterior falla (algunos versiones de npm con workspaces), alternativa:
```bash
cd packages/database && npm run db:generate
```

Esto regenera el cliente Prisma en `packages/database/node_modules/.prisma/client/` sincronizado con el schema actual que incluye la migración `20260429180311_unify_tecnico_ti_role`. El cliente generado es el que importa `apps/api` en runtime.

Verificar que el comando ejecutó sin errores (no hay un archivo de producción a modificar — solo confirmar que la ejecución es exitosa).

---

**Paso 2: Corregir FOLIO_PREFIX en packages/shared/src/index.ts (STB-02)**

Localizar el bloque `export const FOLIO_PREFIX` y reemplazarlo completamente con el mapa corregido:

```typescript
export const FOLIO_PREFIX: Record<string, string> = {
  // Tecnologías — keys corregidos para SubcategoriaTicket actual
  "TECNOLOGIAS-SISTEMAS_INSTITUCIONALES": "TEC-SIS",
  "TECNOLOGIAS-EQUIPOS_DISPOSITIVOS": "TEC-EQP",
  "TECNOLOGIAS-RED_INTERNET": "TEC-RED",
  "TECNOLOGIAS-CUENTAS_DOMINIO": "TEC-DOM",
  "TECNOLOGIAS-CORREO_OUTLOOK": "TEC-COR",
  // Servicios Generales
  "SERVICIOS-SANITARIOS": "SER-SAN",
  "SERVICIOS-ILUMINACION": "SER-ILU",
  "SERVICIOS-MOVILIDAD": "SER-MOV",
  // Recursos Materiales
  "RECURSOS_MATERIALES-SALA_JUNTAS": "REC-SAL",
  "RECURSOS_MATERIALES-EQUIPO_AUDIOVISUAL": "REC-AUD",
  "RECURSOS_MATERIALES-PRESTAMO_EQUIPO": "REC-PRE",
  "RECURSOS_MATERIALES-MOBILIARIO": "REC-MOB",
  "RECURSOS_MATERIALES-PAPELERIA": "REC-PAP",
};
```

**Cambios respecto al mapa anterior:**
- "TECNOLOGIAS-SISTEMAS" → "TECNOLOGIAS-SISTEMAS_INSTITUCIONALES" (key corregido, mismo prefix TEC-SIS)
- "TECNOLOGIAS-SOPORTE_TECNICO" → "TECNOLOGIAS-EQUIPOS_DISPOSITIVOS" (key corregido, nuevo prefix TEC-EQP)
- "TECNOLOGIAS-IMPRESORAS" → eliminado (subcategoría inexistente en enum actual)
- "TECNOLOGIAS-REDES_INTERNET" → "TECNOLOGIAS-RED_INTERNET" (key corregido, mismo prefix TEC-RED)
- "TECNOLOGIAS-CONFIGURACION_CORREO_OUTLOOK" → "TECNOLOGIAS-CORREO_OUTLOOK" (key corregido, mismo prefix TEC-COR)
- NUEVO: "TECNOLOGIAS-CUENTAS_DOMINIO": "TEC-DOM" (faltaba completamente)

**Nota sobre tickets existentes:** Los tickets ya en DB tienen folios como strings (`TIC-0001`, etc.). El cambio solo afecta la generación de folios NUEVOS. No hay migración de datos requerida.
  </action>

  <verify>
    <automated>
      node -e "
      const fs = require('fs');
      const shared = fs.readFileSync('packages/shared/src/index.ts','utf8');
      const checks = [
        [shared.includes('TECNOLOGIAS-SISTEMAS_INSTITUCIONALES'), 'shared: key SISTEMAS_INSTITUCIONALES correcto'],
        [shared.includes('TECNOLOGIAS-EQUIPOS_DISPOSITIVOS'), 'shared: key EQUIPOS_DISPOSITIVOS correcto'],
        [shared.includes('TECNOLOGIAS-RED_INTERNET'), 'shared: key RED_INTERNET correcto'],
        [shared.includes('TECNOLOGIAS-CUENTAS_DOMINIO'), 'shared: key CUENTAS_DOMINIO agregado'],
        [shared.includes('TECNOLOGIAS-CORREO_OUTLOOK'), 'shared: key CORREO_OUTLOOK correcto'],
        [!shared.includes('TECNOLOGIAS-SISTEMAS\"'), 'shared: key stale SISTEMAS eliminado'],
        [!shared.includes('TECNOLOGIAS-SOPORTE_TECNICO'), 'shared: key stale SOPORTE_TECNICO eliminado'],
        [!shared.includes('TECNOLOGIAS-IMPRESORAS'), 'shared: key stale IMPRESORAS eliminado'],
        [!shared.includes('TECNOLOGIAS-REDES_INTERNET'), 'shared: key stale REDES_INTERNET eliminado'],
        [!shared.includes('TECNOLOGIAS-CONFIGURACION_CORREO_OUTLOOK'), 'shared: key stale CONFIGURACION_CORREO eliminado'],
        [shared.includes('TEC-EQP'), 'shared: prefix TEC-EQP para EQUIPOS_DISPOSITIVOS'],
        [shared.includes('TEC-DOM'), 'shared: prefix TEC-DOM para CUENTAS_DOMINIO'],
      ];
      checks.forEach(([ok,msg]) => console.log(ok ? 'PASS:' : 'FAIL:', msg));
      const failed = checks.filter(([ok]) => !ok);
      if (failed.length) process.exit(1);
      "
    </automated>
  </verify>

  <acceptance_criteria>
    - El comando `npm run db:generate --workspace=packages/database` ejecuta sin errores
    - packages/shared/src/index.ts contiene `"TECNOLOGIAS-SISTEMAS_INSTITUCIONALES"`
    - packages/shared/src/index.ts contiene `"TECNOLOGIAS-EQUIPOS_DISPOSITIVOS"`
    - packages/shared/src/index.ts contiene `"TECNOLOGIAS-CUENTAS_DOMINIO"` (era inexistente)
    - packages/shared/src/index.ts contiene `"TECNOLOGIAS-RED_INTERNET"` (sin el "ES" extra)
    - packages/shared/src/index.ts contiene `"TECNOLOGIAS-CORREO_OUTLOOK"` (sin "CONFIGURACION_")
    - packages/shared/src/index.ts NO contiene `"TECNOLOGIAS-SISTEMAS"` seguido de `"` (el key stale)
    - packages/shared/src/index.ts NO contiene `"TECNOLOGIAS-SOPORTE_TECNICO"`
    - packages/shared/src/index.ts NO contiene `"TECNOLOGIAS-IMPRESORAS"`
    - packages/shared/src/index.ts NO contiene `"TECNOLOGIAS-REDES_INTERNET"`
    - packages/shared/src/index.ts NO contiene `"TECNOLOGIAS-CONFIGURACION_CORREO_OUTLOOK"`
  </acceptance_criteria>

  <done>Prisma client sincronizado con schema actual. FOLIO_PREFIX corregido: 5 keys stale eliminados/actualizados, 1 key nuevo (CUENTAS_DOMINIO) agregado. Los tickets nuevos de tecnología generarán prefijos específicos (TEC-SIS, TEC-EQP, TEC-RED, TEC-DOM, TEC-COR) en lugar del fallback TIC.</done>
</task>

<task type="auto">
  <name>Task 2: Verificar y limpiar archivos stale de Building3D</name>
  <files>apps/web/src/components/Building3D/</files>

  <read_first>
    - apps/web/src/components/Building3D/ — listar todos los archivos del directorio para identificar cuáles existen actualmente
    - apps/web/src/components/Building3D/BuildingViewer.jsx — leer el archivo para confirmar que es el único archivo legítimo del directorio y verificar su estructura actual
  </read_first>

  <action>
**Paso 1: Auditar el directorio**

Listar TODOS los archivos en `apps/web/src/components/Building3D/` incluyendo archivos ocultos y cualquier extensión:

```bash
# En la raíz del proyecto
ls -la apps/web/src/components/Building3D/
```

**Paso 2: Evaluar qué es stale**

Según la investigación (RESEARCH.md, Pitfall 5), la expectativa es que el directorio contenga ÚNICAMENTE `BuildingViewer.jsx`. 

Un archivo es stale si cumple alguno de estos criterios:
- Extensión `.old`, `.bak`, `.backup`, `.draft`, `.tmp`
- Nombre que contiene `old`, `backup`, `draft`, `v1`, `v2`, `copy`
- Archivos `.jsx`/`.js` que no son importados por ningún otro archivo en `apps/web/src/`
- Archivos de configuración o test abandonados

**Paso 3: Actuar según lo encontrado**

**Escenario A** — Solo existe `BuildingViewer.jsx`:
- STB-03 es no-op.
- Documentar en el SUMMARY: "STB-03 verificado: directorio Building3D contiene únicamente BuildingViewer.jsx — no hay archivos stale."
- No eliminar nada.

**Escenario B** — Existen archivos adicionales:
- Para cada archivo adicional:
  1. Verificar si está importado en algún otro archivo de `apps/web/src/` con: `grep -r "NombreArchivo" apps/web/src/`
  2. Si NO está importado en ningún lado → es stale → eliminar
  3. Si SÍ está importado → NO es stale → dejar y documentar
- Documentar en el SUMMARY la lista de archivos encontrados y la acción tomada por cada uno.

**Nota:** NO eliminar `BuildingViewer.jsx` bajo ninguna circunstancia — es el componente activo que renderiza el iframe del visor 3D.
  </action>

  <verify>
    <automated>
      node -e "
      const fs = require('fs');
      const path = require('path');
      const dir = 'apps/web/src/components/Building3D';
      try {
        const files = fs.readdirSync(dir);
        const staleExtensions = ['.old', '.bak', '.backup', '.draft', '.tmp'];
        const hasStaleExtensions = files.some(f => staleExtensions.some(ext => f.endsWith(ext)));
        console.log('Files in Building3D:', files.join(', '));
        console.log(hasStaleExtensions ? 'FAIL: hay archivos con extensiones stale' : 'PASS: sin extensiones stale');
        console.log(files.includes('BuildingViewer.jsx') ? 'PASS: BuildingViewer.jsx existe' : 'FAIL: BuildingViewer.jsx no encontrado');
      } catch(e) {
        console.log('ERROR al leer directorio:', e.message);
        process.exit(1);
      }
      "
    </automated>
  </verify>

  <acceptance_criteria>
    - El directorio `apps/web/src/components/Building3D/` fue listado y auditado
    - `BuildingViewer.jsx` existe en el directorio (no fue eliminado)
    - No existen archivos con extensiones `.old`, `.bak`, `.backup`, `.draft`, `.tmp` en el directorio
    - El SUMMARY documenta el resultado del audit: qué archivos se encontraron y qué acción se tomó
  </acceptance_criteria>

  <done>Building3D auditado. Si solo existía BuildingViewer.jsx, STB-03 documentado como no-op. Si había archivos stale no importados, fueron eliminados. El estado final está documentado en el SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| packages/shared → apps/api | FOLIO_PREFIX es importado por tickets.service.ts en el API — datos incorrectos causan folios mal generados |
| packages/database → apps/api | Prisma client desincronizado puede causar type errors silenciosos en runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | packages/shared/src/index.ts (FOLIO_PREFIX) | accept | Los keys stale no son una vulnerabilidad de seguridad — son un bug de folio generation que produce folios con prefijo TIC en lugar de TEC-SIS, etc. El impacto es operacional, no de seguridad. La corrección (D-17) los actualiza. |
| T-03-02 | Tampering | packages/database (Prisma client) | accept | Un Prisma client desincronizado puede omitir campos de la migración reciente causando errores silenciosos. Esto es un bug de estabilidad, no seguridad. La regeneración (D-16) lo resuelve. |
| T-03-03 | Information Disclosure | apps/web/src/components/Building3D/ | accept | Archivos stale en el frontend no son servidos directamente — Vite solo sirve lo que se importa. Si hay archivos `.old` con datos sensibles podría ser un riesgo, pero Building3D solo contiene componentes JSX sin datos sensibles. Riesgo aceptado como bajo. |
</threat_model>

<verification>
Verificación de la fase completa para este plan:

```bash
# 1. Verificar que los keys stale no existen en FOLIO_PREFIX
grep -v "^//" packages/shared/src/index.ts | grep "FOLIO_PREFIX" -A 20 | grep "TECNOLOGIAS-SISTEMAS\""
# Expected: sin output (key eliminado)

grep -c "TECNOLOGIAS-SISTEMAS_INSTITUCIONALES" packages/shared/src/index.ts
# Expected: 1

grep -c "TECNOLOGIAS-CUENTAS_DOMINIO" packages/shared/src/index.ts
# Expected: 1

# 2. Verificar Prisma client generado
ls packages/database/node_modules/.prisma/client/index.js
# Expected: el archivo existe (o equivalent en la versión de Prisma)
```
</verification>

<success_criteria>
- `npm run db:generate --workspace=packages/database` ejecutó exitosamente (Prisma client sincronizado)
- `packages/shared/src/index.ts` tiene el FOLIO_PREFIX con los 13 keys correctos (5 tecnologías, 3 servicios, 5 recursos materiales)
- Los 5 keys stale de tecnología fueron eliminados/corregidos
- El key faltante `TECNOLOGIAS-CUENTAS_DOMINIO` fue agregado
- Building3D auditado — estado documentado en SUMMARY
- STB-01, STB-02, STB-03 satisfechos
</success_criteria>

<output>
Después de completar, crear `.planning/phases/01-seguridad-y-estabilidad/01-03-SUMMARY.md`
</output>
