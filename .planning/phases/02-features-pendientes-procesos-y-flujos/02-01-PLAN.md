---
phase: 02-features-pendientes-procesos-y-flujos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/database/prisma/seed_procesos.ts
autonomous: true
requirements:
  - PRO-02
  - PRO-03

must_haves:
  truths:
    - "La tabla ProcesoDefinicion contiene entradas para SISTEMAS_INSTITUCIONALES:SIRH y SISTEMAS_INSTITUCIONALES:SIAST"
    - "Ambos procesos tienen tipoFlujo DIRECTO y 1 PasoDefinicion con rolRequerido TECNICO_TI"
    - "El seed es idempotente: ejecutar dos veces no duplica ni rompe datos"
  artifacts:
    - path: "packages/database/prisma/seed_procesos.ts"
      provides: "Definiciones SIRH y SIAST en PROCESO_MAP local"
      contains: "SISTEMAS_INSTITUCIONALES:SIRH"
  key_links:
    - from: "packages/database/prisma/seed_procesos.ts"
      to: "prisma.procesoDefinicion"
      via: "seedProcesos(prisma) llamado desde seed.ts"
      pattern: "SISTEMAS_INSTITUCIONALES.*SIRH"
---

<objective>
Agregar las definiciones de proceso SIRH y SIAST a `seed_procesos.ts` para que la tabla
`ProcesoDefinicion` las tenga como flujos `DIRECTO` con 1 paso `TECNICO_TI`.

Purpose: El plan 02-02 va a reemplazar `getProcesoInfo()` por query DB — si SIRH/SIAST no
están en DB con `tipoFlujo: "DIRECTO"`, los tickets de esas subcategorías crearán sin pasos.
Output: `seed_procesos.ts` actualizado; al correr `npm run db:seed` se insertan/actualizan las
dos entradas.
</objective>

<execution_context>
@C:/Users/ilesm/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ilesm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/ilesm/Documents/SIAST/.planning/PROJECT.md
@C:/Users/ilesm/Documents/SIAST/.planning/ROADMAP.md
@C:/Users/ilesm/Documents/SIAST/.planning/STATE.md
</context>

<interfaces>
<!-- Contrato existente en seed_procesos.ts (líneas 1-92) -->

```typescript
// Importa PROCESO_MAP de @stf/shared y la itera
// La función seedProcesos(prisma) hace findFirst + update/create + deleteMany(pasos) + createMany(pasos)
// La clave del PROCESO_MAP es "SUBCATEGORIA" o "SUBCATEGORIA:SUBTIPO"
export async function seedProcesos(prisma: PrismaClient): Promise<void>

// Estructura de PROCESO_MAP esperada:
type ProcesoInfo = {
  nombre: string;
  tipoFlujo: "DIRECTO" | "SECUENCIAL" | "PENDIENTE";
  descripcion?: string;
  pasos: Array<{
    orden: number;
    rolRequerido: string;
    nombre: string;
    descripcion?: string;
    registraUnidades?: boolean;
    labelUnidades?: string;
  }>;
}
```

<!-- Estado actual del PROCESO_MAP en packages/shared/src/index.ts (líneas 599-611) -->

```typescript
// Actualmente en PROCESO_MAP — entradas PENDIENTE a reemplazar:
"SISTEMAS_INSTITUCIONALES:SIRH": {
  nombre: "Soporte SIRH",
  tipoFlujo: "PENDIENTE",
  descripcion: "Personal dedicado SIRH — proceso pendiente de definir.",
  pasos: [],
},
"SISTEMAS_INSTITUCIONALES:SIAST": {
  nombre: "Soporte SIAST",
  tipoFlujo: "PENDIENTE",
  descripcion: "Personal dedicado SIAST — proceso pendiente de definir.",
  pasos: [],
},
```
</interfaces>

<tasks>

<task type="auto">
  <name>Tarea 1: Actualizar PROCESO_MAP en @stf/shared con SIRH y SIAST como DIRECTO</name>
  <files>packages/shared/src/index.ts</files>
  <read_first>
    - packages/shared/src/index.ts (líneas 596-622 — entradas SISTEMAS_INSTITUCIONALES actuales y helpers)
    - packages/database/prisma/seed_procesos.ts (línea 1-92 — cómo itera PROCESO_MAP)
  </read_first>
  <action>
En `packages/shared/src/index.ts`, localizar las dos entradas actuales con `tipoFlujo: "PENDIENTE"`:

```
"SISTEMAS_INSTITUCIONALES:SIRH": { ... tipoFlujo: "PENDIENTE", pasos: [] }
"SISTEMAS_INSTITUCIONALES:SIAST": { ... tipoFlujo: "PENDIENTE", pasos: [] }
```

Reemplazarlas por (per D-05, D-07):

```typescript
"SISTEMAS_INSTITUCIONALES:SIRH": {
  nombre: "Soporte SIRH",
  tipoFlujo: "DIRECTO",
  descripcion: "Atención de soporte técnico para el sistema SIRH.",
  pasos: [
    {
      orden: 1,
      rolRequerido: "TECNICO_TI",
      nombre: "Atención por Soporte TI",
      descripcion: "Atención directa por técnico TI al usuario del SIRH.",
      registraUnidades: false,
    },
  ],
},
"SISTEMAS_INSTITUCIONALES:SIAST": {
  nombre: "Soporte SIAST",
  tipoFlujo: "DIRECTO",
  descripcion: "Atención de soporte técnico para el sistema SIAST.",
  pasos: [
    {
      orden: 1,
      rolRequerido: "TECNICO_TI",
      nombre: "Atención por Soporte TI",
      descripcion: "Atención directa por técnico TI al usuario del SIAST.",
      registraUnidades: false,
    },
  ],
},
```

No eliminar `PROCESO_MAP` ni los helpers (`getProcesoKey`, `getProcesoInfo`) en este plan — eso
es responsabilidad del plan 02-04.
  </action>
  <verify>
    <automated>node -e "const s = require('fs').readFileSync('packages/shared/src/index.ts','utf8'); const hasSIRH = s.includes('\"SISTEMAS_INSTITUCIONALES:SIRH\"') && s.includes('tipoFlujo: \"DIRECTO\"') && s.includes('Atenci'); const hasSIAST = s.includes('\"SISTEMAS_INSTITUCIONALES:SIAST\"'); console.log('SIRH DIRECTO:', hasSIRH, '| SIAST presente:', hasSIAST);"</automated>
  </verify>
  <acceptance_criteria>
    - `packages/shared/src/index.ts` contiene `"SISTEMAS_INSTITUCIONALES:SIRH"` con `tipoFlujo: "DIRECTO"`
    - `packages/shared/src/index.ts` contiene `"SISTEMAS_INSTITUCIONALES:SIAST"` con `tipoFlujo: "DIRECTO"`
    - Ambas entradas tienen array `pasos` con 1 elemento: `orden: 1`, `rolRequerido: "TECNICO_TI"`, `nombre: "Atención por Soporte TI"`
    - Las entradas anteriores con `tipoFlujo: "PENDIENTE"` y `pasos: []` ya no existen en el archivo
  </acceptance_criteria>
  <done>PROCESO_MAP actualizado con SIRH y SIAST como flujos DIRECTO con 1 paso TECNICO_TI</done>
</task>

<task type="auto">
  <name>Tarea 2: Ejecutar seed para poblar ProcesoDefinicion en DB</name>
  <files>packages/database/prisma/seed.ts</files>
  <read_first>
    - packages/database/prisma/seed.ts (líneas 1-50 — confirmar que seedProcesos ya se llama)
    - packages/database/prisma/seed_procesos.ts (líneas 1-92 — lógica idempotente existente)
  </read_first>
  <action>
Verificar que `seed.ts` ya llama a `seedProcesos(prisma)` en el flujo principal. Según el
archivo leído, la importación ya existe (`import { seedProcesos } from "./seed_procesos.js"`).

Si la llamada `await seedProcesos(prisma)` NO está en `main()`, agregarla después del bloque
de upsert de áreas:

```typescript
// Después del bloque de áreas (aprox línea 150):
// ──────────────────────────────────────────────────────────────
// 3. PROCESOS Y PASOS
// ──────────────────────────────────────────────────────────────
await seedProcesos(prisma);
console.log("Procesos y pasos sembrados desde PROCESO_MAP.");
```

Luego ejecutar el seed desde la raíz del monorepo:

```bash
cd C:/Users/ilesm/Documents/SIAST && npm run db:seed
```

Si el seed falla por error de conexión MySQL, anotar el error en el SUMMARY y continuar —
los cambios al código son correctos independientemente de si MySQL está activo.
  </action>
  <verify>
    <automated>cd C:/Users/ilesm/Documents/SIAST && npm run db:seed 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Seed corre sin error TypeScript (errores de conexión DB son aceptables si MySQL no está activo)
    - Salida del seed incluye línea `[UPDATED] SISTEMAS_INSTITUCIONALES:SIRH` o `[CREATED] SISTEMAS_INSTITUCIONALES:SIRH`
    - Salida del seed incluye línea `[UPDATED] SISTEMAS_INSTITUCIONALES:SIAST` o `[CREATED] SISTEMAS_INSTITUCIONALES:SIAST`
    - Si MySQL no está disponible: el script compila y falla solo en conexión, no en lógica TypeScript
  </acceptance_criteria>
  <done>ProcesoDefinicion en DB tiene entradas SIRH y SIAST con tipoFlujo DIRECTO y 1 PasoDefinicion TECNICO_TI</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed script → MySQL | Datos de inicialización — datos hardcodeados en código, no input externo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | seed_procesos.ts — deleteMany(pasos) | accept | Seed corre solo con credenciales de DB; no expuesto vía HTTP; idempotente |
| T-02-01-02 | Denial of Service | seed.ts — deleteMany usuarios/tickets | accept | Script de inicialización — requiere acceso directo al entorno; documentado |
</threat_model>

<verification>
Después de ejecutar:
1. `npm run db:seed` imprime líneas CREATED/UPDATED para SIRH y SIAST
2. En Prisma Studio (`npm run db:studio`): tabla `procesos_definicion` muestra registros SIRH y SIAST con `tipo_flujo = DIRECTO`
3. Tabla `pasos_definicion` muestra 1 paso por proceso con `rol_requerido = TECNICO_TI`
</verification>

<success_criteria>
- `PROCESO_MAP` en `packages/shared/src/index.ts` define SIRH y SIAST con `tipoFlujo: "DIRECTO"` y 1 paso `TECNICO_TI`
- `npm run db:seed` ejecuta sin errores TypeScript y reporta CREATED/UPDATED para ambas entradas
- DB tiene ProcesoDefinicion para `SISTEMAS_INSTITUCIONALES:SIRH` y `SISTEMAS_INSTITUCIONALES:SIAST` con tipoFlujo `DIRECTO`
</success_criteria>

<output>
Después de completar, crear `.planning/phases/02-features-pendientes-procesos-y-flujos/02-01-SUMMARY.md`
</output>
