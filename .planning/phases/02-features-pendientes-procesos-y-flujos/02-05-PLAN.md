---
phase: 02-features-pendientes-procesos-y-flujos
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/services/tickets.service.ts
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [PRO-04, NOT-02]
gap_closure: true

must_haves:
  truths:
    - "PRO-04 aparece en ROADMAP.md bajo el Backlog con nota de diferimiento explícita"
    - "PRO-04 ya no figura como deliverable de Phase 2 en ROADMAP.md"
    - "REQUIREMENTS.md marca PRO-04 con nota de backlog/TBD"
    - "asignarPaso() crea una entrada en historialTicket registrando el técnico asignado al paso"
    - "El audit trail de asignación de paso es consultable via prisma.historialTicket"
  artifacts:
    - path: "apps/api/src/services/tickets.service.ts"
      provides: "historialTicket.create dentro de asignarPaso"
      contains: "historialTicket.create"
    - path: ".planning/ROADMAP.md"
      provides: "PRO-04 movido a Backlog con nota explicita"
    - path: ".planning/REQUIREMENTS.md"
      provides: "PRO-04 marcado como diferido/backlog"
  key_links:
    - from: "asignarPaso()"
      to: "prisma.historialTicket"
      via: "historialTicket.create con comentario de asignación de paso"
      pattern: "historialTicket\\.create"
---

<objective>
Cierra los 2 gaps del reporte de verificación de Phase 2.

Gap 1 (PRO-04 documental): PRO-04 (Escalamiento RM) está diferido en el plan 02-04 pero no tiene fase de aterrizaje en el roadmap. Se mueve explícitamente al Backlog en ROADMAP.md y REQUIREMENTS.md para que la decisión quede documentada y no quede flotando.

Gap 2 (NOT-02 código): `asignarPaso()` en tickets.service.ts emite Socket.IO y actualiza pasoTicket pero no crea entrada en historialTicket. El audit trail de la asignación de técnico a paso queda incompleto. Fix: agregar `prisma.historialTicket.create` dentro de `asignarPaso`.

Purpose: Phase 2 pasa de 12/14 a 14/14 verdades verificadas.
Output: ROADMAP.md + REQUIREMENTS.md actualizados, tickets.service.ts con audit trail completo.
</objective>

<execution_context>
@C:/Users/ilesm/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ilesm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

<interfaces>
<!-- Patrón historialTicket.create extraído de tickets.service.ts (múltiples usos en el mismo archivo) -->

```typescript
// Patrón existente — usar exactamente esta estructura
await prisma.historialTicket.create({
  data: {
    ticketId: id,                                          // number
    estadoAnterior: ticket.estado,                        // EstadoTicket | undefined
    estadoNuevo: body.estado as never,                    // EstadoTicket
    usuarioId: user.rol !== "EMPLEADO" ? user.id : undefined,  // number | undefined
    empleadoRfc: user.rol === "EMPLEADO" ? user.rfc : undefined, // string | undefined
    comentario: body.comentario,                          // string | undefined
  },
});

// En asignarPaso, el contexto disponible es:
// - ticketId: number (parámetro)
// - pasoId: number (parámetro)
// - tecnicoId: number (parámetro)
// - user: JwtPayload (parámetro)
// - paso: PasoTicket (ya consultado en línea 583)
// - tecnico: Usuario (ya consultado en línea 586)
// - estadoAnteriorTicket: EstadoTicket | string (ya obtenido en línea 595-597)
// - ticket: Ticket (ya actualizado en línea 605-613)
```

<!-- Estructura de asignarPaso (líneas 577-639 de tickets.service.ts) -->
<!-- La entrada de historialTicket debe ir DESPUÉS de prisma.ticket.update (línea 613)
     y ANTES de notif.emitirPasoAsignado (línea 616) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mover PRO-04 al Backlog en ROADMAP.md y REQUIREMENTS.md</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <action>
    En ROADMAP.md, Phase 2:
    1. En "### Deliverables" — eliminar la línea: "- Escalamiento Recursos Materiales desde MANTENIMIENTO_CORRECTIVO vinculado a paso SIAST"
    2. En "### UAT" — eliminar la línea: "- Escalamiento de mantenimiento correctivo genera paso en cola de SIAST"
    3. Al final de la sección "## Backlog (v2)", agregar una nueva entrada:
       "- **PRO-04 (diferido)**: Escalamiento Recursos Materiales desde MANTENIMIENTO_CORRECTIVO vinculado a paso SIAST — requiere diseño de flujo entre sistemas (SIAST-MANTENIMIENTO); fase TBD"

    En REQUIREMENTS.md, en la línea de PRO-04:
    - Cambiar: `- [ ] **PRO-04**: Escalamiento Recursos Materiales desde \`MANTENIMIENTO_CORRECTIVO\` vinculado`
    - Por: `- [ ] **PRO-04** _(backlog / fase TBD)_: Escalamiento Recursos Materiales desde \`MANTENIMIENTO_CORRECTIVO\` vinculado — diferido por decisión en 02-CONTEXT.md; requiere diseño de flujo cross-sistema`

    En REQUIREMENTS.md, tabla Traceability:
    - Cambiar la fila `| PRO-01 a PRO-04 | Phase 2 | Pending |`
    - Por dos filas:
      ```
      | PRO-01 a PRO-03 | Phase 2 | Pending |
      | PRO-04          | Backlog  | Diferido (fase TBD) |
      ```
  </action>
  <verify>
    grep -c "Escalamiento de mantenimiento correctivo" .planning/ROADMAP.md
    # debe retornar 0
    grep "PRO-04" .planning/ROADMAP.md
    # debe mostrar la entrada en Backlog
    grep "PRO-04" .planning/REQUIREMENTS.md
    # debe mostrar nota "backlog / fase TBD"
  </verify>
  <done>
    - ROADMAP.md Phase 2 Deliverables y UAT no mencionan escalamiento RM
    - ROADMAP.md Backlog contiene entrada PRO-04 con nota de diferimiento
    - REQUIREMENTS.md PRO-04 tiene etiqueta "(backlog / fase TBD)"
    - REQUIREMENTS.md Traceability separa PRO-01/03 de PRO-04
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Agregar historialTicket.create en asignarPaso (NOT-02)</name>
  <files>apps/api/src/services/tickets.service.ts</files>
  <behavior>
    - Después de actualizar el ticket a EN_PROGRESO, asignarPaso registra en historialTicket que el técnico X fue asignado al paso Y
    - El comentario del historial identifica el paso por orden y nombre: "Paso {orden} — {nombre}: asignado a {tecnico.nombre} {tecnico.apellidos}"
    - El usuarioId del historial es el usuario que ejecutó la asignación (user.id — siempre un técnico o admin, nunca empleado)
    - El estadoAnterior es estadoAnteriorTicket y estadoNuevo es "EN_PROGRESO"
  </behavior>
  <action>
    En `apps/api/src/services/tickets.service.ts`, dentro de `asignarPaso` (líneas 577-639):

    Después de `prisma.ticket.update` (que termina en la línea ~613 con `include: ticketInclude,`) y ANTES de `notif.emitirPasoAsignado` (línea ~616), insertar:

    ```typescript
    // Audit trail — NOT-02: registrar asignación de técnico a paso en historialTicket
    await prisma.historialTicket.create({
      data: {
        ticketId,
        estadoAnterior: estadoAnteriorTicket as never,
        estadoNuevo: "EN_PROGRESO",
        usuarioId: user.id,
        comentario: `Paso ${paso.orden} — ${paso.nombre ?? `Paso ${paso.orden}`}: asignado a ${tecnico.nombre} ${tecnico.apellidos}`,
      },
    });
    ```

    No modificar nada más en la función. No cambiar la lógica de `emitirCambioEstado` — esa notificación ya existe y es correcta.
  </action>
  <verify>
    grep -n "historialTicket.create" apps/api/src/services/tickets.service.ts
    # debe mostrar la línea nueva dentro del bloque asignarPaso (alrededor de línea 614-620)
    cd apps/api && npx tsc --noEmit 2>&1 | head -20
    # debe retornar sin errores de TypeScript
  </verify>
  <done>
    - `historialTicket.create` presente dentro de asignarPaso (entre ticket.update y emitirPasoAsignado)
    - TypeScript compila sin errores
    - El comentario incluye número de paso, nombre y nombre del técnico asignado
    - usuarioId es el id del usuario que ejecuta la acción
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → asignarPaso | El ticketId, pasoId, tecnicoId vienen del request HTTP — ya validados por el controller antes de llegar al service |
| historialTicket.create → DB | Escritura interna al mismo prisma transaction context — datos provienen de objetos ya validados en el mismo service |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02gc-01 | Tampering | historialTicket.create en asignarPaso | accept | El comentario usa datos ya validados (paso.orden, paso.nombre, tecnico.nombre/apellidos) obtenidos de DB — no de input del usuario. Sin vector de inyección. |
| T-02gc-02 | Information Disclosure | historialTicket (audit trail) | accept | historialTicket solo accesible via endpoints autenticados con permisos de admin/técnico. Los datos de auditoría son apropiados para el rol que los consulta. |
| T-02gc-03 | Repudiation | Ausencia de historialTicket en asignarPaso | mitigate | Este es exactamente el gap que cierra este plan — agregar el historialTicket.create es la mitigación. |
| T-02gc-04 | Denial of Service | ROADMAP.md / REQUIREMENTS.md edits | accept | Archivos de planificación internos — no hay vector de ataque externo. |
</threat_model>

<verification>
Verificar cierre de gaps:

```bash
# Gap 1: PRO-04 movido al backlog
grep -c "Escalamiento de mantenimiento correctivo" .planning/ROADMAP.md
# esperado: 0

grep "PRO-04" .planning/ROADMAP.md
# esperado: línea en sección Backlog con nota de diferimiento

# Gap 2: historialTicket en asignarPaso
grep -n "historialTicket.create" apps/api/src/services/tickets.service.ts
# esperado: múltiples líneas incluyendo una en el rango ~614-620 (asignarPaso)

# TypeScript limpio
cd apps/api && npx tsc --noEmit 2>&1 | grep -c "error TS"
# esperado: 0
```
</verification>

<success_criteria>
- ROADMAP.md Phase 2 Deliverables y UAT no contienen referencias a escalamiento RM
- ROADMAP.md Backlog tiene entrada PRO-04 con nota de diferimiento explícita
- REQUIREMENTS.md PRO-04 marcado como backlog/fase TBD
- `asignarPaso()` en tickets.service.ts llama `prisma.historialTicket.create` con comentario de asignación
- `npx tsc --noEmit` en apps/api retorna 0 errores
- Phase 2 pasa de 12/14 a 14/14 verdades verificadas
</success_criteria>

<output>
Después de completar, crear `.planning/phases/02-features-pendientes-procesos-y-flujos/02-05-SUMMARY.md` con:
- Gaps cerrados (PRO-04 y NOT-02)
- Cambios realizados en cada archivo
- Resultado de verificación TypeScript
</output>
