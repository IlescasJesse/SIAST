---
phase: 02-features-pendientes-procesos-y-flujos
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - apps/api/src/controllers/metricas.controller.ts
autonomous: true
requirements:
  - PRO-01

must_haves:
  truths:
    - "metricasProcesos lee ProcesoDefinicion desde DB — nunca usa PROCESO_MAP ni getProcesoKey"
    - "El nombre del proceso en la métrica viene de procesoDefinicion.nombre (DB), no del map en memoria"
    - "metricas.controller.ts no importa nada de PROCESO_MAP ni getProcesoKey de @stf/shared"
  artifacts:
    - path: "apps/api/src/controllers/metricas.controller.ts"
      provides: "Métricas de procesos usando DB como fuente de verdad"
      contains: "prisma.procesoDefinicion.findMany"
  key_links:
    - from: "apps/api/src/controllers/metricas.controller.ts metricasProcesos"
      to: "prisma.procesoDefinicion"
      via: "findFirst({ where: { subcategoria, subTipo } })"
      pattern: "procesoDefinicion\\.findFirst"
---

<objective>
Migrar `metricas.controller.ts` para que `metricasProcesos` lea el nombre del proceso desde
`ProcesoDefinicion` DB en lugar del `PROCESO_MAP` hardcodeado, y eliminar el import de
`getProcesoKey` y `PROCESO_MAP` de `@stf/shared`.

Purpose: Consistencia total — una única fuente de verdad para nombres de procesos.
Si un admin renombra un proceso en DB, las métricas lo reflejan inmediatamente.
Output: `metricas.controller.ts` sin referencias a `PROCESO_MAP` ni `getProcesoKey`.
</objective>

<execution_context>
@C:/Users/ilesm/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ilesm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/ilesm/Documents/SIAST/.planning/ROADMAP.md
@C:/Users/ilesm/Documents/SIAST/.planning/STATE.md
@C:/Users/ilesm/Documents/SIAST/.planning/phases/02-features-pendientes-procesos-y-flujos/02-01-SUMMARY.md
</context>

<interfaces>
<!-- Estado actual de metricas.controller.ts — secciones relevantes -->

```typescript
// Línea 5 — import a cambiar:
import { getProcesoKey, PROCESO_MAP } from "@stf/shared";
// Debe quedar eliminada esta línea completa

// Líneas 191-242 — loop metricasProcesos actual:
for (const grupo of grupos) {
  const key = getProcesoKey(grupo.subcategoria, grupo.sub_tipo);
  const procesoInfo = PROCESO_MAP[key];
  // ...
  resultado.push({
    proceso: key,
    nombre: procesoInfo?.nombre ?? key,  // <-- usa PROCESO_MAP
    // ...
  });
}

// DEBE QUEDAR (reemplazar la consulta a PROCESO_MAP dentro del loop):
for (const grupo of grupos) {
  // Query DB para nombre del proceso (D-03)
  const procesoDb = await prisma.procesoDefinicion.findFirst({
    where: {
      subcategoria: grupo.subcategoria as never,
      subTipo: grupo.sub_tipo ?? null,
    },
    select: { nombre: true },
  });
  const key = grupo.sub_tipo
    ? `${grupo.subcategoria}:${grupo.sub_tipo}`
    : grupo.subcategoria;
  // ...
  resultado.push({
    proceso: key,
    nombre: procesoDb?.nombre ?? key,  // <-- usa DB
    // ...
  });
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Tarea 1: Reemplazar PROCESO_MAP con query DB en metricasProcesos</name>
  <files>apps/api/src/controllers/metricas.controller.ts</files>
  <read_first>
    - apps/api/src/controllers/metricas.controller.ts (líneas 1-10 — imports actuales)
    - apps/api/src/controllers/metricas.controller.ts (líneas 165-252 — función metricasProcesos completa)
  </read_first>
  <action>
Realizar los siguientes cambios en `apps/api/src/controllers/metricas.controller.ts`:

**Cambio 1 — Línea 5, eliminar import (D-03 + D-04):**
Eliminar completamente la línea:
```typescript
import { getProcesoKey, PROCESO_MAP } from "@stf/shared";
```
El import restante de `@stf/shared` (línea 4 con tipos) se mantiene intacto.

**Cambio 2 — Loop metricasProcesos (D-03):**
Dentro de `metricasProcesos`, localizar el loop `for (const grupo of grupos)` (aprox línea 193).
Actualmente hace:
```typescript
const key = getProcesoKey(grupo.subcategoria, grupo.sub_tipo);
const procesoInfo = PROCESO_MAP[key];
// ...
resultado.push({
  proceso: key,
  nombre: procesoInfo?.nombre ?? key,
  // ...
```

Reemplazar por:
```typescript
// Construir la clave del proceso (reemplaza getProcesoKey — D-03)
const key = grupo.sub_tipo
  ? `${grupo.subcategoria}:${grupo.sub_tipo}`
  : grupo.subcategoria;

// Leer nombre desde DB en lugar de PROCESO_MAP (D-03)
const procesoDb = await prisma.procesoDefinicion.findFirst({
  where: {
    subcategoria: grupo.subcategoria as never,
    subTipo: grupo.sub_tipo ?? null,
  },
  select: { nombre: true },
});

// ...resto del loop sin cambios...

resultado.push({
  proceso: key,
  nombre: procesoDb?.nombre ?? key,
  // ...resto de campos sin cambios...
});
```

La lógica de cálculo de `tiempoPromedioHoras`, `resueltasATiempo`, `unidadesAtendidas` NO cambia.
Solo cambia la obtención del `nombre` del proceso.

NOTA: la función `getProcesoKey` se inline aquí manualmente. No hay que importarla.
  </action>
  <verify>
    <automated>cd C:/Users/ilesm/Documents/SIAST && npx tsc --noEmit -p apps/api/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/controllers/metricas.controller.ts` NO contiene `PROCESO_MAP`
    - `apps/api/src/controllers/metricas.controller.ts` NO contiene `getProcesoKey`
    - `apps/api/src/controllers/metricas.controller.ts` contiene `prisma.procesoDefinicion.findFirst(`
    - `apps/api/src/controllers/metricas.controller.ts` contiene `procesoDb?.nombre ?? key`
    - `npx tsc --noEmit` no reporta errores en `metricas.controller.ts`
  </acceptance_criteria>
  <done>metricasProcesos usa DB; imports de PROCESO_MAP y getProcesoKey eliminados; TypeScript compila</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request → metricasProcesos | Staff autenticado (ADMIN/MESA_AYUDA) consulta métricas |
| metricas query → ProcesoDefinicion | Lectura de tabla interna; no input del usuario |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Information Disclosure | metricasProcesos expone nombre de proceso por subcategoria/subTipo | accept | Solo accesible a roles ADMIN/MESA_AYUDA (validado por middleware de auth existente) |
| T-02-03-02 | Tampering | procesoDefinicion.findFirst sin activo:true puede devolver proceso inactivo | accept | El nombre fallback es la clave (`key`) — comportamiento aceptable; procesos inactivos no tienen tickets nuevos |
</threat_model>

<verification>
1. `npx tsc --noEmit -p apps/api/tsconfig.json` — sin errores en metricas.controller.ts
2. `grep "PROCESO_MAP\|getProcesoKey" apps/api/src/controllers/metricas.controller.ts` — sin resultados
3. `grep "procesoDefinicion.findFirst" apps/api/src/controllers/metricas.controller.ts` — muestra la nueva query
</verification>

<success_criteria>
- `metricas.controller.ts` sin ninguna referencia a `PROCESO_MAP` ni `getProcesoKey`
- Query `prisma.procesoDefinicion.findFirst` en el loop de metricasProcesos
- TypeScript compila sin errores
</success_criteria>

<output>
Después de completar, crear `.planning/phases/02-features-pendientes-procesos-y-flujos/02-03-SUMMARY.md`
</output>
