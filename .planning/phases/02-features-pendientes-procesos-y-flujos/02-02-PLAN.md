---
phase: 02-features-pendientes-procesos-y-flujos
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - apps/api/src/services/tickets.service.ts
autonomous: true
requirements:
  - PRO-01
  - NOT-01
  - NOT-02

must_haves:
  truths:
    - "crearTicket lee el proceso desde ProcesoDefinicion DB — nunca llama getProcesoInfo()"
    - "completarPaso no resuelve el ticket prematuramente si el siguiente paso ya está EN_PROGRESO"
    - "completarPaso valida que el usuario sea el técnico asignado al paso (no solo el rol)"
    - "cambiarEstado a RESUELTO falla con 400 si el ticket tiene pasos no completados"
    - "listarTickets para técnicos excluye tickets RESUELTO y CANCELADO por defecto"
    - "asignarTicket rechaza tickets que usan flujo de pasos"
    - "historialTicket usa el estado real del ticket como estadoAnterior (no hardcodeado)"
    - "Socket.IO emite ticket:paso_asignado y ticket:paso_listo en los flujos correctos"
  artifacts:
    - path: "apps/api/src/services/tickets.service.ts"
      provides: "Servicio central de tickets con DB como fuente de verdad para procesos"
      contains: "prisma.procesoDefinicion.findFirst"
  key_links:
    - from: "apps/api/src/services/tickets.service.ts crearTicket"
      to: "prisma.procesoDefinicion"
      via: "findFirst({ where: { subcategoria, subTipo, activo: true } })"
      pattern: "procesoDefinicion\\.findFirst"
    - from: "apps/api/src/services/tickets.service.ts completarPaso"
      to: "pasoTicket siguiente"
      via: "findFirst({ where: { orden: { gt: paso.orden }, estado: { not: 'COMPLETADO' } } })"
      pattern: "estado.*not.*COMPLETADO"
---

<objective>
Migrar `tickets.service.ts` para que use `ProcesoDefinicion` DB en lugar de `getProcesoInfo()`
y aplicar los 6 bug fixes del flujo de pasos definidos en CONTEXT.md.

Purpose: Es el cambio central de la fase — DB como única fuente de verdad para procesos.
Los bugs corrigen casos donde el flujo multi-técnico se rompe silenciosamente.
Output: `tickets.service.ts` sin dependencia de `PROCESO_MAP`; 6 guards nuevos activos.
</objective>

<execution_context>
@C:/Users/ilesm/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ilesm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/ilesm/Documents/SIAST/.planning/ROADMAP.md
@C:/Users/ilesm/Documents/SIAST/.planning/STATE.md
@C:/Users/ilesm/Documents/SIAST/.planning/phases/02-features-pendientes-procesos-y-flujos/02-01-SUMMARY.md
</context>

<interfaces>
<!-- Estado actual de tickets.service.ts — secciones relevantes -->

```typescript
// Línea 6 — importación a eliminar:
import { FOLIO_PREFIX, getProcesoInfo } from "@stf/shared";
// Debe quedar solo:
import { FOLIO_PREFIX } from "@stf/shared";

// Línea 79 — listarTickets técnico (bug D-11):
// ACTUAL: where.tecnicoId = user.id;  (sin filtro de estado)
// DEBE quedar:
where.tecnicoId = user.id;
where.estado = { notIn: ["RESUELTO", "CANCELADO"] };

// Línea 247 — crearTicket (D-01):
// ACTUAL: const proceso = getProcesoInfo(subcategoriaVal, body.subTipo);
// DEBE quedar (query async):
const proceso = await prisma.procesoDefinicion.findFirst({
  where: { subcategoria: subcategoriaVal, subTipo: body.subTipo ?? null, activo: true },
  include: { pasos: { orderBy: { orden: "asc" } } },
});

// Línea 325 — asignarTicket (D-12 — agregar guard antes de validar rol del técnico):
const pasosExistentes = await prisma.pasoTicket.findMany({ where: { ticketId: id } });
if (pasosExistentes.length > 0) {
  throw Object.assign(
    new Error("Este ticket usa flujo de pasos. Asignar técnico desde el panel de pasos."),
    { status: 400 },
  );
}

// Línea 383 — cambiarEstado (D-10 — agregar guard antes de actualizar estado):
if (body.estado === "RESUELTO") {
  const pasosPendientes = await prisma.pasoTicket.findMany({
    where: { ticketId: id, estado: { not: "COMPLETADO" } },
  });
  if (pasosPendientes.length > 0) {
    throw Object.assign(
      new Error("El ticket tiene pasos pendientes. Completa todos los pasos para resolver."),
      { status: 400 },
    );
  }
}

// Línea 475 — completarPaso (D-09 — agregar ANTES del check de rol):
if (paso.tecnicoId !== null && paso.tecnicoId !== user.id) {
  throw Object.assign(
    new Error("Solo el técnico asignado puede completar este paso"),
    { status: 403 },
  );
}

// Línea 493 — completarPaso siguientePaso (D-08 — cambiar query):
// ACTUAL: { ticketId, orden: paso.orden + 1, estado: "PENDIENTE" }
// DEBE quedar:
const siguientePaso = await prisma.pasoTicket.findFirst({
  where: { ticketId, orden: { gt: paso.orden }, estado: { not: "COMPLETADO" } },
  orderBy: { orden: "asc" },
});

// Línea 504-511 — completarPaso historial al resolver (D-13):
// ACTUAL: estadoAnterior: "EN_PROGRESO" (hardcodeado)
// Antes del update, leer estado real:
const ticketActual = await prisma.ticket.findUnique({
  where: { id: ticketId },
  select: { estado: true },
});
// Luego usar ticketActual!.estado como estadoAnterior en historialTicket y emitirCambioEstado
```
</interfaces>

<tasks>

<task type="auto">
  <name>Tarea 1: Migrar crearTicket a ProcesoDefinicion DB y aplicar bug fix D-11 en listarTickets</name>
  <files>apps/api/src/services/tickets.service.ts</files>
  <read_first>
    - apps/api/src/services/tickets.service.ts (líneas 1-100 — imports, listarTickets, ticketInclude)
    - apps/api/src/services/tickets.service.ts (líneas 230-300 — crearTicket bloque TECNOLOGIAS)
  </read_first>
  <action>
Realizar los siguientes cambios en `apps/api/src/services/tickets.service.ts`:

**Cambio 1 — Línea 6, import (D-01 + D-04):**
Cambiar:
```typescript
import { FOLIO_PREFIX, getProcesoInfo } from "@stf/shared";
```
Por:
```typescript
import { FOLIO_PREFIX } from "@stf/shared";
```

**Cambio 2 — Línea 79, listarTickets (D-11):**
Dentro del bloque `else if (user.rol === "TECNICO_TI" || ...)`, la línea actual es:
```typescript
where.tecnicoId = user.id;
```
Cambiar por:
```typescript
where.tecnicoId = user.id;
where.estado = { notIn: ["RESUELTO", "CANCELADO"] };
```
IMPORTANTE: este cambio solo aplica cuando NO hay `query.estado` explícito. El query override
(`if (query.estado) where.estado = query.estado;`) sigue en las líneas siguientes y tiene
precedencia por sobrescribir `where.estado`, lo cual es el comportamiento correcto: si el técnico
pasa `?estado=RESUELTO` explícitamente, puede ver su historial.

**Cambio 3 — Línea 247, crearTicket (D-01):**
La función `crearTicket` ya es async. Cambiar:
```typescript
const proceso = getProcesoInfo(subcategoriaVal, body.subTipo);
```
Por:
```typescript
const proceso = await prisma.procesoDefinicion.findFirst({
  where: { subcategoria: subcategoriaVal as never, subTipo: body.subTipo ?? null, activo: true },
  include: { pasos: { orderBy: { orden: "asc" } } },
});
```
La condición de la línea 248 actual es `if (proceso && proceso.tipoFlujo !== "PENDIENTE" && proceso.pasos.length > 0)`.
Dado que DB ya no tiene `tipoFlujo: "PENDIENTE"` para estos procesos (reemplazado por DIRECTO en
el plan 02-01), la condición queda correcta sin cambios adicionales. El `createMany` de pasos en
líneas 249-258 continúa funcionando porque `proceso.pasos` son `PasoDefinicion` (misma estructura:
`orden`, `rolRequerido`, `nombre`, `labelUnidades`).

Verificar que el campo `labelUnidades` existe en `PasoDefinicion` — si no existe en el tipo Prisma,
usar `(paso as any).labelUnidades ?? null` o simplemente `null`. Revisar el schema antes de decidir.
  </action>
  <verify>
    <automated>cd C:/Users/ilesm/Documents/SIAST && npx tsc --noEmit -p apps/api/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/services/tickets.service.ts` NO contiene `getProcesoInfo` (ni como import ni como llamada)
    - `apps/api/src/services/tickets.service.ts` contiene `prisma.procesoDefinicion.findFirst(`
    - `apps/api/src/services/tickets.service.ts` contiene `notIn: ["RESUELTO", "CANCELADO"]` en el bloque de técnicos de listarTickets
    - `npx tsc --noEmit` no reporta errores en `tickets.service.ts`
  </acceptance_criteria>
  <done>crearTicket usa DB; listarTickets excluye RESUELTO/CANCELADO para técnicos; sin errores TypeScript</done>
</task>

<task type="auto">
  <name>Tarea 2: Aplicar guards de flujo (D-09, D-10, D-12) y corregir completarPaso (D-08, D-13)</name>
  <files>apps/api/src/services/tickets.service.ts</files>
  <read_first>
    - apps/api/src/services/tickets.service.ts (líneas 325-430 — asignarTicket, cambiarEstado completos)
    - apps/api/src/services/tickets.service.ts (líneas 462-534 — completarPaso completo)
  </read_first>
  <action>
Continuar en `apps/api/src/services/tickets.service.ts`:

**Cambio 4 — asignarTicket (D-12):**
Después de la línea `if (!ticket) throw ...` (línea 327), ANTES de buscar al técnico, agregar:
```typescript
// Guard: tickets con flujo de pasos no se asignan manualmente (D-12)
const pasosExistentes = await prisma.pasoTicket.findMany({ where: { ticketId: id } });
if (pasosExistentes.length > 0) {
  throw Object.assign(
    new Error("Este ticket usa flujo de pasos. Asignar técnico desde el panel de pasos."),
    { status: 400 },
  );
}
```

**Cambio 5 — cambiarEstado (D-10):**
Después de la validación de transición (`if (!permitidos.includes(body.estado))`) y ANTES
del bloque `const fechas: Record<...> = {}`, agregar:
```typescript
// Guard: no resolver ticket con pasos pendientes (D-10)
if (body.estado === "RESUELTO") {
  const pasosPendientes = await prisma.pasoTicket.findMany({
    where: { ticketId: id, estado: { not: "COMPLETADO" } },
  });
  if (pasosPendientes.length > 0) {
    throw Object.assign(
      new Error("El ticket tiene pasos pendientes. Completa todos los pasos para resolver."),
      { status: 400 },
    );
  }
}
```

**Cambio 6 — completarPaso, identidad técnico (D-09):**
Después del check `if (paso.estado === "COMPLETADO")` y ANTES del check `if (paso.rolRequerido !== user.rol)`, agregar:
```typescript
// Validar identidad del técnico, no solo el rol (D-09)
if (paso.tecnicoId !== null && paso.tecnicoId !== user.id) {
  throw Object.assign(
    new Error("Solo el técnico asignado puede completar este paso"),
    { status: 403 },
  );
}
```
Si `paso.tecnicoId` es null (paso asignado por rol pero sin técnico específico), se permite
que cualquier técnico con el rol correcto lo complete (decisión conservadora per Claude's Discretion).

**Cambio 7 — completarPaso, búsqueda siguientePaso (D-08):**
Cambiar la query de `siguientePaso` (línea 493):
```typescript
// ANTES:
const siguientePaso = await prisma.pasoTicket.findFirst({
  where: { ticketId, orden: paso.orden + 1, estado: "PENDIENTE" },
});
// DESPUÉS:
const siguientePaso = await prisma.pasoTicket.findFirst({
  where: { ticketId, orden: { gt: paso.orden }, estado: { not: "COMPLETADO" } },
  orderBy: { orden: "asc" },
});
```

**Cambio 8 — completarPaso, estadoAnterior real (D-13):**
Dentro del bloque `if (!siguientePaso)` donde se resuelve el ticket, ANTES de la llamada
`prisma.ticket.update({ data: { estado: "RESUELTO" ... } })`, agregar:
```typescript
// Leer estado real antes de actualizar (D-13)
const ticketPreResolve = await prisma.ticket.findUnique({
  where: { id: ticketId },
  select: { estado: true },
});
const estadoAnteriorReal = ticketPreResolve?.estado ?? "EN_PROGRESO";
```
Luego reemplazar `estadoAnterior: "EN_PROGRESO"` (hardcodeado, líneas 507 y 517) por
`estadoAnterior: estadoAnteriorReal` en AMBAS ocurrencias dentro de ese bloque
(`historialTicket.create` y `emitirCambioEstado`).
  </action>
  <verify>
    <automated>cd C:/Users/ilesm/Documents/SIAST && npx tsc --noEmit -p apps/api/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/services/tickets.service.ts` contiene `"Este ticket usa flujo de pasos. Asignar técnico desde el panel de pasos."` (guard D-12)
    - `apps/api/src/services/tickets.service.ts` contiene `"El ticket tiene pasos pendientes. Completa todos los pasos para resolver."` (guard D-10)
    - `apps/api/src/services/tickets.service.ts` contiene `"Solo el técnico asignado puede completar este paso"` (guard D-09)
    - `apps/api/src/services/tickets.service.ts` contiene `estado: { not: "COMPLETADO" }` en la query de siguientePaso (D-08)
    - `apps/api/src/services/tickets.service.ts` contiene `estadoAnteriorReal` y NO contiene `estadoAnterior: "EN_PROGRESO"` hardcodeado en el bloque de resolución de completarPaso (D-13)
    - `npx tsc --noEmit` no reporta errores en `tickets.service.ts`
  </acceptance_criteria>
  <done>Los 6 guards y bug fixes aplicados; tickets.service.ts compila sin errores TypeScript</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request → completarPaso | Usuario autenticado envía pasoId + ticketId |
| HTTP request → cambiarEstado | Staff autenticado cambia estado directamente |
| HTTP request → asignarTicket | Admin/Mesa Ayuda asigna técnico |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Spoofing | completarPaso — técnico completa paso ajeno | mitigate | D-09: validar `paso.tecnicoId === user.id` antes de procesar; 403 si no coincide |
| T-02-02-02 | Tampering | cambiarEstado RESUELTO bypass pasos | mitigate | D-10: verificar `PasoTicket.estado != COMPLETADO` antes de resolver; 400 si existen |
| T-02-02-03 | Elevation of Privilege | asignarTicket omite guard de pasos | mitigate | D-12: verificar si ticket tiene pasos antes de asignación directa; 400 si tiene pasos |
| T-02-02-04 | Information Disclosure | DB query ProcesoDefinicion sin activo:true | mitigate | D-01: query siempre incluye `activo: true`; procesos inactivos no se exponen a nuevos tickets |
| T-02-02-05 | Tampering | completarPaso con rol correcto pero sin ser el asignado | mitigate | D-09: `tecnicoId !== null && tecnicoId !== user.id` → 403 explícito |
</threat_model>

<verification>
1. `npx tsc --noEmit -p apps/api/tsconfig.json` — sin errores
2. `grep -n "getProcesoInfo" apps/api/src/services/tickets.service.ts` — sin resultados
3. `grep -n "procesoDefinicion.findFirst" apps/api/src/services/tickets.service.ts` — muestra línea en crearTicket
4. `grep -n "estadoAnterior: \"EN_PROGRESO\"" apps/api/src/services/tickets.service.ts` — sin resultados en completarPaso
5. `grep -n "not.*COMPLETADO\|COMPLETADO.*not" apps/api/src/services/tickets.service.ts` — muestra línea D-08 y D-10
</verification>

<success_criteria>
- `tickets.service.ts` sin ninguna referencia a `getProcesoInfo`
- Todos los guards D-08 a D-13 implementados con los mensajes de error exactos del CONTEXT.md
- TypeScript compila sin errores
- Socket.IO sigue emitiendo `paso_listo` en el flujo correcto (verificar que el fix D-08 no rompa la emisión existente)
</success_criteria>

<output>
Después de completar, crear `.planning/phases/02-features-pendientes-procesos-y-flujos/02-02-SUMMARY.md`
</output>
