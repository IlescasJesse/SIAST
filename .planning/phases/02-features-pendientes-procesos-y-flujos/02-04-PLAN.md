---
phase: 02-features-pendientes-procesos-y-flujos
plan: 04
type: execute
wave: 3
depends_on:
  - 02-02
  - 02-03
files_modified:
  - packages/shared/src/index.ts
  - packages/database/prisma/seed_procesos.ts
autonomous: true
requirements:
  - PRO-01
  - PRO-04

must_haves:
  truths:
    - "PROCESO_MAP, getProcesoKey y getProcesoInfo eliminados de packages/shared/src/index.ts"
    - "seed_procesos.ts no importa PROCESO_MAP — usa datos hardcodeados locales o es archivado"
    - "El monorepo compila sin errores TypeScript después de las eliminaciones"
    - "Ningún archivo activo en el proyecto importa PROCESO_MAP, getProcesoKey o getProcesoInfo"
  artifacts:
    - path: "packages/shared/src/index.ts"
      provides: "Módulo shared sin PROCESO_MAP"
      contains: "FOLIO_PREFIX"
  key_links:
    - from: "packages/shared/src/index.ts"
      to: "consumers (tickets.service.ts, metricas.controller.ts)"
      via: "imports de @stf/shared ya no incluyen PROCESO_MAP"
      pattern: "FOLIO_PREFIX"
---

<objective>
Eliminar `PROCESO_MAP`, `getProcesoKey` y `getProcesoInfo` de `packages/shared/src/index.ts`
ahora que ningún consumer activo los usa. Actualizar `seed_procesos.ts` para que no dependa
del map eliminado.

Purpose: Limpieza final — PROCESO_MAP era el síntoma; DB es la cura. Eliminar el código
muerto evita confusión futura y reduce el riesgo de regresión.
Output: `@stf/shared` sin legado de PROCESO_MAP; `seed_procesos.ts` autónomo.

NOTA: PRO-04 (Escalamiento Recursos Materiales) está marcado como OUT OF SCOPE por decisión
del usuario en CONTEXT.md. Aparece en `requirements` solo para confirmar explícitamente que
está diferido y no se implementa en esta fase.
</objective>

<execution_context>
@C:/Users/ilesm/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ilesm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/ilesm/Documents/SIAST/.planning/ROADMAP.md
@C:/Users/ilesm/Documents/SIAST/.planning/STATE.md
@C:/Users/ilesm/Documents/SIAST/.planning/phases/02-features-pendientes-procesos-y-flujos/02-02-SUMMARY.md
@C:/Users/ilesm/Documents/SIAST/.planning/phases/02-features-pendientes-procesos-y-flujos/02-03-SUMMARY.md
</context>

<interfaces>
<!-- Bloque a eliminar de packages/shared/src/index.ts (líneas ~471-622) -->

```typescript
// Todo el bloque desde la línea del comentario "\ Mapa de procesos" hasta getProcesoInfo:

// ProcesoInfo type (puede estar antes del PROCESO_MAP — eliminar si solo es usada por él)
export type ProcesoInfo = { ... };  // líneas ~454-470 aprox

// El map completo
export const PROCESO_MAP: Record<string, ProcesoInfo> = { ... };  // líneas ~472-612

// Los dos helpers
export function getProcesoKey(subcategoria: string, subTipo?: string | null): string { ... }
export function getProcesoInfo(subcategoria: string, subTipo?: string | null): ProcesoInfo | null { ... }
```

<!-- Verificación de que los consumers ya no los usan (resultado de planes 02-02 y 02-03) -->

```
grep -rn "getProcesoInfo\|getProcesoKey\|PROCESO_MAP" apps/ packages/ --include="*.ts"
→ Solo debe aparecer en seed_procesos.ts (que también se actualiza en este plan)
→ NO debe aparecer en tickets.service.ts ni metricas.controller.ts
```
</interfaces>

<tasks>

<task type="auto">
  <name>Tarea 1: Verificar consumers y eliminar PROCESO_MAP de @stf/shared</name>
  <files>packages/shared/src/index.ts</files>
  <read_first>
    - packages/shared/src/index.ts (líneas 450-624 — bloque ProcesoInfo + PROCESO_MAP + helpers)
    - apps/api/src/services/tickets.service.ts (línea 6 — confirmar que getProcesoInfo ya no está importado)
    - apps/api/src/controllers/metricas.controller.ts (línea 5 — confirmar que PROCESO_MAP ya no está importado)
  </read_first>
  <action>
**Paso 0 — Verificación pre-eliminación (OBLIGATORIO):**
Antes de tocar `packages/shared/src/index.ts`, correr:
```bash
grep -rn "getProcesoInfo\|getProcesoKey\|PROCESO_MAP" apps/ --include="*.ts"
```
Si aparece algún resultado en `apps/api/src/services/tickets.service.ts` o
`apps/api/src/controllers/metricas.controller.ts` → DETENER y marcar como bloqueado.
Los planes 02-02 y 02-03 deben estar completos antes de proceder.

**Paso 1 — Identificar líneas exactas:**
Leer `packages/shared/src/index.ts` líneas 450-624 para confirmar los límites del bloque.
Buscar:
- Inicio del tipo `ProcesoInfo` (tipo que solo usa PROCESO_MAP)
- Inicio de `export const PROCESO_MAP`
- Fin de `getProcesoInfo` (última función a eliminar)

**Paso 2 — Eliminar el bloque completo:**
Eliminar de `packages/shared/src/index.ts`:
- `export type ProcesoInfo` — si y solo si no es usada por nada más (grep para confirmar)
- `export const PROCESO_MAP` — bloque completo
- `export function getProcesoKey` — función completa
- `export function getProcesoInfo` — función completa

Mantener intacto TODO lo demás: `FOLIO_PREFIX`, `SUBTIPO_SISTEMAS`, enums, schemas Zod,
`SUBCATEGORIAS_POR_CATEGORIA`, `LABEL_SUBCATEGORIA`, etc.

**Paso 3 — Compilar shared para verificar:**
```bash
cd C:/Users/ilesm/Documents/SIAST && npx tsc --noEmit -p packages/shared/tsconfig.json 2>&1
```
  </action>
  <verify>
    <automated>cd C:/Users/ilesm/Documents/SIAST && grep -rn "getProcesoInfo\|getProcesoKey\|PROCESO_MAP" apps/ packages/shared/ --include="*.ts" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `packages/shared/src/index.ts` NO contiene `PROCESO_MAP`
    - `packages/shared/src/index.ts` NO contiene `getProcesoKey`
    - `packages/shared/src/index.ts` NO contiene `getProcesoInfo`
    - `grep -rn "getProcesoInfo\|getProcesoKey\|PROCESO_MAP" apps/ --include="*.ts"` retorna cero resultados en archivos de `apps/`
    - `packages/shared/src/index.ts` aún contiene `FOLIO_PREFIX` y `SUBTIPO_SISTEMAS`
    - `npx tsc --noEmit -p packages/shared/tsconfig.json` no reporta errores
  </acceptance_criteria>
  <done>PROCESO_MAP y helpers eliminados de @stf/shared; módulo shared compila sin errores</done>
</task>

<task type="auto">
  <name>Tarea 2: Actualizar seed_procesos.ts para no depender de PROCESO_MAP eliminado</name>
  <files>packages/database/prisma/seed_procesos.ts</files>
  <read_first>
    - packages/database/prisma/seed_procesos.ts (líneas 1-93 — implementación completa actual)
  </read_first>
  <action>
`seed_procesos.ts` actualmente importa `PROCESO_MAP` de `@stf/shared` y lo itera para poblar DB.
Con PROCESO_MAP eliminado, el archivo romperá la compilación.

**Estrategia:** Definir el mapa de procesos localmente en el mismo archivo, usando la misma
estructura de `ProcesoInfo` pero como tipo local (sin depender del tipo exportado que también
se eliminó).

Reescribir `seed_procesos.ts` para que:

1. No importe nada de `@stf/shared` (eliminar `import { PROCESO_MAP } from "@stf/shared"`)
2. Defina un tipo local `type ProcesoSeedInfo` con la misma estructura:
```typescript
type ProcesoSeedInfo = {
  nombre: string;
  tipoFlujo: "DIRECTO" | "SECUENCIAL" | "PENDIENTE";
  descripcion?: string;
  pasos: Array<{
    orden: number;
    rolRequerido: string;
    nombre: string;
    descripcion?: string;
    registraUnidades?: boolean;
    labelUnidades?: string;
  }>;
};
```
3. Defina un `PROCESO_SEED_MAP: Record<string, ProcesoSeedInfo>` local con TODOS los
procesos que actualmente están en `PROCESO_MAP` de `@stf/shared`. Para obtener los valores
exactos, leer `packages/shared/src/index.ts` líneas 472-612 ANTES de eliminarlos (o leer el
estado actual si ya fueron eliminados en la Tarea 1 del mismo plan y el git diff los tiene).

Los procesos a incluir son (basado en CONTEXT.md y PATTERNS.md — todos los que existían más
los nuevos DIRECTO):
- `EQUIPOS_DISPOSITIVOS:EQUIPO_NUEVO_CON_RED` — proceso existente
- Todos los demás procesos de EQUIPOS_DISPOSITIVOS con subtipos
- `SISTEMAS_INSTITUCIONALES:SIRH` — `tipoFlujo: "DIRECTO"`, 1 paso TECNICO_TI (D-07)
- `SISTEMAS_INSTITUCIONALES:SIAST` — `tipoFlujo: "DIRECTO"`, 1 paso TECNICO_TI (D-07)
- Cualquier otro proceso que estuviera en PROCESO_MAP

IMPORTANTE: Leer `packages/shared/src/index.ts` ANTES de ejecutar la Tarea 1 para capturar
todos los procesos. Si la Tarea 1 ya eliminó el bloque, usar `git diff HEAD~1 packages/shared/src/index.ts`
para recuperar los valores.

4. La función `seedProcesos(prisma)` conserva exactamente la misma lógica de findFirst + update/create
— solo cambia que itera sobre `PROCESO_SEED_MAP` local en lugar del `PROCESO_MAP` importado.

5. Mantener el bloque de ejecución standalone al final del archivo.

Verificar compilación:
```bash
cd C:/Users/ilesm/Documents/SIAST && npx tsc --noEmit -p packages/database/tsconfig.json 2>&1
```
  </action>
  <verify>
    <automated>cd C:/Users/ilesm/Documents/SIAST && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `packages/database/prisma/seed_procesos.ts` NO contiene `import.*@stf/shared`
    - `packages/database/prisma/seed_procesos.ts` contiene `PROCESO_SEED_MAP` (mapa local)
    - `packages/database/prisma/seed_procesos.ts` contiene `"SISTEMAS_INSTITUCIONALES:SIRH"` con `tipoFlujo: "DIRECTO"`
    - `packages/database/prisma/seed_procesos.ts` contiene `"SISTEMAS_INSTITUCIONALES:SIAST"` con `tipoFlujo: "DIRECTO"`
    - `npx tsc --noEmit` (monorepo completo) no reporta errores
    - `npm run db:seed` ejecuta sin errores TypeScript
  </acceptance_criteria>
  <done>seed_procesos.ts autónomo; monorepo completo compila sin errores TypeScript</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| packages/shared → consumers | Módulo shared exporta solo lo que es seguro y necesario |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Tampering | Eliminación accidental de exports necesarios de @stf/shared | mitigate | Verificación pre-eliminación con grep antes de modificar; compilación TS como gate |
| T-02-04-02 | Denial of Service | seed_procesos.ts con datos incorrectos rompe DB al re-sembrar | mitigate | La lógica es idempotente (findFirst + update); datos de SIRH/SIAST definidos explícitamente |
</threat_model>

<verification>
1. `grep -rn "PROCESO_MAP\|getProcesoKey\|getProcesoInfo" apps/ packages/ --include="*.ts"` — cero resultados
2. `npx tsc --noEmit` desde raíz del monorepo — sin errores
3. `npm run db:seed` — ejecuta exitosamente con entradas CREATED/UPDATED para todos los procesos
4. `npm run dev:api` — API arranca sin errores de importación
</verification>

<success_criteria>
- Monorepo completo compila con `npx tsc --noEmit` sin errores
- Ningún archivo activo referencia `PROCESO_MAP`, `getProcesoKey` o `getProcesoInfo`
- `seed_procesos.ts` es autónomo y funcional
- PRO-04 (Escalamiento RM) confirmado como DIFERIDO — no implementado en esta fase (decisión en CONTEXT.md)
</success_criteria>

<output>
Después de completar, crear `.planning/phases/02-features-pendientes-procesos-y-flujos/02-04-SUMMARY.md`
</output>
